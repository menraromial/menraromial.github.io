<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://menraromial.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://menraromial.com/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-04-01T19:50:24+00:00</updated><id>https://menraromial.com/feed.xml</id><title type="html">blank</title><subtitle>MENRA W. Romial (Menra Wedwang Romial) - Personal website and blog showcasing expertise in cloud computing, edge technologies, IoT, and Kubernetes. Computer science researcher specializing in energy-aware computing, green cloud infrastructure, and sustainable data centers. Explore research insights, technical articles, talks, and innovative projects in distributed systems and energy-efficient orchestration.
</subtitle><entry><title type="html">A Comprehensive Guide to powercap-utils on Linux</title><link href="https://menraromial.com/blog/2025/comprehensive-guide-powercap-utils-linux/" rel="alternate" type="text/html" title="A Comprehensive Guide to powercap-utils on Linux" /><published>2025-10-23T13:00:00+00:00</published><updated>2025-10-23T13:00:00+00:00</updated><id>https://menraromial.com/blog/2025/comprehensive-guide-powercap-utils-linux</id><content type="html" xml:base="https://menraromial.com/blog/2025/comprehensive-guide-powercap-utils-linux/"><![CDATA[<h3 id="1-introduction-what-is-powercap-utils">1. Introduction: What is <code class="language-plaintext highlighter-rouge">powercap-utils</code>?</h3>

<p><code class="language-plaintext highlighter-rouge">powercap-utils</code> is a suite of user-space command-line tools for Linux that allow you to interact with the kernel’s <strong>Power Capping Framework</strong>. This framework provides a standardized way to monitor and limit the power consumption of hardware devices.</p>

<p>Its most common use is to control <strong>Intel’s Running Average Power Limit (RAPL)</strong> and <strong>AMD’s RAPL-like features</strong> on modern CPUs. These features let you directly set power budgets for the CPU package, its cores, and DRAM.</p>

<h3 id="2-prerequisites-and-installation">2. Prerequisites and Installation</h3>

<h4 id="prerequisites">Prerequisites</h4>
<ol>
  <li><strong>A Linux System:</strong> Any modern Linux distribution.</li>
  <li><strong>Supported Hardware:</strong>
    <ul>
      <li>Intel Sandy Bridge and newer processors.</li>
      <li>Modern AMD Zen-based processors.</li>
    </ul>
  </li>
  <li><strong>Kernel Support:</strong> The <code class="language-plaintext highlighter-rouge">intel_rapl</code> (for Intel) or <code class="language-plaintext highlighter-rouge">amd_energy</code> (for AMD) kernel module must be loaded. This is typically automatic.</li>
</ol>

<h4 id="installation">Installation</h4>
<p>The package name is usually <code class="language-plaintext highlighter-rouge">powercap-utils</code>.</p>

<ul>
  <li><strong>On Debian / Ubuntu:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>powercap-utils
</code></pre></div>    </div>
  </li>
  <li><strong>On Fedora / CentOS / RHEL:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>dnf <span class="nb">install </span>powercap-utils
</code></pre></div>    </div>
  </li>
  <li><strong>On Arch Linux:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>pacman <span class="nt">-S</span> powercap-utils
</code></pre></div>    </div>
  </li>
</ul>

<h3 id="3-core-concepts-of-the-power-capping-framework">3. Core Concepts of the Power Capping Framework</h3>

<p>The framework is organized as a hierarchy, which you navigate using the tools.</p>

<p><img src="/assets/img/posts/rapl_domains.png" alt="RAPL domains diagram showing package, core, uncore, and DRAM" style="max-width:100%;height:auto;display:block;margin:0 auto;" /></p>

<p><em>Figure — RAPL domains: package, core, uncore, and DRAM.</em></p>

<ul>
  <li><strong>Control Type:</strong> The top-level driver. This will be <code class="language-plaintext highlighter-rouge">intel-rapl</code> on Intel systems.</li>
  <li><strong>Zone:</strong> A device or a part of a device that can be power-managed. Common zones are:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">package-0</code> (or <code class="language-plaintext highlighter-rouge">0</code>): The entire CPU socket. This is the most important zone.</li>
      <li><code class="language-plaintext highlighter-rouge">core</code> (sub-zone): A group of CPU cores.</li>
      <li><code class="language-plaintext highlighter-rouge">uncore</code> (sub-zone): Components like the L3 cache and memory controller.</li>
      <li><code class="language-plaintext highlighter-rouge">dram</code> (sub-zone): The DRAM interface.</li>
    </ul>
  </li>
  <li><strong>Constraint:</strong> A specific power limiting rule within a zone.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">Constraint 0</code>: The <strong><code class="language-plaintext highlighter-rouge">long_term</code></strong> limit. This is the sustained power limit, similar to the CPU’s TDP (Thermal Design Power).</li>
      <li><code class="language-plaintext highlighter-rouge">Constraint 1</code>: The <strong><code class="language-plaintext highlighter-rouge">short_term</code></strong> limit. This is a higher power limit that the CPU is allowed to use for a short burst of time.</li>
    </ul>
  </li>
  <li><strong>Parameters:</strong> Each constraint has settings you can read and write:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">power_limit_uw</code>: The power limit in <strong>microwatts (µW)</strong>.</li>
      <li><code class="language-plaintext highlighter-rouge">time_window_us</code>: The time period in <strong>microseconds (µs)</strong> over which the power limit is enforced.</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p><strong>⚠️ Critical Note on Units:</strong> The tools operate in micro-units. This is the most common source of error.</p>
  <ul>
    <li>To set a limit of <strong>50 Watts</strong>, you must use <code class="language-plaintext highlighter-rouge">50000000</code> microwatts.</li>
    <li>To set a time window of <strong>1 second</strong>, you must use <code class="language-plaintext highlighter-rouge">1000000</code> microseconds.</li>
  </ul>
</blockquote>

<hr />

<h3 id="4-the-tools-powercap-info-and-powercap-set">4. The Tools: <code class="language-plaintext highlighter-rouge">powercap-info</code> and <code class="language-plaintext highlighter-rouge">powercap-set</code></h3>

<h4 id="part-1-exploring-with-powercap-info-read-only">Part 1: Exploring with <code class="language-plaintext highlighter-rouge">powercap-info</code> (Read-Only)</h4>

<p><code class="language-plaintext highlighter-rouge">powercap-info</code> is your tool for discovery. It’s safe to use as it only reads information.</p>

<p><strong>1. List all power capping control types and their full hierarchy:</strong>
This is the most common command to get an overview of your system.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>powercap-info
</code></pre></div></div>
<p><em>Output (example from an Intel CPU):</em></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>intel-rapl
└── package-0
    ├── core
    ├── uncore
    └── dram
</code></pre></div></div>
<p>This shows the control type is <code class="language-plaintext highlighter-rouge">intel-rapl</code> and the primary zone is <code class="language-plaintext highlighter-rouge">package-0</code>.</p>

<p><strong>2. Get detailed information for a specific zone:</strong>
Based on the <code class="language-plaintext highlighter-rouge">--help</code> output, the correct syntax is <code class="language-plaintext highlighter-rouge">powercap-info &lt;control-type-name&gt; -z &lt;zone-id&gt;</code>.</p>

<p>Let’s inspect <code class="language-plaintext highlighter-rouge">package-0</code>, which corresponds to zone <code class="language-plaintext highlighter-rouge">0</code>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>powercap-info intel-rapl <span class="nt">-z</span> 0
</code></pre></div></div>
<p><em>Output (abbreviated):</em></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>package-0:
  enabled: 1
  energy_uj: 1234567890
  constraints:
    0: long_term
      power_limit_uw: 65000000
      time_window_us: 28000000
      name: long_term
    1: short_term
      power_limit_uw: 85000000
      time_window_us: 2441
      name: short_term
</code></pre></div></div>
<p><strong>Explanation:</strong></p>
<ul>
  <li>This package has a <code class="language-plaintext highlighter-rouge">long_term</code> limit (constraint <code class="language-plaintext highlighter-rouge">0</code>) of 65,000,000 µW (<strong>65 W</strong>).</li>
  <li>It has a <code class="language-plaintext highlighter-rouge">short_term</code> limit (constraint <code class="language-plaintext highlighter-rouge">1</code>) of 85,000,000 µW (<strong>85 W</strong>).</li>
  <li><code class="language-plaintext highlighter-rouge">energy_uj</code> is the total energy consumed (in microjoules) since boot.</li>
</ul>

<p><strong>3. Get a single value:</strong>
The <code class="language-plaintext highlighter-rouge">--help</code> shows many flags to query specific values. For example, to get <em>only</em> the power limit for the <code class="language-plaintext highlighter-rouge">long_term</code> constraint:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Syntax: powercap-info &lt;control-type&gt; -z &lt;zone&gt; -c &lt;constraint&gt; --c-power-limit</span>
powercap-info intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 0 <span class="nt">-l</span>
</code></pre></div></div>
<p><em>Output:</em></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>65000000
</code></pre></div></div>
<p>This is excellent for scripting, as it gives you a clean number to work with.</p>

<h4 id="part-2-setting-limits-with-powercap-set-writemodify">Part 2: Setting Limits with <code class="language-plaintext highlighter-rouge">powercap-set</code> (Write/Modify)</h4>

<p><code class="language-plaintext highlighter-rouge">powercap-set</code> is used to change the power settings. <strong>You will need <code class="language-plaintext highlighter-rouge">sudo</code> for this.</strong></p>

<p><strong>Syntax:</strong> <code class="language-plaintext highlighter-rouge">sudo powercap-set &lt;control-type-name&gt; [options]</code></p>

<p><strong>Key Options:</strong></p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">-z, --zone=&lt;zone-id&gt;</code>: Specifies the zone to modify (e.g., <code class="language-plaintext highlighter-rouge">-z 0</code>).</li>
  <li><code class="language-plaintext highlighter-rouge">-c, --constraint=&lt;constraint-id&gt;</code>: Specifies the constraint (<code class="language-plaintext highlighter-rouge">0</code> for long_term, <code class="language-plaintext highlighter-rouge">1</code> for short_term).</li>
  <li><code class="language-plaintext highlighter-rouge">-l, --c-power-limit=&lt;uw&gt;</code>: Sets the power limit in <strong>microwatts</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">-s, --c-time-window=&lt;us&gt;</code>: Sets the time window in <strong>microseconds</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">-e, --z-enabled=1|0</code>: Enables (<code class="language-plaintext highlighter-rouge">1</code>) or disables (<code class="language-plaintext highlighter-rouge">0</code>) an entire zone.</li>
</ul>

<p><strong>Example Workflow: Limiting a CPU to 35 Watts</strong></p>

<p>Let’s cap the entire CPU package (<code class="language-plaintext highlighter-rouge">package-0</code>, which is zone <code class="language-plaintext highlighter-rouge">0</code>) to a sustained 35 Watts.</p>

<p><strong>Step 1: Identify the current limits.</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>powercap-info intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 0
</code></pre></div></div>
<p>Let’s assume the current limit is <code class="language-plaintext highlighter-rouge">65000000</code> (65W).</p>

<p><strong>Step 2: Set the new power limit.</strong>
We want to set a 35W limit for the <code class="language-plaintext highlighter-rouge">long_term</code> constraint (ID <code class="language-plaintext highlighter-rouge">0</code>).
35 Watts = <code class="language-plaintext highlighter-rouge">35000000</code> microwatts.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>powercap-set intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 0 <span class="nt">-l</span> 35000000
</code></pre></div></div>

<p><strong>Step 3: Verify the change.</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>powercap-info intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 0 <span class="nt">-l</span>
</code></pre></div></div>
<p><em>New Output:</em></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>35000000
</code></pre></div></div>
<p>Success! The CPU’s sustained power limit is now 35W.</p>

<h3 id="5-practical-example-setting-a-custom-boost-and-sustained-limit">5. Practical Example: Setting a Custom Boost and Sustained Limit</h3>

<p><strong>Goal:</strong> Configure a CPU to run at a sustained 60W but allow it to boost to 90W for up to 15 seconds.</p>

<p><strong>1. Set the <code class="language-plaintext highlighter-rouge">long_term</code> (sustained) limit:</strong></p>
<ul>
  <li>Constraint: <code class="language-plaintext highlighter-rouge">0</code></li>
  <li>Power: 60W = <code class="language-plaintext highlighter-rouge">60000000</code> µW</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>powercap-set intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 0 <span class="nt">-l</span> 60000000
</code></pre></div></div>
<p><em>(The default time window for <code class="language-plaintext highlighter-rouge">long_term</code> is usually fine, so we don’t need to set it.)</em></p>

<p><strong>2. Set the <code class="language-plaintext highlighter-rouge">short_term</code> (boost) limit:</strong></p>
<ul>
  <li>Constraint: <code class="language-plaintext highlighter-rouge">1</code></li>
  <li>Power: 90W = <code class="language-plaintext highlighter-rouge">90000000</code> µW</li>
  <li>Time Window: 15 seconds = <code class="language-plaintext highlighter-rouge">15000000</code> µs</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>powercap-set intel-rapl <span class="nt">-z</span> 0 <span class="nt">-c</span> 1 <span class="nt">-l</span> 90000000 <span class="nt">-s</span> 15000000
</code></pre></div></div>

<p><strong>3. Verify all settings:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>powercap-info intel-rapl <span class="nt">-z</span> 0
</code></pre></div></div>
<p>Check the output to confirm that both <code class="language-plaintext highlighter-rouge">constraint 0</code> and <code class="language-plaintext highlighter-rouge">constraint 1</code> now reflect your new values. The CPU will now adhere to this custom power profile.</p>

<h3 id="6-scripting-and-automation">6. Scripting and Automation</h3>

<p>Because the tools can output clean numbers, they are perfect for scripts.</p>

<p><strong>Example: A script to toggle a “low power” mode (25W) and a “high power” mode (80W).</strong></p>

<p><strong><code class="language-plaintext highlighter-rouge">power-mode.sh</code></strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># Check for root privileges</span>
<span class="k">if</span> <span class="o">[[</span> <span class="nv">$EUID</span> <span class="nt">-ne</span> 0 <span class="o">]]</span><span class="p">;</span> <span class="k">then
   </span><span class="nb">echo</span> <span class="s2">"This script must be run as root"</span> 
   <span class="nb">exit </span>1
<span class="k">fi

</span><span class="nv">MODE</span><span class="o">=</span><span class="nv">$1</span>
<span class="nv">CONTROL_TYPE</span><span class="o">=</span><span class="s2">"intel-rapl"</span>
<span class="nv">ZONE</span><span class="o">=</span><span class="s2">"0"</span>
<span class="nv">CONSTRAINT</span><span class="o">=</span><span class="s2">"0"</span> <span class="c"># We will only change the long_term limit</span>

<span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$MODE</span><span class="s2">"</span> <span class="o">==</span> <span class="s2">"low"</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
    </span><span class="nv">POWER_LIMIT_UW</span><span class="o">=</span>25000000 <span class="c"># 25 Watts</span>
    <span class="nb">echo</span> <span class="s2">"Setting low power mode (25W)..."</span>
<span class="k">elif</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$MODE</span><span class="s2">"</span> <span class="o">==</span> <span class="s2">"high"</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
    </span><span class="nv">POWER_LIMIT_UW</span><span class="o">=</span>80000000 <span class="c"># 80 Watts</span>
    <span class="nb">echo</span> <span class="s2">"Setting high power mode (80W)..."</span>
<span class="k">else
    </span><span class="nb">echo</span> <span class="s2">"Usage: sudo </span><span class="nv">$0</span><span class="s2"> &lt;low|high&gt;"</span>
    <span class="nb">exit </span>1
<span class="k">fi

</span>powercap-set <span class="s2">"</span><span class="nv">$CONTROL_TYPE</span><span class="s2">"</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$ZONE</span><span class="s2">"</span> <span class="nt">-c</span> <span class="s2">"</span><span class="nv">$CONSTRAINT</span><span class="s2">"</span> <span class="nt">-l</span> <span class="s2">"</span><span class="nv">$POWER_LIMIT_UW</span><span class="s2">"</span>

<span class="c"># Verify</span>
<span class="nv">CURRENT_LIMIT</span><span class="o">=</span><span class="si">$(</span>powercap-info <span class="s2">"</span><span class="nv">$CONTROL_TYPE</span><span class="s2">"</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$ZONE</span><span class="s2">"</span> <span class="nt">-c</span> <span class="s2">"</span><span class="nv">$CONSTRAINT</span><span class="s2">"</span> <span class="nt">-l</span><span class="si">)</span>
<span class="nv">CURRENT_LIMIT_W</span><span class="o">=</span><span class="k">$((</span>CURRENT_LIMIT <span class="o">/</span> <span class="m">1000000</span><span class="k">))</span>
<span class="nb">echo</span> <span class="s2">"Successfully set power limit to </span><span class="k">${</span><span class="nv">CURRENT_LIMIT_W</span><span class="k">}</span><span class="s2">W."</span>
</code></pre></div></div>

<p><strong>How to use the script:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x power-mode.sh

<span class="c"># Set low power mode</span>
<span class="nb">sudo</span> ./power-mode.sh low

<span class="c"># Set high power mode</span>
<span class="nb">sudo</span> ./power-mode.sh high
</code></pre></div></div>

<h3 id="7-conclusion">7. Conclusion</h3>

<p><code class="language-plaintext highlighter-rouge">powercap-utils</code> gives you precise, low-level control over your CPU’s power consumption. By understanding the hierarchy of control types, zones, and constraints, you can effectively use these tools to manage thermals, optimize performance, and control energy usage.</p>

<p><strong>Key Takeaways:</strong></p>
<ul>
  <li>Use <code class="language-plaintext highlighter-rouge">powercap-info</code> to explore the available zones and constraints on your system.</li>
  <li>Use <code class="language-plaintext highlighter-rouge">sudo powercap-set</code> to apply limits, always remembering that units are in <strong>microwatts</strong> and <strong>microseconds</strong>.</li>
  <li>The primary targets are usually <code class="language-plaintext highlighter-rouge">zone 0</code> (<code class="language-plaintext highlighter-rouge">package-0</code>), with <code class="language-plaintext highlighter-rouge">constraint 0</code> (<code class="language-plaintext highlighter-rouge">long_term</code>) for sustained limits and <code class="language-plaintext highlighter-rouge">constraint 1</code> (<code class="language-plaintext highlighter-rouge">short_term</code>) for boost behavior.</li>
  <li>The single-value flags (like <code class="language-plaintext highlighter-rouge">-l</code> for <code class="language-plaintext highlighter-rouge">powercap-info</code>) are incredibly useful for scripting and verification.</li>
</ul>]]></content><author><name></name></author><category term="tutorial" /><category term="linux" /><category term="powercap" /><category term="rapl" /><summary type="html"><![CDATA[powercap-utils is a suite of user-space command-line tools for Linux that allow you to interact with the kernel's Power Capping Framework. This framework provides a standardized way to monitor and limit the power consumption of hardware devices.]]></summary></entry><entry><title type="html">Building a Kubernetes Controller with Kubebuilder from Scratch</title><link href="https://menraromial.com/blog/2025/building-a-kubernetes-controller-with-kubebuilder-from-scratch/" rel="alternate" type="text/html" title="Building a Kubernetes Controller with Kubebuilder from Scratch" /><published>2025-09-22T10:30:00+00:00</published><updated>2025-09-22T10:30:00+00:00</updated><id>https://menraromial.com/blog/2025/building-a-kubernetes-controller-with-kubebuilder-from-scratch</id><content type="html" xml:base="https://menraromial.com/blog/2025/building-a-kubernetes-controller-with-kubebuilder-from-scratch/"><![CDATA[<p>In this tutorial, you will learn how to build a custom Kubernetes controller (also known as an Operator) using Kubebuilder. We will create a Custom Resource Definition (CRD) for a simple <code class="language-plaintext highlighter-rouge">Website</code> resource. The goal of our controller will be to watch for these <code class="language-plaintext highlighter-rouge">Website</code> resources and, for each one, create a corresponding Kubernetes <code class="language-plaintext highlighter-rouge">Deployment</code> and <code class="language-plaintext highlighter-rouge">Service</code>.</p>

<p>This is a powerful pattern that allows you to extend the Kubernetes API to manage your own applications declaratively.</p>

<h4 id="what-we-are-building"><strong>What We Are Building</strong></h4>

<p>We will define a <code class="language-plaintext highlighter-rouge">Website</code> custom resource that looks like this:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># config/samples/website_v1alpha1_website.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">website.my.domain/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Website</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-first-website</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="c1"># The git repository URL containing the website's static files</span>
  <span class="na">gitRepo</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://github.com/someuser/my-static-site.git"</span>
  <span class="c1"># The number of replicas for the web server</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
</code></pre></div></div>

<p>Our controller will see this resource and automatically create:</p>
<ol>
  <li>A <code class="language-plaintext highlighter-rouge">Deployment</code> to run an Nginx server with the specified number of <code class="language-plaintext highlighter-rouge">replicas</code>. We’ll imagine it serves content from the <code class="language-plaintext highlighter-rouge">gitRepo</code>.</li>
  <li>A <code class="language-plaintext highlighter-rouge">Service</code> of type <code class="language-plaintext highlighter-rouge">LoadBalancer</code> to expose the Nginx pods to the internet.</li>
</ol>

<hr />

<h3 id="prerequisites"><strong>Prerequisites</strong></h3>

<p>Before you begin, make sure you have the following tools installed:</p>

<ol>
  <li><strong>Go</strong> (version 1.19+): <a href="https://golang.org/dl/">https://golang.org/dl/</a></li>
  <li><strong>Docker</strong>: To build container images.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">kubectl</code></strong>: The Kubernetes command-line tool.</li>
  <li><strong>A Kubernetes Cluster</strong>: A local one like <a href="https://minikube.sigs.k8s.io/docs/start/">Minikube</a> or <a href="https://kind.sigs.k8s.io/docs/user/quick-start/">Kind</a> is perfect for development.</li>
  <li><strong>Kubebuilder</strong>: The scaffolding tool.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install Kubebuilder</span>
<span class="nv">os</span><span class="o">=</span><span class="si">$(</span>go <span class="nb">env </span>GOOS<span class="si">)</span>
<span class="nb">arch</span><span class="o">=</span><span class="si">$(</span>go <span class="nb">env </span>GOARCH<span class="si">)</span>
curl <span class="nt">-L</span> <span class="nt">-o</span> kubebuilder <span class="s2">"https://go.kubebuilder.io/dl/latest/</span><span class="k">${</span><span class="nv">os</span><span class="k">}</span><span class="s2">/</span><span class="k">${</span><span class="nv">arch</span><span class="k">}</span><span class="s2">"</span>
<span class="nb">sudo mv </span>kubebuilder /usr/local/bin/
</code></pre></div>    </div>
  </li>
</ol>

<hr />

<h3 id="step-1-initialize-your-project"><strong>Step 1: Initialize Your Project</strong></h3>

<p>First, create a new directory for your project and initialize it with Kubebuilder.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create and enter the project directory</span>
<span class="nb">mkdir </span>website-operator
<span class="nb">cd </span>website-operator

<span class="c"># Initialize the project</span>
<span class="c"># --domain is used for the API group of your CRDs</span>
<span class="c"># --repo is your Go module path (e.g., github.com/your-user/website-operator)</span>
kubebuilder init <span class="nt">--domain</span> my.domain <span class="nt">--repo</span> github.com/your-user/website-operator
</code></pre></div></div>

<p>This command scaffolds a complete project structure, including a <code class="language-plaintext highlighter-rouge">Makefile</code>, a <code class="language-plaintext highlighter-rouge">main.go</code> file, and a <code class="language-plaintext highlighter-rouge">Dockerfile</code>.</p>

<h3 id="step-2-create-the-api-crd-and-controller"><strong>Step 2: Create the API (CRD and Controller)</strong></h3>

<p>Now, let’s create the API for our <code class="language-plaintext highlighter-rouge">Website</code> resource. This single command will generate the CRD definition, the Go struct for our resource, and a skeleton for our controller.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create the API. We'll create the resource and the controller.</span>
kubebuilder create api <span class="nt">--group</span> website <span class="nt">--version</span> v1alpha1 <span class="nt">--kind</span> Website
</code></pre></div></div>
<p>When prompted, answer <code class="language-plaintext highlighter-rouge">y</code> to create both the Resource and the Controller.</p>

<p>This command does several important things:</p>
<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">api/v1alpha1/website_types.go</code></strong>: This file defines the Go structs that represent your <code class="language-plaintext highlighter-rouge">Website</code> resource. This is where you define your <code class="language-plaintext highlighter-rouge">Spec</code> (the desired state) and <code class="language-plaintext highlighter-rouge">Status</code> (the observed state).</li>
  <li><strong><code class="language-plaintext highlighter-rouge">controllers/website_controller.go</code></strong>: This is the skeleton for your controller’s logic.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">config/crd/bases/website.my.domain_websites.yaml</code></strong>: The YAML definition for your CRD.</li>
</ul>

<h4 id="defining-the-spec-and-status"><strong>Defining the <code class="language-plaintext highlighter-rouge">Spec</code> and <code class="language-plaintext highlighter-rouge">Status</code></strong></h4>

<p>Open <code class="language-plaintext highlighter-rouge">api/v1alpha1/website_types.go</code>. We need to define the fields for our resource.</p>

<p>Find the <code class="language-plaintext highlighter-rouge">WebsiteSpec</code> and <code class="language-plaintext highlighter-rouge">WebsiteStatus</code> structs and modify them as follows.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// api/v1alpha1/website_types.go</span>

<span class="c">// ... (other imports)</span>

<span class="c">// WebsiteSpec defines the desired state of Website</span>
<span class="k">type</span> <span class="n">WebsiteSpec</span> <span class="k">struct</span> <span class="p">{</span>
	<span class="c">// INSERT ADDITIONAL SPEC FIELDS - desired state of cluster</span>
	<span class="c">// Important: Run "make" to regenerate code after modifying this file</span>

	<span class="c">// GitRepo is the git repository URL for the website's content.</span>
	<span class="c">// +kubebuilder:validation:Required</span>
	<span class="n">GitRepo</span> <span class="kt">string</span> <span class="s">`json:"gitRepo"`</span>

	<span class="c">// Replicas is the number of desired pods. Defaults to 1.</span>
	<span class="c">// +kubebuilder:validation:Minimum=1</span>
	<span class="c">// +kubebuilder:default=1</span>
	<span class="c">// +optional</span>
	<span class="n">Replicas</span> <span class="o">*</span><span class="kt">int32</span> <span class="s">`json:"replicas"`</span>
<span class="p">}</span>

<span class="c">// WebsiteStatus defines the observed state of Website</span>
<span class="k">type</span> <span class="n">WebsiteStatus</span> <span class="k">struct</span> <span class="p">{</span>
	<span class="c">// INSERT ADDITIONAL STATUS FIELD - define observed state of cluster</span>
	<span class="c">// Important: Run "make" to regenerate code after modifying this file</span>

	<span class="c">// DeploymentStatus reflects the status of the created Deployment.</span>
	<span class="c">// +optional</span>
	<span class="n">DeploymentStatus</span> <span class="n">appsv1</span><span class="o">.</span><span class="n">DeploymentStatus</span> <span class="s">`json:"deploymentStatus,omitempty"`</span>

	<span class="c">// ServiceStatus reflects the status of the created Service.</span>
	<span class="c">// +optional</span>
	<span class="n">ServiceStatus</span> <span class="n">v1</span><span class="o">.</span><span class="n">ServiceStatus</span> <span class="s">`json:"serviceStatus,omitempty"`</span>
<span class="p">}</span>
<span class="c">// ... (rest of the file)</span>
</code></pre></div></div>
<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">WebsiteSpec</code></strong>: We added <code class="language-plaintext highlighter-rouge">GitRepo</code> (a required string) and <code class="language-plaintext highlighter-rouge">Replicas</code> (an optional integer with a minimum value of 1). The <code class="language-plaintext highlighter-rouge">//+kubebuilder</code> comments are “markers” that generate validation rules in the CRD.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">WebsiteStatus</code></strong>: We will use this to report the status of the <code class="language-plaintext highlighter-rouge">Deployment</code> and <code class="language-plaintext highlighter-rouge">Service</code> we create. You’ll need to add the <code class="language-plaintext highlighter-rouge">appsv1</code> and <code class="language-plaintext highlighter-rouge">v1</code> imports from <code class="language-plaintext highlighter-rouge">k8s.io/api</code>.</li>
</ul>

<p>After modifying the <code class="language-plaintext highlighter-rouge">_types.go</code> file, <strong>always run <code class="language-plaintext highlighter-rouge">make</code></strong> to regenerate the deepcopy functions and CRD manifests.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make
</code></pre></div></div>

<h3 id="step-3-implement-the-controller-logic"><strong>Step 3: Implement the Controller Logic</strong></h3>

<p>This is the most important part. The controller’s job is to read the state of a <code class="language-plaintext highlighter-rouge">Website</code> resource and take action to make the real world (the cluster state) match the desired state in the <code class="language-plaintext highlighter-rouge">Spec</code>.</p>

<p>Open <code class="language-plaintext highlighter-rouge">internal/controllers/website_controller.go</code>. The core logic goes inside the <code class="language-plaintext highlighter-rouge">Reconcile</code> function.</p>

<h4 id="the-reconciliation-loop"><strong>The Reconciliation Loop</strong></h4>

<p>The <code class="language-plaintext highlighter-rouge">Reconcile</code> function is called every time a <code class="language-plaintext highlighter-rouge">Website</code> resource is created, updated, or deleted. Its goal is to reach a steady state. It’s like a thermostat: it checks the temperature (current state) and turns the heating on or off (takes action) to reach the desired temperature (desired state).</p>

<p>Let’s implement the logic.</p>

<ol>
  <li><strong>Get the <code class="language-plaintext highlighter-rouge">Website</code> resource</strong>: The request contains the name and namespace of the resource that triggered the reconciliation.</li>
  <li><strong>Check if Deployment exists</strong>: If not, create it.</li>
  <li><strong>If Deployment exists</strong>: Ensure its spec (e.g., number of replicas) matches the <code class="language-plaintext highlighter-rouge">Website</code> spec. If not, update it.</li>
  <li><strong>Check if Service exists</strong>: If not, create it.</li>
  <li><strong>Update Status</strong>: Report the current status back to the <code class="language-plaintext highlighter-rouge">Website</code> resource.</li>
</ol>

<h4 id="updating-the-controller-code"><strong>Updating the Controller Code</strong></h4>

<p>Replace the contents of <code class="language-plaintext highlighter-rouge">internal/controllers/website_controller.go</code> with the following code. Read the comments carefully to understand each step.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// internal/controllers/website_controller.go</span>

<span class="k">package</span> <span class="n">controllers</span>

<span class="k">import</span> <span class="p">(</span>
	<span class="s">"context"</span>
	<span class="s">"reflect"</span>

	<span class="n">appsv1</span> <span class="s">"k8s.ioio/api/apps/v1"</span>
	<span class="n">corev1</span> <span class="s">"k8s.io/api/core/v1"</span>
	<span class="s">"k8s.io/apimachinery/pkg/api/errors"</span>
	<span class="n">metav1</span> <span class="s">"k8s.io/apimachinery/pkg/apis/meta/v1"</span>
	<span class="s">"k8s.io/apimachinery/pkg/runtime"</span>
	<span class="s">"k8s.io/apimachinery/pkg/types"</span>
	<span class="n">ctrl</span> <span class="s">"sigs.k8s.io/controller-runtime"</span>
	<span class="s">"sigs.k8s.io/controller-runtime/pkg/client"</span>
	<span class="s">"sigs.k8s.io/controller-runtime/pkg/log"</span>

	<span class="n">websitev1alpha1</span> <span class="s">"github.com/your-user/website-operator/api/v1alpha1"</span>
<span class="p">)</span>

<span class="c">// WebsiteReconciler reconciles a Website object</span>
<span class="k">type</span> <span class="n">WebsiteReconciler</span> <span class="k">struct</span> <span class="p">{</span>
	<span class="n">client</span><span class="o">.</span><span class="n">Client</span>
	<span class="n">Scheme</span> <span class="o">*</span><span class="n">runtime</span><span class="o">.</span><span class="n">Scheme</span>
<span class="p">}</span>

<span class="c">//+kubebuilder:rbac:groups=website.my.domain,resources=websites,verbs=get;list;watch;create;update;patch;delete</span>
<span class="c">//+kubebuilder:rbac:groups=website.my.domain,resources=websites/status,verbs=get;update;patch</span>
<span class="c">//+kubebuilder:rbac:groups=website.my.domain,resources=websites/finalizers,verbs=update</span>
<span class="c">//+kubebuilder:rbac:groups=apps,resources=deployments,verbs=get;list;watch;create;update;patch;delete</span>
<span class="c">//+kubebuilder:rbac:groups=core,resources=services,verbs=get;list;watch;create;update;patch;delete</span>

<span class="k">func</span> <span class="p">(</span><span class="n">r</span> <span class="o">*</span><span class="n">WebsiteReconciler</span><span class="p">)</span> <span class="n">Reconcile</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">req</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">(</span><span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">logger</span> <span class="o">:=</span> <span class="n">log</span><span class="o">.</span><span class="n">FromContext</span><span class="p">(</span><span class="n">ctx</span><span class="p">)</span>

	<span class="c">// 1. Fetch the Website instance</span>
	<span class="n">website</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">websitev1alpha1</span><span class="o">.</span><span class="n">Website</span><span class="p">{}</span>
	<span class="n">err</span> <span class="o">:=</span> <span class="n">r</span><span class="o">.</span><span class="n">Get</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">req</span><span class="o">.</span><span class="n">NamespacedName</span><span class="p">,</span> <span class="n">website</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">if</span> <span class="n">errors</span><span class="o">.</span><span class="n">IsNotFound</span><span class="p">(</span><span class="n">err</span><span class="p">)</span> <span class="p">{</span>
			<span class="n">logger</span><span class="o">.</span><span class="n">Info</span><span class="p">(</span><span class="s">"Website resource not found. Ignoring since object must be deleted."</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="no">nil</span>
		<span class="p">}</span>
		<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to get Website"</span><span class="p">)</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="c">// 2. Reconcile the Deployment</span>
	<span class="n">deployment</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">appsv1</span><span class="o">.</span><span class="n">Deployment</span><span class="p">{}</span>
	<span class="n">err</span> <span class="o">=</span> <span class="n">r</span><span class="o">.</span><span class="n">Get</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">types</span><span class="o">.</span><span class="n">NamespacedName</span><span class="p">{</span><span class="n">Name</span><span class="o">:</span> <span class="n">website</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">Namespace</span><span class="o">:</span> <span class="n">website</span><span class="o">.</span><span class="n">Namespace</span><span class="p">},</span> <span class="n">deployment</span><span class="p">)</span>

	<span class="c">// If the deployment does not exist, create it. Otherwise, update it.</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="o">&amp;&amp;</span> <span class="n">errors</span><span class="o">.</span><span class="n">IsNotFound</span><span class="p">(</span><span class="n">err</span><span class="p">)</span> <span class="p">{</span>
		<span class="c">// Define a new deployment</span>
		<span class="n">dep</span> <span class="o">:=</span> <span class="n">r</span><span class="o">.</span><span class="n">deploymentForWebsite</span><span class="p">(</span><span class="n">website</span><span class="p">)</span>
		<span class="n">logger</span><span class="o">.</span><span class="n">Info</span><span class="p">(</span><span class="s">"Creating a new Deployment"</span><span class="p">,</span> <span class="s">"Deployment.Namespace"</span><span class="p">,</span> <span class="n">dep</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span> <span class="s">"Deployment.Name"</span><span class="p">,</span> <span class="n">dep</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
		<span class="n">err</span> <span class="o">=</span> <span class="n">r</span><span class="o">.</span><span class="n">Create</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">dep</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to create new Deployment"</span><span class="p">,</span> <span class="s">"Deployment.Namespace"</span><span class="p">,</span> <span class="n">dep</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span> <span class="s">"Deployment.Name"</span><span class="p">,</span> <span class="n">dep</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
		<span class="p">}</span>
		<span class="c">// Deployment created successfully - return and requeue</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{</span><span class="n">Requeue</span><span class="o">:</span> <span class="no">true</span><span class="p">},</span> <span class="no">nil</span>
	<span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to get Deployment"</span><span class="p">)</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="c">// Ensure the deployment size is the same as the spec</span>
	<span class="n">size</span> <span class="o">:=</span> <span class="n">website</span><span class="o">.</span><span class="n">Spec</span><span class="o">.</span><span class="n">Replicas</span>
	<span class="k">if</span> <span class="o">*</span><span class="n">deployment</span><span class="o">.</span><span class="n">Spec</span><span class="o">.</span><span class="n">Replicas</span> <span class="o">!=</span> <span class="o">*</span><span class="n">size</span> <span class="p">{</span>
		<span class="n">deployment</span><span class="o">.</span><span class="n">Spec</span><span class="o">.</span><span class="n">Replicas</span> <span class="o">=</span> <span class="n">size</span>
		<span class="n">err</span> <span class="o">=</span> <span class="n">r</span><span class="o">.</span><span class="n">Update</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">deployment</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to update Deployment"</span><span class="p">,</span> <span class="s">"Deployment.Namespace"</span><span class="p">,</span> <span class="n">deployment</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span> <span class="s">"Deployment.Name"</span><span class="p">,</span> <span class="n">deployment</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
		<span class="p">}</span>
		<span class="c">// Ask to requeue after 1 minute in order to give time for the</span>
		<span class="c">// pods be created</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{</span><span class="n">RequeueAfter</span><span class="o">:</span> <span class="n">time</span><span class="o">.</span><span class="n">Minute</span><span class="p">},</span> <span class="no">nil</span>
	<span class="p">}</span>

	<span class="c">// 3. Reconcile the Service</span>
	<span class="n">service</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">corev1</span><span class="o">.</span><span class="n">Service</span><span class="p">{}</span>
	<span class="n">err</span> <span class="o">=</span> <span class="n">r</span><span class="o">.</span><span class="n">Get</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">types</span><span class="o">.</span><span class="n">NamespacedName</span><span class="p">{</span><span class="n">Name</span><span class="o">:</span> <span class="n">website</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">Namespace</span><span class="o">:</span> <span class="n">website</span><span class="o">.</span><span class="n">Namespace</span><span class="p">},</span> <span class="n">service</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="o">&amp;&amp;</span> <span class="n">errors</span><span class="o">.</span><span class="n">IsNotFound</span><span class="p">(</span><span class="n">err</span><span class="p">)</span> <span class="p">{</span>
		<span class="c">// Define a new service</span>
		<span class="n">svc</span> <span class="o">:=</span> <span class="n">r</span><span class="o">.</span><span class="n">serviceForWebsite</span><span class="p">(</span><span class="n">website</span><span class="p">)</span>
		<span class="n">logger</span><span class="o">.</span><span class="n">Info</span><span class="p">(</span><span class="s">"Creating a new Service"</span><span class="p">,</span> <span class="s">"Service.Namespace"</span><span class="p">,</span> <span class="n">svc</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span> <span class="s">"Service.Name"</span><span class="p">,</span> <span class="n">svc</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
		<span class="n">err</span> <span class="o">=</span> <span class="n">r</span><span class="o">.</span><span class="n">Create</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">svc</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to create new Service"</span><span class="p">,</span> <span class="s">"Service.Namespace"</span><span class="p">,</span> <span class="n">svc</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span> <span class="s">"Service.Name"</span><span class="p">,</span> <span class="n">svc</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
		<span class="p">}</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{</span><span class="n">Requeue</span><span class="o">:</span> <span class="no">true</span><span class="p">},</span> <span class="no">nil</span>
	<span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to get Service"</span><span class="p">)</span>
		<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
	<span class="p">}</span>
	
	<span class="c">// 4. Update the Website status</span>
	<span class="c">// We'll just reflect the deployment status for simplicity</span>
	<span class="k">if</span> <span class="o">!</span><span class="n">reflect</span><span class="o">.</span><span class="n">DeepEqual</span><span class="p">(</span><span class="n">deployment</span><span class="o">.</span><span class="n">Status</span><span class="p">,</span> <span class="n">website</span><span class="o">.</span><span class="n">Status</span><span class="o">.</span><span class="n">DeploymentStatus</span><span class="p">)</span> <span class="p">{</span>
		<span class="n">website</span><span class="o">.</span><span class="n">Status</span><span class="o">.</span><span class="n">DeploymentStatus</span> <span class="o">=</span> <span class="n">deployment</span><span class="o">.</span><span class="n">Status</span>
		<span class="n">err</span> <span class="o">:=</span> <span class="n">r</span><span class="o">.</span><span class="n">Status</span><span class="p">()</span><span class="o">.</span><span class="n">Update</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">website</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">logger</span><span class="o">.</span><span class="n">Error</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="s">"Failed to update Website status"</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="n">err</span>
		<span class="p">}</span>
	<span class="p">}</span>

	<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Result</span><span class="p">{},</span> <span class="no">nil</span>
<span class="p">}</span>

<span class="c">// deploymentForWebsite returns a Website Deployment object</span>
<span class="k">func</span> <span class="p">(</span><span class="n">r</span> <span class="o">*</span><span class="n">WebsiteReconciler</span><span class="p">)</span> <span class="n">deploymentForWebsite</span><span class="p">(</span><span class="n">w</span> <span class="o">*</span><span class="n">websitev1alpha1</span><span class="o">.</span><span class="n">Website</span><span class="p">)</span> <span class="o">*</span><span class="n">appsv1</span><span class="o">.</span><span class="n">Deployment</span> <span class="p">{</span>
	<span class="n">labels</span> <span class="o">:=</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="kt">string</span><span class="p">{</span><span class="s">"app"</span><span class="o">:</span> <span class="n">w</span><span class="o">.</span><span class="n">Name</span><span class="p">}</span>
	<span class="n">replicas</span> <span class="o">:=</span> <span class="n">w</span><span class="o">.</span><span class="n">Spec</span><span class="o">.</span><span class="n">Replicas</span>

	<span class="c">// For a real-world controller, you would likely use an initContainer</span>
	<span class="c">// to clone the gitRepo into a volume. For this tutorial, we'll use</span>
	<span class="c">// a simple nginx image.</span>
	<span class="n">dep</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">appsv1</span><span class="o">.</span><span class="n">Deployment</span><span class="p">{</span>
		<span class="n">ObjectMeta</span><span class="o">:</span> <span class="n">metav1</span><span class="o">.</span><span class="n">ObjectMeta</span><span class="p">{</span>
			<span class="n">Name</span><span class="o">:</span>      <span class="n">w</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span>
			<span class="n">Namespace</span><span class="o">:</span> <span class="n">w</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span>
		<span class="p">},</span>
		<span class="n">Spec</span><span class="o">:</span> <span class="n">appsv1</span><span class="o">.</span><span class="n">DeploymentSpec</span><span class="p">{</span>
			<span class="n">Replicas</span><span class="o">:</span> <span class="n">replicas</span><span class="p">,</span>
			<span class="n">Selector</span><span class="o">:</span> <span class="o">&amp;</span><span class="n">metav1</span><span class="o">.</span><span class="n">LabelSelector</span><span class="p">{</span>
				<span class="n">MatchLabels</span><span class="o">:</span> <span class="n">labels</span><span class="p">,</span>
			<span class="p">},</span>
			<span class="n">Template</span><span class="o">:</span> <span class="n">corev1</span><span class="o">.</span><span class="n">PodTemplateSpec</span><span class="p">{</span>
				<span class="n">ObjectMeta</span><span class="o">:</span> <span class="n">metav1</span><span class="o">.</span><span class="n">ObjectMeta</span><span class="p">{</span>
					<span class="n">Labels</span><span class="o">:</span> <span class="n">labels</span><span class="p">,</span>
				<span class="p">},</span>
				<span class="n">Spec</span><span class="o">:</span> <span class="n">corev1</span><span class="o">.</span><span class="n">PodSpec</span><span class="p">{</span>
					<span class="n">Containers</span><span class="o">:</span> <span class="p">[]</span><span class="n">corev1</span><span class="o">.</span><span class="n">Container</span><span class="p">,</span>
					<span class="p">}},</span>
				<span class="p">},</span>
			<span class="p">},</span>
		<span class="p">},</span>
	<span class="p">}</span>

	<span class="c">// Set Website instance as the owner and controller</span>
	<span class="n">ctrl</span><span class="o">.</span><span class="n">SetControllerReference</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">dep</span><span class="p">,</span> <span class="n">r</span><span class="o">.</span><span class="n">Scheme</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">dep</span>
<span class="p">}</span>

<span class="c">// serviceForWebsite returns a Website Service object</span>
<span class="k">func</span> <span class="p">(</span><span class="n">r</span> <span class="o">*</span><span class="n">WebsiteReconciler</span><span class="p">)</span> <span class="n">serviceForWebsite</span><span class="p">(</span><span class="n">w</span> <span class="o">*</span><span class="n">websitev1alpha1</span><span class="o">.</span><span class="n">Website</span><span class="p">)</span> <span class="o">*</span><span class="n">corev1</span><span class="o">.</span><span class="n">Service</span> <span class="p">{</span>
	<span class="n">labels</span> <span class="o">:=</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="kt">string</span><span class="p">{</span><span class="s">"app"</span><span class="o">:</span> <span class="n">w</span><span class="o">.</span><span class="n">Name</span><span class="p">}</span>

	<span class="n">svc</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">corev1</span><span class="o">.</span><span class="n">Service</span><span class="p">{</span>
		<span class="n">ObjectMeta</span><span class="o">:</span> <span class="n">metav1</span><span class="o">.</span><span class="n">ObjectMeta</span><span class="p">{</span>
			<span class="n">Name</span><span class="o">:</span>      <span class="n">w</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span>
			<span class="n">Namespace</span><span class="o">:</span> <span class="n">w</span><span class="o">.</span><span class="n">Namespace</span><span class="p">,</span>
		<span class="p">},</span>
		<span class="n">Spec</span><span class="o">:</span> <span class="n">corev1</span><span class="o">.</span><span class="n">ServiceSpec</span><span class="p">{</span>
			<span class="n">Selector</span><span class="o">:</span> <span class="n">labels</span><span class="p">,</span>
			<span class="n">Ports</span><span class="o">:</span> <span class="p">[]</span><span class="n">corev1</span><span class="o">.</span><span class="n">ServicePort</span><span class="p">,</span>
			<span class="n">Type</span><span class="o">:</span> <span class="n">corev1</span><span class="o">.</span><span class="n">ServiceTypeLoadBalancer</span><span class="p">,</span>
		<span class="p">},</span>
	<span class="p">}</span>
	<span class="c">// Set Website instance as the owner and controller</span>
	<span class="n">ctrl</span><span class="o">.</span><span class="n">SetControllerReference</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">svc</span><span class="p">,</span> <span class="n">r</span><span class="o">.</span><span class="n">Scheme</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">svc</span>
<span class="p">}</span>

<span class="c">// SetupWithManager sets up the controller with the Manager.</span>
<span class="k">func</span> <span class="p">(</span><span class="n">r</span> <span class="o">*</span><span class="n">WebsiteReconciler</span><span class="p">)</span> <span class="n">SetupWithManager</span><span class="p">(</span><span class="n">mgr</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">Manager</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
	<span class="k">return</span> <span class="n">ctrl</span><span class="o">.</span><span class="n">NewControllerManagedBy</span><span class="p">(</span><span class="n">mgr</span><span class="p">)</span><span class="o">.</span>
		<span class="n">For</span><span class="p">(</span><span class="o">&amp;</span><span class="n">websitev1alpha1</span><span class="o">.</span><span class="n">Website</span><span class="p">{})</span><span class="o">.</span>
		<span class="n">Owns</span><span class="p">(</span><span class="o">&amp;</span><span class="n">appsv1</span><span class="o">.</span><span class="n">Deployment</span><span class="p">{})</span><span class="o">.</span>
		<span class="n">Owns</span><span class="p">(</span><span class="o">&amp;</span><span class="n">corev1</span><span class="o">.</span><span class="n">Service</span><span class="p">{})</span><span class="o">.</span>
		<span class="n">Complete</span><span class="p">(</span><span class="n">r</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Key changes and explanations:</strong></p>

<ul>
  <li><strong>RBAC Permissions (<code class="language-plaintext highlighter-rouge">//+kubebuilder:rbac</code>):</strong> We added permissions for the controller to manage <code class="language-plaintext highlighter-rouge">Deployments</code> and <code class="language-plaintext highlighter-rouge">Services</code>. After adding these, run <code class="language-plaintext highlighter-rouge">make manifests</code> to update the RBAC roles in <code class="language-plaintext highlighter-rouge">config/rbac/role.yaml</code>.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">deploymentForWebsite</code> &amp; <code class="language-plaintext highlighter-rouge">serviceForWebsite</code>:</strong> These helper functions create the desired Deployment and Service objects.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ctrl.SetControllerReference</code></strong>: <strong>This is crucial.</strong> It sets the <code class="language-plaintext highlighter-rouge">Website</code> resource as the “owner” of the Deployment and Service. This means when you delete the <code class="language-plaintext highlighter-rouge">Website</code> resource, Kubernetes garbage collection will automatically delete the owned Deployment and Service.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">SetupWithManager</code></strong>: We configured the controller to <code class="language-plaintext highlighter-rouge">Own</code> Deployments and Services. This tells the controller-runtime framework to trigger a reconciliation for a <code class="language-plaintext highlighter-rouge">Website</code> resource whenever its owned <code class="language-plaintext highlighter-rouge">Deployment</code> or <code class="language-plaintext highlighter-rouge">Service</code> is changed or deleted.</li>
</ul>

<h3 id="step-4-run-and-test-the-controller"><strong>Step 4: Run and Test the Controller</strong></h3>

<p>Now for the exciting part! Let’s run our controller and see it in action.</p>

<h4 id="1-install-the-crd-into-the-cluster"><strong>1. Install the CRD into the Cluster</strong></h4>

<p>The cluster needs to know about the <code class="language-plaintext highlighter-rouge">Website</code> resource type.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nb">install</span>
</code></pre></div></div>

<p>Verify that the CRD is installed:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get crd websites.website.my.domain
<span class="c"># NAME                          CREATED AT</span>
<span class="c"># websites.website.my.domain    2023-10-27T10:30:00Z</span>
</code></pre></div></div>

<h4 id="2-run-the-controller-locally"><strong>2. Run the Controller Locally</strong></h4>

<p>Running the controller on your local machine is fantastic for development and debugging. It will use your local kubeconfig to talk to the cluster.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make run
</code></pre></div></div>
<p>You will see logs from the controller manager starting up. It is now waiting for <code class="language-plaintext highlighter-rouge">Website</code> resources to be created.</p>

<h4 id="3-create-a-website-resource"><strong>3. Create a <code class="language-plaintext highlighter-rouge">Website</code> Resource</strong></h4>

<p>In a <strong>new terminal</strong>, apply the sample <code class="language-plaintext highlighter-rouge">Website</code> resource. Kubebuilder has already created a sample for you in <code class="language-plaintext highlighter-rouge">config/samples/website_v1alpha1_website.yaml</code>. Let’s modify it to match our <code class="language-plaintext highlighter-rouge">Spec</code>.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># config/samples/website_v1alpha1_website.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">website.my.domain/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Website</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app.kubernetes.io/name</span><span class="pi">:</span> <span class="s">website</span>
    <span class="na">app.kubernetes.io/instance</span><span class="pi">:</span> <span class="s">website-sample</span>
    <span class="na">app.kubernetes.io/part-of</span><span class="pi">:</span> <span class="s">website-operator</span>
    <span class="na">app.kubernetes.io/managed-by</span><span class="pi">:</span> <span class="s">kustomize</span>
    <span class="na">app.kubernetes.io/created-by</span><span class="pi">:</span> <span class="s">website-operator</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">website-sample</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">gitRepo</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://github.com/my-user/my-repo.git"</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span>
</code></pre></div></div>

<p>Apply it:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> config/samples/website_v1alpha1_website.yaml
</code></pre></div></div>

<h4 id="4-observe-the-results"><strong>4. Observe the Results</strong></h4>

<p>Look at the logs from your <code class="language-plaintext highlighter-rouge">make run</code> terminal. You should see the controller creating the Deployment and Service.</p>

<p>Now, check the resources in your cluster:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check our custom resource</span>
kubectl get website

<span class="c"># NAME             AGE</span>
<span class="c"># website-sample   15s</span>

<span class="c"># Check the Deployment created by our controller</span>
kubectl get deployment website-sample

<span class="c"># NAME             READY   UP-TO-DATE   AVAILABLE   AGE</span>
<span class="c"># website-sample   2/2     2            2           12s</span>

<span class="c"># Check the Service created by our controller</span>
kubectl get service website-sample

<span class="c"># NAME             TYPE           CLUSTER-IP     EXTERNAL-IP   PORT(S)        AGE</span>
<span class="c"># website-sample   LoadBalancer   10.10.10.10    &lt;pending&gt;     80:31234/TCP   10s</span>
</code></pre></div></div>
<p>Success! Our controller saw the <code class="language-plaintext highlighter-rouge">Website</code> resource and created the corresponding Deployment and Service.</p>

<p>Now, try changing the number of replicas in the YAML file to <code class="language-plaintext highlighter-rouge">3</code> and <code class="language-plaintext highlighter-rouge">kubectl apply</code> it again. You will see the controller update the Deployment to match.</p>

<h4 id="5-cleanup"><strong>5. Cleanup</strong></h4>
<p>Delete the <code class="language-plaintext highlighter-rouge">Website</code> resource. Because we set the Owner Reference, the Deployment and Service will be automatically deleted too.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> config/samples/website_v1alpha1_website.yaml

<span class="c"># Verify they are gone</span>
kubectl get deployment,service
</code></pre></div></div>

<hr />

<h3 id="step-5-build-and-deploy-to-the-cluster"><strong>Step 5: Build and Deploy to the Cluster</strong></h3>

<p>Running locally is great for development, but for production, you need to run the controller as a <code class="language-plaintext highlighter-rouge">Deployment</code> inside the cluster.</p>

<ol>
  <li><strong>Build and Push the Docker Image</strong>:
(Replace <code class="language-plaintext highlighter-rouge">your-dockerhub-username</code> with your actual username)
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make docker-build <span class="nv">IMG</span><span class="o">=</span>your-dockerhub-username/website-operator:v0.0.1
make docker-push <span class="nv">IMG</span><span class="o">=</span>your-dockerhub-username/website-operator:v0.0.1
</code></pre></div>    </div>
  </li>
  <li><strong>Deploy the Controller to the Cluster</strong>:
This command will use the manifests in the <code class="language-plaintext highlighter-rouge">config</code> directory to create the <code class="language-plaintext highlighter-rouge">Deployment</code>, <code class="language-plaintext highlighter-rouge">RBAC</code> roles, and everything else needed to run the controller.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make deploy <span class="nv">IMG</span><span class="o">=</span>your-dockerhub-username/website-operator:v0.0.1
</code></pre></div>    </div>
    <p>Your controller is now running inside the cluster, just like any other application. It will continue to watch for <code class="language-plaintext highlighter-rouge">Website</code> resources and reconcile them.</p>
  </li>
</ol>

<h3 id="conclusion-and-next-steps"><strong>Conclusion and Next Steps</strong></h3>

<p>Congratulations! You have successfully built a fully functional Kubernetes controller. You learned how to:</p>
<ul>
  <li>Scaffold a project with Kubebuilder.</li>
  <li>Define a Custom Resource Definition (<code class="language-plaintext highlighter-rouge">CRD</code>).</li>
  <li>Implement the reconciliation logic in a controller.</li>
  <li>Set owner references for garbage collection.</li>
  <li>Run, test, and deploy your controller.</li>
</ul>

<p>From here, you can explore more advanced topics:</p>
<ul>
  <li><strong>Finalizers</strong>: To perform cleanup actions before a resource is deleted.</li>
  <li><strong>Webhooks</strong>: To validate or mutate your custom resources on creation/update.</li>
  <li><strong>More Complex Status Updates</strong>: Provide richer status information about the resources your controller manages.</li>
  <li><strong>Testing</strong>: Write unit and integration tests for your controller logic.</li>
</ul>]]></content><author><name></name></author><category term="tutorial" /><category term="kubernetes" /><category term="controller" /><category term="Kubebuilder" /><category term="crd" /><category term="opensource" /><summary type="html"><![CDATA[In this tutorial, you will learn how to build a custom Kubernetes controller (also known as an Operator) using Kubebuilder. We will create a Custom Resource Definition (CRD) for a simple Website resource.]]></summary></entry><entry><title type="html">Guide to Building a Custom Kubernetes Scheduler</title><link href="https://menraromial.com/blog/2025/guide-building-custom-kubernetes-scheduler/" rel="alternate" type="text/html" title="Guide to Building a Custom Kubernetes Scheduler" /><published>2025-08-24T17:30:00+00:00</published><updated>2025-08-24T17:30:00+00:00</updated><id>https://menraromial.com/blog/2025/guide-building-custom-kubernetes-scheduler</id><content type="html" xml:base="https://menraromial.com/blog/2025/guide-building-custom-kubernetes-scheduler/"><![CDATA[<p>This guide will walk you through creating a custom scheduler plugin that influences where pods are placed based on custom logic. We will build a simple “Network-Aware” plugin that prefers to schedule pods on nodes with a specific network label.</p>

<p><strong>What We Will Build:</strong></p>
<ul>
  <li><strong>A <code class="language-plaintext highlighter-rouge">Filter</code> plugin:</strong> It will only allow pods that request <code class="language-plaintext highlighter-rouge">high-speed</code> networking to be scheduled on nodes that are labeled as <code class="language-plaintext highlighter-rouge">network-topology=high-speed</code>.</li>
  <li><strong>A <code class="language-plaintext highlighter-rouge">Score</code> plugin:</strong> It will give a higher score to nodes with the <code class="language-plaintext highlighter-rouge">high-speed</code> label, making them more desirable.</li>
  <li>We will then package this plugin into a new scheduler binary, containerize it, and deploy it to a local Kubernetes cluster.</li>
</ul>

<hr />

<h3 id="part-1-understanding-the-foundations---the-scheduling-framework"><strong>Part 1: Understanding the Foundations - The Scheduling Framework</strong></h3>

<p>Before writing any code, it’s crucial to understand the “why” and “how”.</p>

<h4 id="what-is-the-kubernetes-scheduler">What is the Kubernetes Scheduler?</h4>

<p>The default Kubernetes scheduler (<code class="language-plaintext highlighter-rouge">kube-scheduler</code>) is a control plane component that watches for newly created Pods that have no <code class="language-plaintext highlighter-rouge">nodeName</code> assigned. For every Pod it discovers, the scheduler becomes responsible for finding the best Node for that Pod to run on. This process involves two main phases:</p>

<ol>
  <li><strong>Filtering (Predicates):</strong> The scheduler finds the set of feasible Nodes where the Pod <em>can</em> run. For example, if a Pod requests 2 CPUs, all nodes with less than 2 available CPUs are filtered out.</li>
  <li><strong>Scoring (Priorities):</strong> From the list of feasible nodes, the scheduler ranks each node by assigning it a score. It then picks the node with the highest score. For example, it might prefer a node that has fewer running pods.</li>
</ol>

<h4 id="the-scheduling-framework-your-entry-point">The Scheduling Framework: Your Entry Point</h4>

<p>The Scheduling Framework is a pluggable architecture for the <code class="language-plaintext highlighter-rouge">kube-scheduler</code> that makes it easy to add custom logic without forking the entire Kubernetes codebase. It exposes a set of <strong>Extension Points</strong> in the scheduling lifecycle where your custom code (a <strong>Plugin</strong>) can be executed.</p>

<p><strong>Visualizing the Extension Points:</strong></p>

<p>Here is the flow of the scheduling process and where the main extension points fit in.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                  (A Pod is created without a node)
                                  |
                                  v
+-------------------------------------------------------------------------+
|                        SCHEDULING CYCLE (for one Pod)                     |
+-------------------------------------------------------------------------+
                                  |
                                  v
[ QueueSort ] &lt;--- Your plugin can sort pods in the scheduling queue.
                                  |
                                  v
+-------------------------------------------------------------------------+
|                      SCHEDULING ATTEMPT (for one Pod)                     |
+-------------------------------------------------------------------------+
|                                 |                                       |
|                                 v                                       |
| [ PreFilter ] &lt;--- Pre-process info about the Pod before filtering.     |
|                                 |                                       |
|          --------------------- FOR EACH NODE ----------------------     |
|          |                                                        |     |
|          |    [ Filter ] &lt;--- Can this Pod run on this Node? (Yes/No) |   |
|          |          (If No, Node is discarded for this Pod)        |     |
|          |                                                        |     |
|          ----------------------------------------------------------     |
|                                 |                                       |
|                                 v                                       |
| [ PostFilter ] &lt;- If no nodes are viable, your plugin can take action.  |
|                                 |                                       |
|                                 v                                       |
| [ PreScore ] &lt;--- Pre-process info before scoring viable nodes.         |
|                                 |                                       |
|          --------------------- FOR EACH VIABLE NODE ---------------     |
|          |                                                        |     |
|          |    [ Score ] &lt;--- Give this Node a score (e.g., 0-100).  |   |
|          |                                                        |     |
|          ----------------------------------------------------------     |
|                                 |                                       |
|                                 v                                       |
| [ NormalizeScore ] &lt;- Modify all scores to fit a common scale.          |
|                                 |                                       |
|                                 v                                       |
|                          (Scheduler picks Node with highest score)        |
|                                 |                                       |
|                                 v                                       |
+-------------------------------------------------------------------------+
|                           BINDING CYCLE                                 |
+-------------------------------------------------------------------------+
|                                 |                                       |
|                                 v                                       |
| [ Reserve ] &lt;--- Mark resources as "reserved" on the chosen node.       |
|                                 |                                       |
|                                 v                                       |
| [ Permit ] &lt;--- Approve or deny the binding. Can delay binding.         |
|                                 |                                       |
|                                 v                                       |
| [ PreBind ] &lt;--- Actions to take right before the Pod is bound.         |
|                                 |                                       |
|                                 v                                       |
| [ Bind ] &lt;--- The actual binding of the Pod to the Node.                |
|                                 |                                       |
|                                 v                                       |
| [ PostBind ] &lt;--- Actions to take after the binding is successful.      |
|                                 |                                       |
+-------------------------------------------------------------------------+
</code></pre></div></div>

<p>Our plugin will focus on the <strong><code class="language-plaintext highlighter-rouge">Filter</code></strong> and <strong><code class="language-plaintext highlighter-rouge">Score</code></strong> extension points.</p>

<hr />

<h3 id="part-2-setting-up-the-professional-development-environment"><strong>Part 2: Setting Up the Professional Development Environment</strong></h3>

<p>This is where we align with the project’s standards.</p>

<h4 id="prerequisites">Prerequisites:</h4>

<ol>
  <li><strong>Go:</strong> Version 1.19+ (the <code class="language-plaintext highlighter-rouge">scheduler-plugins</code> project specifies its version in the <code class="language-plaintext highlighter-rouge">go.mod</code> file).</li>
  <li><strong>Docker:</strong> To build container images. Make sure the Docker daemon is running.</li>
  <li><strong>kubectl:</strong> The Kubernetes command-line tool.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">kind</code></strong>: Our local Kubernetes cluster.</li>
</ol>

<h4 id="step-1-clone-the-scheduler-plugins-repository">Step 1: Clone the <code class="language-plaintext highlighter-rouge">scheduler-plugins</code> Repository</h4>

<p>We will use the official repository not just as a template, but as our direct development environment. This is the standard practice.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/kubernetes-sigs/scheduler-plugins.git
<span class="nb">cd </span>scheduler-plugins
</code></pre></div></div>

<h4 id="step-2-explore-the-makefile">Step 2: Explore the <code class="language-plaintext highlighter-rouge">Makefile</code></h4>

<p>The <code class="language-plaintext highlighter-rouge">Makefile</code> in this repository is the heart of the development workflow. Instead of running raw <code class="language-plaintext highlighter-rouge">go</code> or <code class="language-plaintext highlighter-rouge">docker</code> commands, we will use <code class="language-plaintext highlighter-rouge">make</code> targets. This ensures consistency and correctness.</p>

<p>Key targets we will use:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">make verify</code>: Runs formatters and linters to ensure code quality. <strong>Always run this before committing.</strong></li>
  <li><code class="language-plaintext highlighter-rouge">make unit-test</code>: Runs all unit tests in the repository.</li>
  <li><code class="language-plaintext highlighter-rouge">make local-image</code>: <strong>This is crucial.</strong> It builds a scheduler container image for your local architecture, gives it a standard local name (<code class="language-plaintext highlighter-rouge">localhost:5000/...</code>), and loads it directly into your local Docker daemon. Perfect for testing with <code class="language-plaintext highlighter-rouge">kind</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">make build</code>: Compiles the Go binaries (<code class="language-plaintext highlighter-rouge">kube-scheduler</code> and <code class="language-plaintext highlighter-rouge">controller</code>) into the <code class="language-plaintext highlighter-rouge">/bin</code> directory.</li>
</ul>

<hr />

<h3 id="part-3-developing-our-custom-plugin"><strong>Part 3: Developing Our Custom Plugin</strong></h3>

<p>The core logic remains the same, but the process around it will be more rigorous.</p>

<h4 id="step-1-create-the-plugin-directory-and-file">Step 1: Create the Plugin Directory and File</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> pkg/networkspeed
<span class="nb">touch </span>pkg/networkspeed/networkspeed.go
</code></pre></div></div>

<h4 id="step-2-write-the-plugin-code">Step 2: Write the Plugin Code</h4>

<p>Place the same Go code from the previous guide into <code class="language-plaintext highlighter-rouge">pkg/networkspeed/networkspeed.go</code>. This code implements the <code class="language-plaintext highlighter-rouge">Filter</code> and <code class="language-plaintext highlighter-rouge">Score</code> extension points.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// pkg/networkspeed/networkspeed.go</span>

<span class="k">package</span> <span class="n">networkspeed</span>

<span class="k">import</span> <span class="p">(</span>
	<span class="s">"context"</span>
	<span class="s">"fmt"</span>

	<span class="n">v1</span> <span class="s">"k8s.io/api/core/v1"</span>
	<span class="s">"k8s.io/apimachinery/pkg/runtime"</span>
	<span class="s">"k8s.io/klog/v2"</span>
	<span class="s">"k8s.io/kubernetes/pkg/scheduler/framework"</span>
<span class="p">)</span>

<span class="c">// Define plugin name</span>
<span class="k">const</span> <span class="n">Name</span> <span class="o">=</span> <span class="s">"Networkspeed"</span>

<span class="k">const</span> <span class="p">(</span>
	<span class="c">// Annotation key on a Pod to specify network preference.</span>
	<span class="n">PodNetworkAnnotation</span> <span class="o">=</span> <span class="s">"my-network-preference"</span>
	<span class="c">// Label key on a Node to specify the type of network it has.</span>
	<span class="n">NodeNetworkLabel</span> <span class="o">=</span> <span class="s">"network-topology"</span>
	<span class="c">// Value for high-speed network preference.</span>
	<span class="n">HighSpeedNetwork</span> <span class="o">=</span> <span class="s">"high-speed"</span>
<span class="p">)</span>

<span class="c">// Networkspeed is a plugin that filters and scores nodes based on network labels.</span>
<span class="k">type</span> <span class="n">Networkspeed</span> <span class="k">struct</span> <span class="p">{</span>
	<span class="n">handle</span> <span class="n">framework</span><span class="o">.</span><span class="n">Handle</span>
<span class="p">}</span>

<span class="c">// Ensure our plugin implements the necessary interfaces.</span>
<span class="k">var</span> <span class="n">_</span> <span class="n">framework</span><span class="o">.</span><span class="n">FilterPlugin</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">Networkspeed</span><span class="p">{}</span>
<span class="k">var</span> <span class="n">_</span> <span class="n">framework</span><span class="o">.</span><span class="n">ScorePlugin</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">Networkspeed</span><span class="p">{}</span>

<span class="c">// Name returns the name of the plugin.</span>
<span class="k">func</span> <span class="p">(</span><span class="n">na</span> <span class="o">*</span><span class="n">Networkspeed</span><span class="p">)</span> <span class="n">Name</span><span class="p">()</span> <span class="kt">string</span> <span class="p">{</span>
	<span class="k">return</span> <span class="n">Name</span>
<span class="p">}</span>

<span class="c">// New is the factory function that creates a new instance of the plugin.</span>
<span class="c">// This is called by the scheduler framework when it initializes.</span>
<span class="k">func</span>  <span class="n">New</span><span class="p">(</span><span class="n">_</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">obj</span> <span class="n">runtime</span><span class="o">.</span><span class="n">Object</span><span class="p">,</span> <span class="n">handle</span> <span class="n">framework</span><span class="o">.</span><span class="n">Handle</span><span class="p">)</span> <span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Plugin</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Creating new Networkspeed plugin"</span><span class="p">)</span>
	<span class="k">return</span> <span class="o">&amp;</span><span class="n">Networkspeed</span><span class="p">{</span>
		<span class="n">handle</span><span class="o">:</span> <span class="n">handle</span><span class="p">,</span>
	<span class="p">},</span> <span class="no">nil</span>
<span class="p">}</span>

<span class="c">// ----------------- FILTERING LOGIC -----------------</span>

<span class="c">// Filter is called by the framework for each node to see if the pod can be scheduled on it.</span>
<span class="k">func</span> <span class="p">(</span><span class="n">na</span> <span class="o">*</span><span class="n">Networkspeed</span><span class="p">)</span> <span class="n">Filter</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">state</span> <span class="o">*</span><span class="n">framework</span><span class="o">.</span><span class="n">CycleState</span><span class="p">,</span> <span class="n">pod</span> <span class="o">*</span><span class="n">v1</span><span class="o">.</span><span class="n">Pod</span><span class="p">,</span> <span class="n">nodeInfo</span> <span class="o">*</span><span class="n">framework</span><span class="o">.</span><span class="n">NodeInfo</span><span class="p">)</span> <span class="o">*</span><span class="n">framework</span><span class="o">.</span><span class="n">Status</span> <span class="p">{</span>
	<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Filtering pod %v for node %v"</span><span class="p">,</span> <span class="n">pod</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>

	<span class="c">// 1. Get the pod's network preference from its annotations.</span>
	<span class="n">podNetworkPreference</span><span class="p">,</span> <span class="n">ok</span> <span class="o">:=</span> <span class="n">pod</span><span class="o">.</span><span class="n">Annotations</span><span class="p">[</span><span class="n">PodNetworkAnnotation</span><span class="p">]</span>
	<span class="k">if</span> <span class="o">!</span><span class="n">ok</span> <span class="p">{</span>
		<span class="c">// If the pod doesn't have the annotation, it has no special network needs.</span>
		<span class="c">// So, it can run on any node. We allow it.</span>
		<span class="k">return</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Success</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
	<span class="p">}</span>

	<span class="c">// 2. If the pod requests a high-speed network...</span>
	<span class="k">if</span> <span class="n">podNetworkPreference</span> <span class="o">==</span> <span class="n">HighSpeedNetwork</span> <span class="p">{</span>
		<span class="c">// ...check if the node has the required label.</span>
		<span class="n">nodeLabels</span> <span class="o">:=</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">GetLabels</span><span class="p">()</span>
		<span class="n">nodeNetworkType</span><span class="p">,</span> <span class="n">labelExists</span> <span class="o">:=</span> <span class="n">nodeLabels</span><span class="p">[</span><span class="n">NodeNetworkLabel</span><span class="p">]</span>

		<span class="k">if</span> <span class="n">labelExists</span> <span class="o">&amp;&amp;</span> <span class="n">nodeNetworkType</span> <span class="o">==</span> <span class="n">HighSpeedNetwork</span> <span class="p">{</span>
			<span class="c">// The node has the required label. The pod can run here.</span>
			<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Node %v has the required '%s' label. Allowing pod %v."</span><span class="p">,</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">HighSpeedNetwork</span><span class="p">,</span> <span class="n">pod</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Success</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
		<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
			<span class="c">// The node does not have the label. The pod cannot run here.</span>
			<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Node %v does not have the required '%s' label. Filtering out."</span><span class="p">,</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">HighSpeedNetwork</span><span class="p">)</span>
			<span class="k">return</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Unschedulable</span><span class="p">,</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Sprintf</span><span class="p">(</span><span class="s">"Node %s lacks required network label"</span><span class="p">,</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">Name</span><span class="p">))</span>
		<span class="p">}</span>
	<span class="p">}</span>

	<span class="c">// For any other annotation value, we don't apply any special filtering.</span>
	<span class="k">return</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Success</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
<span class="p">}</span>


<span class="c">// ----------------- SCORING LOGIC -----------------</span>

<span class="c">// Score is called for each node that passed the Filter phase.</span>
<span class="c">// It returns a score for the node, with higher scores being better.</span>
<span class="k">func</span> <span class="p">(</span><span class="n">na</span> <span class="o">*</span><span class="n">Networkspeed</span><span class="p">)</span> <span class="n">Score</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">state</span> <span class="o">*</span><span class="n">framework</span><span class="o">.</span><span class="n">CycleState</span><span class="p">,</span> <span class="n">pod</span> <span class="o">*</span><span class="n">v1</span><span class="o">.</span><span class="n">Pod</span><span class="p">,</span> <span class="n">nodeName</span> <span class="kt">string</span><span class="p">)</span> <span class="p">(</span><span class="kt">int64</span><span class="p">,</span> <span class="o">*</span><span class="n">framework</span><span class="o">.</span><span class="n">Status</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Scoring node %v for pod %v"</span><span class="p">,</span> <span class="n">nodeName</span><span class="p">,</span> <span class="n">pod</span><span class="o">.</span><span class="n">Name</span><span class="p">)</span>

	<span class="n">nodeInfo</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">na</span><span class="o">.</span><span class="n">handle</span><span class="o">.</span><span class="n">SnapshotSharedLister</span><span class="p">()</span><span class="o">.</span><span class="n">NodeInfos</span><span class="p">()</span><span class="o">.</span><span class="n">Get</span><span class="p">(</span><span class="n">nodeName</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="m">0</span><span class="p">,</span> <span class="n">framework</span><span class="o">.</span><span class="n">AsStatus</span><span class="p">(</span><span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"getting node %s from snapshot: %w"</span><span class="p">,</span> <span class="n">nodeName</span><span class="p">,</span> <span class="n">err</span><span class="p">))</span>
	<span class="p">}</span>

	<span class="n">nodeLabels</span> <span class="o">:=</span> <span class="n">nodeInfo</span><span class="o">.</span><span class="n">Node</span><span class="p">()</span><span class="o">.</span><span class="n">GetLabels</span><span class="p">()</span>
	<span class="n">podNetworkPreference</span><span class="p">,</span> <span class="n">ok</span> <span class="o">:=</span> <span class="n">pod</span><span class="o">.</span><span class="n">Annotations</span><span class="p">[</span><span class="n">PodNetworkAnnotation</span><span class="p">]</span>

	<span class="c">// If the pod wants a high-speed network and the node has it, give it the highest score.</span>
	<span class="k">if</span> <span class="n">ok</span> <span class="o">&amp;&amp;</span> <span class="n">podNetworkPreference</span> <span class="o">==</span> <span class="n">HighSpeedNetwork</span> <span class="p">{</span>
		<span class="k">if</span> <span class="n">nodeNetworkType</span><span class="p">,</span> <span class="n">labelExists</span> <span class="o">:=</span> <span class="n">nodeLabels</span><span class="p">[</span><span class="n">NodeNetworkLabel</span><span class="p">];</span> <span class="n">labelExists</span> <span class="o">&amp;&amp;</span> <span class="n">nodeNetworkType</span> <span class="o">==</span> <span class="n">HighSpeedNetwork</span> <span class="p">{</span>
			<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Node %v matches high-speed preference. Giving max score (100)."</span><span class="p">,</span> <span class="n">nodeName</span><span class="p">)</span>
			<span class="k">return</span> <span class="m">100</span><span class="p">,</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Success</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>

	<span class="c">// For all other cases, give a minimal score. The scheduler will still use other plugins</span>
	<span class="c">// (like least-allocated) to make a final decision.</span>
	<span class="n">klog</span><span class="o">.</span><span class="n">V</span><span class="p">(</span><span class="m">3</span><span class="p">)</span><span class="o">.</span><span class="n">Infof</span><span class="p">(</span><span class="s">"Node %v does not match high-speed preference. Giving min score (10)."</span><span class="p">,</span> <span class="n">nodeName</span><span class="p">)</span>
	<span class="k">return</span> <span class="m">10</span><span class="p">,</span> <span class="n">framework</span><span class="o">.</span><span class="n">NewStatus</span><span class="p">(</span><span class="n">framework</span><span class="o">.</span><span class="n">Success</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
<span class="p">}</span>

<span class="c">// ScoreExtensions returns a ScoreExtensions interface if the plugin implements one.</span>
<span class="k">func</span> <span class="p">(</span><span class="n">na</span> <span class="o">*</span><span class="n">Networkspeed</span><span class="p">)</span> <span class="n">ScoreExtensions</span><span class="p">()</span> <span class="n">framework</span><span class="o">.</span><span class="n">ScoreExtensions</span> <span class="p">{</span>
	<span class="c">// We don't need to normalize scores, so we return nil.</span>
	<span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h4 id="step-3-register-the-plugin">Step 3: Register the Plugin</h4>

<p>This step is also the same. We must tell the main binary about our new plugin.</p>

<p>Edit <code class="language-plaintext highlighter-rouge">cmd/scheduler/main.go</code> and add our plugin to the registry.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// cmd/scheduler/main.go</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="c">// ... other imports</span>
	<span class="s">"k8s.io/component-base/cli"</span>
	<span class="s">"k8s.io/kubernetes/cmd/kube-scheduler/app"</span>

    <span class="c">// IMPORT YOUR PLUGIN'S PACKAGE</span>
	<span class="s">"sigs.k8s.io/scheduler-plugins/pkg/networkspeed"</span>
    
	<span class="c">// ... other plugin imports</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
	<span class="c">// Register all plugins.</span>
	<span class="n">command</span> <span class="o">:=</span> <span class="n">app</span><span class="o">.</span><span class="n">NewSchedulerCommand</span><span class="p">(</span>
		<span class="n">app</span><span class="o">.</span><span class="n">WithPlugin</span><span class="p">(</span><span class="n">networkspeed</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">networkspeed</span><span class="o">.</span><span class="n">New</span><span class="p">),</span> <span class="c">// &lt;-- ADD THIS LINE</span>
		<span class="n">app</span><span class="o">.</span><span class="n">WithPlugin</span><span class="p">(</span><span class="n">coscheduling</span><span class="o">.</span><span class="n">Name</span><span class="p">,</span> <span class="n">coscheduling</span><span class="o">.</span><span class="n">New</span><span class="p">),</span>
		<span class="c">// ... other plugins</span>
	<span class="p">)</span>
    <span class="c">// ...</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h3 id="part-4-verifying-building-and-packaging"><strong>Part 4: Verifying, Building, and Packaging</strong></h3>

<p>This section is completely revamped to use the official <code class="language-plaintext highlighter-rouge">Makefile</code>.</p>

<h4 id="step-1-verify-your-code-quality">Step 1: Verify Your Code Quality</h4>

<p>Before you even think about building, run the verification scripts. This catches formatting errors, dependency issues, and other common problems.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make verify
</code></pre></div></div>
<p>If this command fails, fix the issues it reports (e.g., run <code class="language-plaintext highlighter-rouge">go fmt ./...</code>) and try again. This is a standard prerequisite for contributing to open-source projects.</p>

<h4 id="step-2-build-the-local-container-image">Step 2: Build the Local Container Image</h4>

<p>This is the most significant improvement. Forget manually setting registry and image names. The <code class="language-plaintext highlighter-rouge">local-image</code> target is designed specifically for this workflow.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make local-image
</code></pre></div></div>

<p><strong>What does this command do?</strong></p>
<ol>
  <li>It compiles your Go code, including the new <code class="language-plaintext highlighter-rouge">networkspeed</code> plugin, into a <code class="language-plaintext highlighter-rouge">kube-scheduler</code> binary.</li>
  <li>It builds a container image using Docker.</li>
  <li>It automatically sets the platform to your local machine’s architecture (e.g., <code class="language-plaintext highlighter-rouge">linux/amd64</code> or <code class="language-plaintext highlighter-rouge">linux/arm64</code>).</li>
  <li>It tags the image with a convenient name for local development: <code class="language-plaintext highlighter-rouge">localhost:5000/scheduler-plugins/kube-scheduler:v0.0.0</code>.</li>
  <li>It uses the <code class="language-plaintext highlighter-rouge">--load</code> flag with <code class="language-plaintext highlighter-rouge">docker buildx</code> to ensure the final image is available in your local Docker daemon, ready for <code class="language-plaintext highlighter-rouge">kind</code> to use.</li>
</ol>

<p>After it finishes, you can confirm the image exists:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker images | <span class="nb">grep </span>scheduler-plugins
</code></pre></div></div>
<p>You should see <code class="language-plaintext highlighter-rouge">localhost:5000/scheduler-plugins/kube-scheduler</code>.</p>

<hr />

<h3 id="part-5--production-grade-deployment-on-vms"><strong>Part 5 : Production-Grade Deployment on VMs</strong></h3>

<p>We will provision two Virtual Machines (one control-plane, one worker) and bootstrap a Kubernetes cluster using the <code class="language-plaintext highlighter-rouge">kubeadm</code> tool.</p>

<h4 id="prerequisites-for-this-section">Prerequisites for this Section:</h4>

<ul>
  <li><strong>VirtualBox and Vagrant:</strong> For easily provisioning and managing local VMs.</li>
  <li><strong>Sufficient System Resources:</strong> At least 4 CPUs and 8GB of RAM available for the VMs.</li>
</ul>

<h4 id="step-1-provision-the-virtual-machines">Step 1: Provision the Virtual Machines</h4>

<p>We’ll use a <code class="language-plaintext highlighter-rouge">Vagrantfile</code> to define our two-node cluster. Create a file named <code class="language-plaintext highlighter-rouge">Vagrantfile</code> with the following content:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Vagrantfile</span>
<span class="no">Vagrant</span><span class="p">.</span><span class="nf">configure</span><span class="p">(</span><span class="s2">"2"</span><span class="p">)</span> <span class="k">do</span> <span class="o">|</span><span class="n">config</span><span class="o">|</span>
  <span class="n">config</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">box</span> <span class="o">=</span> <span class="s2">"ubuntu/jammy64"</span>
  <span class="n">config</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">provider</span> <span class="s2">"virtualbox"</span> <span class="k">do</span> <span class="o">|</span><span class="n">v</span><span class="o">|</span>
    <span class="n">v</span><span class="p">.</span><span class="nf">memory</span> <span class="o">=</span> <span class="mi">4096</span>
    <span class="n">v</span><span class="p">.</span><span class="nf">cpus</span> <span class="o">=</span> <span class="mi">2</span>
  <span class="k">end</span>

  <span class="n">config</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">define</span> <span class="s2">"k8s-control-plane"</span> <span class="k">do</span> <span class="o">|</span><span class="n">control</span><span class="o">|</span>
    <span class="n">control</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">hostname</span> <span class="o">=</span> <span class="s2">"k8s-control-plane"</span>
    <span class="n">control</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">network</span> <span class="s2">"private_network"</span><span class="p">,</span> <span class="ss">ip: </span><span class="s2">"192.168.56.10"</span>
  <span class="k">end</span>

  <span class="n">config</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">define</span> <span class="s2">"k8s-worker-01"</span> <span class="k">do</span> <span class="o">|</span><span class="n">worker</span><span class="o">|</span>
    <span class="n">worker</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">hostname</span> <span class="o">=</span> <span class="s2">"k8s-worker-01"</span>
    <span class="n">worker</span><span class="p">.</span><span class="nf">vm</span><span class="p">.</span><span class="nf">network</span> <span class="s2">"private_network"</span><span class="p">,</span> <span class="ss">ip: </span><span class="s2">"192.168.56.11"</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>Now, bring up the machines:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vagrant up
</code></pre></div></div>

<h4 id="step-2-install-container-runtime-and-kubernetes-tools-on-both-vms">Step 2: Install Container Runtime and Kubernetes Tools on Both VMs</h4>

<p>Execute these commands on <strong>both</strong> the <code class="language-plaintext highlighter-rouge">k8s-control-plane</code> and <code class="language-plaintext highlighter-rouge">k8s-worker-01</code> VMs. You can connect using <code class="language-plaintext highlighter-rouge">vagrant ssh k8s-control-plane</code> and <code class="language-plaintext highlighter-rouge">vagrant ssh k8s-worker-01</code>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># SSH into each VM and run the following:</span>

<span class="c"># 1. Install prerequisites and containerd</span>
<span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> apt-transport-https ca-certificates curl
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/apt/keyrings
curl <span class="nt">-fsSL</span> https://download.docker.com/linux/ubuntu/gpg | <span class="nb">sudo </span>gpg <span class="nt">--dearmor</span> <span class="nt">-o</span> /etc/apt/keyrings/docker.gpg
<span class="nb">echo</span> <span class="s2">"deb [arch=</span><span class="si">$(</span>dpkg <span class="nt">--print-architecture</span><span class="si">)</span><span class="s2"> signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu </span><span class="si">$(</span>lsb_release <span class="nt">-cs</span><span class="si">)</span><span class="s2"> stable"</span> | <span class="nb">sudo tee</span> /etc/apt/sources.list.d/docker.list <span class="o">&gt;</span> /dev/null
<span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> containerd.io

<span class="c"># 2. Configure containerd and enable required kernel modules</span>
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/containerd
<span class="nb">sudo </span>containerd config default | <span class="nb">sudo tee</span> /etc/containerd/config.toml
<span class="nb">sudo sed</span> <span class="nt">-i</span> <span class="s1">'s/SystemdCgroup = false/SystemdCgroup = true/'</span> /etc/containerd/config.toml
<span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
</span><span class="no">EOF
</span><span class="nb">sudo </span>modprobe overlay
<span class="nb">sudo </span>modprobe br_netfilter
<span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
</span><span class="no">EOF
</span><span class="nb">sudo </span>sysctl <span class="nt">--system</span>
<span class="nb">sudo </span>systemctl restart containerd

<span class="c"># 3. Disable swap</span>
<span class="nb">sudo </span>swapoff <span class="nt">-a</span>
<span class="nb">sudo sed</span> <span class="nt">-i</span> <span class="s1">'/ swap / s/^\(.*\)$/#\1/g'</span> /etc/fstab

<span class="c"># 4. Install kubeadm, kubelet, and kubectl</span>
<span class="nb">sudo </span>apt-get update
curl <span class="nt">-fsSLo</span> /usr/share/keyrings/kubernetes-archive-keyring.gpg https://packages.cloud.google.com/apt/doc/apt-key.gpg
<span class="nb">echo</span> <span class="s2">"deb [signed-by=/usr/share/keyrings/kubernetes-archive-keyring.gpg] https://apt.kubernetes.io/ kubernetes-xenial main"</span> | <span class="nb">sudo tee</span> /etc/apt/sources.list.d/kubernetes.list
<span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> kubelet kubeadm kubectl
<span class="nb">sudo </span>apt-mark hold kubelet kubeadm kubectl
</code></pre></div></div>

<h4 id="step-3-initialize-the-control-plane-node">Step 3: Initialize the Control-Plane Node</h4>

<p>Run this command <strong>only on the <code class="language-plaintext highlighter-rouge">k8s-control-plane</code> VM</strong>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane</span>
<span class="nb">sudo </span>kubeadm init <span class="nt">--pod-network-cidr</span><span class="o">=</span>10.244.0.0/16 <span class="nt">--apiserver-advertise-address</span><span class="o">=</span>192.168.56.10
</code></pre></div></div>
<p>This command will take a few minutes. At the end, it will output two important things:</p>
<ol>
  <li>Commands to set up <code class="language-plaintext highlighter-rouge">kubectl</code> for the <code class="language-plaintext highlighter-rouge">vagrant</code> user.</li>
  <li>A <code class="language-plaintext highlighter-rouge">kubeadm join</code> command with a token. <strong>Copy this join command and save it.</strong></li>
</ol>

<p>Run the <code class="language-plaintext highlighter-rouge">kubectl</code> setup commands on the control-plane node:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="nv">$HOME</span>/.kube
<span class="nb">sudo cp</span> <span class="nt">-i</span> /etc/kubernetes/admin.conf <span class="nv">$HOME</span>/.kube/config
<span class="nb">sudo chown</span> <span class="si">$(</span><span class="nb">id</span> <span class="nt">-u</span><span class="si">)</span>:<span class="si">$(</span><span class="nb">id</span> <span class="nt">-g</span><span class="si">)</span> <span class="nv">$HOME</span>/.kube/config
</code></pre></div></div>

<h4 id="step-4-install-a-cni-network-plugin">Step 4: Install a CNI Network Plugin</h4>

<p>The cluster nodes will not be <code class="language-plaintext highlighter-rouge">Ready</code> until a CNI is installed. From the control-plane node, install Calico:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane</span>
kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/projectcalico/calico/v3.25.0/manifests/calico.yaml
</code></pre></div></div>

<h4 id="step-5-join-the-worker-node">Step 5: Join the Worker Node</h4>

<p>Now, SSH into the <code class="language-plaintext highlighter-rouge">k8s-worker-01</code> VM and run the <code class="language-plaintext highlighter-rouge">kubeadm join</code> command that you saved from Step 3. It will look something like this (your token and hash will be different):</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-worker-01</span>
<span class="nb">sudo </span>kubeadm <span class="nb">join </span>192.168.56.10:6443 <span class="nt">--token</span> abcdef.1234567890abcdef <span class="se">\</span>
	<span class="nt">--discovery-token-ca-cert-hash</span> sha256:1234...cdef
</code></pre></div></div>

<p>After a minute, verify that both nodes are <code class="language-plaintext highlighter-rouge">Ready</code> from the control-plane node:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane</span>
kubectl get nodes
</code></pre></div></div>

<h4 id="step-6-provision-the-static-configuration-on-the-control-plane">Step 6: Provision the Static Configuration on the Control-Plane</h4>

<p>This is the critical step. We must place our custom scheduler’s configuration file at the exact path <code class="language-plaintext highlighter-rouge">/etc/kubernetes/config/scheduler-config.yaml</code> on the <code class="language-plaintext highlighter-rouge">k8s-control-plane</code> VM.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run this from your local machine (not inside a VM)</span>
vagrant ssh k8s-control-plane <span class="nt">--</span> <span class="nt">-t</span> <span class="s1">'
  sudo mkdir -p /etc/kubernetes/config &amp;&amp; \
  sudo bash -c "cat &gt; /etc/kubernetes/config/scheduler-config.yaml" &lt;&lt;EOF
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
clientConnection:
  kubeconfig: "/etc/kubernetes/scheduler.conf"
profiles:
  - schedulerName: my-custom-scheduler
    plugins:
      multiPoint:
        enabled:
          - name: "Networkspeed"
EOF'</span>

<span class="c"># Verify the file was created correctly</span>
vagrant ssh k8s-control-plane <span class="nt">--</span> <span class="s2">"sudo cat /etc/kubernetes/config/scheduler-config.yaml"</span>
</code></pre></div></div>

<h4 id="step-7-load-the-custom-scheduler-image-onto-the-cluster-nodes">Step 7: Load the Custom Scheduler Image onto the Cluster Nodes</h4>

<p>The container image we built exists only on our local machine. We need to transfer it to our VMs.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. On your local machine, save the Docker image to a tar file</span>
docker save localhost:5000/scheduler-plugins/kube-scheduler:v0.0.0 <span class="o">&gt;</span> scheduler.tar

<span class="c"># 2. Copy the tar file to both VMs</span>
vagrant scp scheduler.tar k8s-control-plane:/home/vagrant/scheduler.tar
vagrant scp scheduler.tar k8s-worker-01:/home/vagrant/scheduler.tar

<span class="c"># 3. SSH into EACH VM and load the image into containerd</span>
<span class="c"># On k8s-control-plane:</span>
vagrant ssh k8s-control-plane <span class="nt">--</span> <span class="s2">"sudo ctr -n=k8s.io images import /home/vagrant/scheduler.tar"</span>
<span class="c"># On k8s-worker-01:</span>
vagrant ssh k8s-worker-01 <span class="nt">--</span> <span class="s2">"sudo ctr -n=k8s.io images import /home/vagrant/scheduler.tar"</span>
</code></pre></div></div>

<h4 id="step-8-deploy-the-custom-scheduler">Step 8: Deploy the Custom Scheduler</h4>

<p>Create the final <code class="language-plaintext highlighter-rouge">deploy-kubeadm.yaml</code> manifest on your local machine. This file is identical to the previous “correct” version, as it’s designed to run in a real cluster environment.</p>

<p><code class="language-plaintext highlighter-rouge">deploy-kubeadm.yaml</code>:</p>
<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ServiceAccount</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">kube-system</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ClusterRoleBinding</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-custom-scheduler-as-kube-scheduler</span>
<span class="na">subjects</span><span class="pi">:</span>
<span class="pi">-</span> <span class="na">kind</span><span class="pi">:</span> <span class="s">ServiceAccount</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">kube-system</span>
<span class="na">roleRef</span><span class="pi">:</span>
  <span class="na">kind</span><span class="pi">:</span> <span class="s">ClusterRole</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">system:kube-scheduler</span>
  <span class="na">apiGroup</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">kube-system</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">serviceAccountName</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
      <span class="na">nodeSelector</span><span class="pi">:</span>
        <span class="na">node-role.kubernetes.io/control-plane</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>
      <span class="na">tolerations</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">effect</span><span class="pi">:</span> <span class="s">NoSchedule</span>
        <span class="na">key</span><span class="pi">:</span> <span class="s">node-role.kubernetes.io/control-plane</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">my-custom-scheduler-ctr</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">localhost:5000/scheduler-plugins/kube-scheduler:v0.0.0</span>
          <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
          <span class="na">args</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">/bin/kube-scheduler</span>
            <span class="pi">-</span> <span class="s">--authentication-kubeconfig=/etc/kubernetes/scheduler.conf</span>
            <span class="pi">-</span> <span class="s">--authorization-kubeconfig=/etc/kubernetes/scheduler.conf</span>
            <span class="pi">-</span> <span class="s">--config=/etc/kubernetes/config/scheduler-config.yaml</span>
            <span class="pi">-</span> <span class="s">-v=3</span>
          <span class="na">volumeMounts</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/etc/kubernetes</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">etckubernetes</span>
            <span class="na">readOnly</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">etckubernetes</span>
        <span class="na">hostPath</span><span class="pi">:</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">/etc/kubernetes</span>
          <span class="na">type</span><span class="pi">:</span> <span class="s">Directory</span>
</code></pre></div></div>

<p>Now, apply this manifest to your new <code class="language-plaintext highlighter-rouge">kubeadm</code> cluster. You can copy this file to the control-plane node or configure <code class="language-plaintext highlighter-rouge">kubectl</code> on your host to point to the new cluster. The simplest is to run it from the control-plane:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Copy the YAML to the control plane</span>
vagrant scp deploy-kubeadm.yaml k8s-control-plane:/home/vagrant/

<span class="c"># Apply it from within the control plane VM</span>
vagrant ssh k8s-control-plane <span class="nt">--</span> <span class="s2">"kubectl apply -f deploy-kubeadm.yaml"</span>
</code></pre></div></div>

<p>Verify the scheduler pod is running on the control-plane node:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vagrant ssh k8s-control-plane <span class="nt">--</span> <span class="s2">"kubectl get pods -n kube-system -l app=my-custom-scheduler -o wide"</span>
</code></pre></div></div>

<hr />

<h3 id="part-6-testing-the-custom-scheduler"><strong>Part 6: Testing the Custom Scheduler</strong></h3>

<p>The testing process is identical, but you will run the commands from your <code class="language-plaintext highlighter-rouge">k8s-control-plane</code> VM where <code class="language-plaintext highlighter-rouge">kubectl</code> is configured.</p>

<ol>
  <li><strong>Label the worker node:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c"># On k8s-control-plane VM</span>
    kubectl label node k8s-worker-01 network-topology<span class="o">=</span>high-speed
</code></pre></div>    </div>
  </li>
  <li><strong>Create and apply <code class="language-plaintext highlighter-rouge">test-pod.yaml</code>:</strong>
    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># test-pod.yaml</span>
    <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
    <span class="na">metadata</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">high-speed-pod</span>
    <span class="na">annotations</span><span class="pi">:</span>
        <span class="na">my-network-preference</span><span class="pi">:</span> <span class="s2">"</span><span class="s">high-speed"</span>
    <span class="na">spec</span><span class="pi">:</span>
    <span class="na">schedulerName</span><span class="pi">:</span> <span class="s">my-custom-scheduler</span>
    <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="s">image</span><span class="err">:</span> <span class="s">nginx</span>
</code></pre></div>    </div>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane VM, apply the pod</span>
kubectl apply <span class="nt">-f</span> test-pod.yaml
</code></pre></div>    </div>
  </li>
  <li><strong>Verify the pod placement:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On k8s-control-plane VM</span>
kubectl get pod high-speed-pod <span class="nt">-o</span> wide
</code></pre></div>    </div>
    <p>The pod should be running on the <code class="language-plaintext highlighter-rouge">k8s-worker-01</code> node.</p>
  </li>
</ol>

<p>You have now successfully built a custom scheduler and deployed it to a realistic, multi-node <code class="language-plaintext highlighter-rouge">kubeadm</code> cluster.</p>

<p><strong>Your Professional Next Steps:</strong></p>

<ol>
  <li><strong>Write Unit Tests:</strong> Create a <code class="language-plaintext highlighter-rouge">pkg/networkspeed/networkspeed_test.go</code> file. The repository has many examples. Once written, you can run them with <code class="language-plaintext highlighter-rouge">make unit-test</code>.</li>
  <li><strong>Run Integration Tests:</strong> The project has a framework for integration tests that spin up real API servers. You can add one for your plugin and run it via <code class="language-plaintext highlighter-rouge">make integration-test</code>.</li>
  <li><strong>Pushing to a Real Registry:</strong> When you are ready to share your scheduler, you can use the <code class="language-plaintext highlighter-rouge">push-images</code> target. This requires setting environment variables:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Example for pushing to Docker Hub</span>
<span class="nb">export </span><span class="nv">REGISTRY</span><span class="o">=</span>docker.io/your-username
<span class="nb">export </span><span class="nv">RELEASE_VERSION</span><span class="o">=</span>v0.1.0
make push-images 
</code></pre></div>    </div>
    <p>This will build and push a properly tagged image (e.g., <code class="language-plaintext highlighter-rouge">docker.io/your-username/kube-scheduler:v0.1.0</code>) to a remote registry.</p>
  </li>
  <li><strong>Contribute Back:</strong> If you’ve built a generally useful plugin, you can contribute it back to the <code class="language-plaintext highlighter-rouge">scheduler-plugins</code> project! Following this workflow (<code class="language-plaintext highlighter-rouge">make verify</code>, adding tests) is the first step to a successful Pull Request.</li>
</ol>]]></content><author><name></name></author><category term="tutorial" /><category term="kubernetes" /><category term="scheduler" /><category term="kubeadm" /><category term="plugin" /><category term="opensource" /><summary type="html"><![CDATA[Here is a complete, A-to-Z guide in English to develop and deploy your own Kubernetes scheduler using the Scheduling Framework. This guide is practical, detailed, and follows a step-by-step approach from concept to a working deployment in a kubeadm cluster.]]></summary></entry><entry><title type="html">Master the CI/CD Pipeline Locally</title><link href="https://menraromial.com/blog/2025/master-ci-ci-pipeline-locally/" rel="alternate" type="text/html" title="Master the CI/CD Pipeline Locally" /><published>2025-08-20T10:30:00+00:00</published><updated>2025-08-20T10:30:00+00:00</updated><id>https://menraromial.com/blog/2025/master-ci-ci-pipeline-locally</id><content type="html" xml:base="https://menraromial.com/blog/2025/master-ci-ci-pipeline-locally/"><![CDATA[<p>Welcome to this comprehensive course on DevOps culture and tooling. The goal is to guide you, step-by-step, from theory to practice, empowering you to build, secure, and automate a full Continuous Integration and Continuous Deployment (CI/CD) pipeline.</p>

<p><strong>Course Philosophy:</strong>
Everything we learn will be put into practice in a <strong>100% local lab environment</strong>, running on your own machine using Docker. You will not need any cloud accounts or paid services. Every concept is illustrated with hands-on workshops and concrete examples.</p>

<h2 id="course-navigation"><strong>Course Navigation</strong></h2>

<ul>
  <li><a href="#module-00-introduction-to-devops--lab-setup">Module 00: Introduction to DevOps &amp; Lab Setup</a></li>
  <li><a href="#module-01-source-code-management-with-gitea">Module 01: Source Code Management with Gitea</a></li>
  <li><a href="#module-02-containerization-with-docker">Module 02: Containerization with Docker</a></li>
  <li><a href="#module-03-continuous-integration-ci-with-jenkins">Module 03: Continuous Integration (CI) with Jenkins</a></li>
  <li><a href="#module-04-orchestration-with-kubernetes-via-kind">Module 04: Orchestration with Kubernetes (via Kind)</a></li>
  <li><a href="#module-05-continuous-deployment-cd-to-kubernetes">Module 05: Continuous Deployment (CD) to Kubernetes</a></li>
  <li><a href="#module-06-reverse-proxy--load-balancing-nginx-ingress">Module 06: Reverse Proxy &amp; Load Balancing (Nginx Ingress)</a></li>
  <li><a href="#module-07-infrastructure-as-code-iac-with-terraform">Module 07: Infrastructure as Code (IaC) with Terraform</a></li>
  <li><a href="#module-08-configuration-management-with-ansible">Module 08: Configuration Management with Ansible</a></li>
  <li><a href="#module-09-integrating-security-devsecops">Module 09: Integrating Security (DevSecOps)</a></li>
  <li><a href="#module-10-monitoring--observability">Module 10: Monitoring &amp; Observability</a></li>
  <li><a href="#module-11-final-project---the-complete-a-to-z-pipeline">Module 11: Final Project - The Complete A-to-Z Pipeline</a></li>
  <li><a href="#module-12-conclusion--next-steps">Module 12: Conclusion &amp; Next Steps</a></li>
</ul>

<h2 id="module-00-introduction-to-devops--lab-setup"><strong>Module 00: Introduction to DevOps &amp; Lab Setup</strong></h2>

<h3 id="what-is-devops">What is DevOps?</h3>

<p>DevOps is not a tool, but a <strong>culture</strong> and a <strong>philosophy</strong>. It’s a portmanteau of “Development” (Dev) and “Operations” (Ops). The goal is to break down the silos between development teams, who want to deliver new features quickly, and operations teams, who want to ensure production stability.</p>

<p>The pillars of DevOps are based on the <strong>C.A.L.M.S</strong> model:</p>
<ul>
  <li><strong>C</strong>ulture: Fostering collaboration, sharing, and trust.</li>
  <li><strong>A</strong>utomation: Automating everything that is repetitive (tests, builds, deployments).</li>
  <li><strong>L</strong>ean: Optimizing processes, eliminating waste, and delivering value quickly.</li>
  <li><strong>M</strong>easurement: Collecting metrics to understand and improve the system.</li>
  <li><strong>S</strong>haring: Sharing knowledge, tools, and responsibilities.</li>
</ul>

<h3 id="our-local-lab-architecture">Our Local Lab Architecture</h3>

<p>We will use <code class="language-plaintext highlighter-rouge">docker-compose</code> to orchestrate our lab services. This will allow us to easily start and stop all the necessary tools, which will communicate over a shared Docker network.</p>

<pre><code class="language-mermaid">graph TD
    subgraph "Your Host Machine"
        subgraph "Docker Network 'devops-net'"
            Gitea[Gitea&lt;br&gt;git.lab.local:3000]
            Jenkins[Jenkins&lt;br&gt;jenkins.lab.local:8080]
            Registry[Docker Registry&lt;br&gt;registry.lab.local:5000]
        end
        subgraph "Kubernetes Cluster (Kind)"
            K8sControl[Control Plane]
            K8sWorker[Worker Node]
            App[Deployed Application]
        end
        Jenkins -- Pull/Clone --&gt; Gitea
        Jenkins -- Build &amp; Push --&gt; Registry
        Jenkins -- Deploy --&gt; K8sControl
        K8sWorker -- Pull Image --&gt; Registry
        K8sControl -- Manages --&gt; K8sWorker
        K8sWorker -- Runs --&gt; App
    end
    User[User] -- Access via Browser --&gt; Services
</code></pre>

<h3 id="prerequisites">Prerequisites</h3>

<ul>
  <li><strong>Docker</strong> and <strong>Docker Compose</strong> installed.</li>
  <li>A text editor (e.g., VS Code).</li>
  <li>A terminal.</li>
  <li>Basic knowledge of the Linux command line.</li>
  <li><strong>Kind</strong> and <strong>kubectl</strong> installed for the Kubernetes module.</li>
</ul>

<h3 id="workshop-launching-the-lab-with-an-automated-script">Workshop: Launching the Lab with an Automated Script</h3>

<p>To make setting up our lab environment as simple as possible, we will use a shell script. This script embodies the DevOps principle of automation. It will perform prerequisite checks, build our custom Jenkins image, start all services, and verify that everything is running correctly, providing a smooth start to the course.</p>

<h4 id="step-1-create-the-project-directory-structure">Step 1: Create the Project Directory Structure</h4>

<p>First, create a root folder for our project, for example, <code class="language-plaintext highlighter-rouge">devops-course-lab</code>. All subsequent files and folders will be created inside this one.</p>

<h4 id="step-2-create-the-custom-jenkins-image-definition">Step 2: Create the Custom Jenkins Image Definition</h4>

<p>Our lab requires a Jenkins container that can execute Docker commands. The official Jenkins image doesn’t include the Docker CLI, so we must build our own.</p>

<ul>
  <li>Inside <code class="language-plaintext highlighter-rouge">devops-course-lab</code>, create a new folder named <code class="language-plaintext highlighter-rouge">jenkins-image</code>.</li>
  <li>Inside <code class="language-plaintext highlighter-rouge">jenkins-image</code>, create a file named <code class="language-plaintext highlighter-rouge">Dockerfile</code>.</li>
</ul>

<p><strong><code class="language-plaintext highlighter-rouge">jenkins-image/Dockerfile</code>:</strong></p>
<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Start from the official Jenkins image</span>
<span class="k">FROM</span><span class="s"> jenkins/jenkins:lts</span>

<span class="c"># Switch to the root user to install packages</span>
<span class="k">USER</span><span class="s"> root</span>

<span class="c"># Install necessary dependencies and the Docker CLI</span>
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> lsb-release curl gpg
<span class="k">RUN </span>curl <span class="nt">-fsSL</span> https://download.docker.com/linux/debian/gpg | gpg <span class="nt">--dearmor</span> <span class="nt">-o</span> /usr/share/keyrings/docker-archive-keyring.gpg
<span class="k">RUN </span><span class="nb">echo</span> <span class="s2">"deb [arch=</span><span class="si">$(</span>dpkg <span class="nt">--print-architecture</span><span class="si">)</span><span class="s2"> signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/debian </span><span class="si">$(</span>lsb_release <span class="nt">-cs</span><span class="si">)</span><span class="s2"> stable"</span> | <span class="nb">tee</span> /etc/apt/sources.list.d/docker.list <span class="o">&gt;</span> /dev/null
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> docker-ce-cli

<span class="c"># --- THE FIX IS HERE ---</span>
<span class="c"># 1. Create the 'docker' group</span>
<span class="c">#    The installation of docker-ce-cli doesn't create it, so we do it manually.</span>
<span class="k">RUN </span>groupadd docker

<span class="c"># 2. Add the 'jenkins' user to the 'docker' group</span>
<span class="c">#    This allows the jenkins user to communicate with the Docker socket.</span>
<span class="k">RUN </span>usermod <span class="nt">-aG</span> docker jenkins

<span class="c"># Switch back to the jenkins user</span>
<span class="k">USER</span><span class="s"> jenkins</span>
</code></pre></div></div>

<h4 id="step-3-define-the-lab-services-with-docker-compose">Step 3: Define the Lab Services with Docker Compose</h4>

<p>At the root of the <code class="language-plaintext highlighter-rouge">devops-course-lab</code> folder, create your <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> file. This file defines all the services needed for our course.</p>

<p><strong><code class="language-plaintext highlighter-rouge">docker-compose.yml</code>:</strong></p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3.8'</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">gitea</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">gitea/gitea:latest</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">gitea</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">USER_UID=1000</span>
      <span class="pi">-</span> <span class="s">USER_GID=1000</span>
      <span class="pi">-</span> <span class="s">GITEA__database__DB_TYPE=sqlite3</span>
      <span class="pi">-</span> <span class="s">GITEA__database__PATH=/data/gitea/gitea.db</span>
      <span class="pi">-</span> <span class="s">GITEA__server__DOMAIN=localhost</span>
      <span class="pi">-</span> <span class="s">GITEA__server__HTTP_PORT=3000</span>
      <span class="pi">-</span> <span class="s">GITEA__server__ROOT_URL=http://localhost:3000/</span>
      <span class="pi">-</span> <span class="s">GITEA__security__INSTALL_LOCK=true</span>
      <span class="pi">-</span> <span class="s">GITEA__security__SECRET_KEY=changeme</span>
      <span class="pi">-</span> <span class="s">GITEA__security__INTERNAL_TOKEN=changeme</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">unless-stopped</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">gitea-data:/data</span>
      <span class="pi">-</span> <span class="s">/etc/timezone:/etc/timezone:ro</span>
      <span class="pi">-</span> <span class="s">/etc/localtime:/etc/localtime:ro</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3000:3000"</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">2222:22"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

  <span class="na">jenkins</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">./jenkins-image</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">jenkins</span>
    <span class="na">privileged</span><span class="pi">:</span> <span class="kc">true</span> <span class="c1"># Required for Docker-in-Docker</span>
    <span class="na">user</span><span class="pi">:</span> <span class="s">root</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">50000:50000"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./jenkins_home:/var/jenkins_home</span>
      <span class="pi">-</span> <span class="s">/var/run/docker.sock:/var/run/docker.sock</span> <span class="c1"># Mount the Docker socket</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

  <span class="na">registry</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">registry:2</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">registry2</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">5002:5000"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./registry_data:/var/lib/registry</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">gitea-data</span><span class="pi">:</span>
  <span class="na">jenkins_home</span><span class="pi">:</span>
  <span class="na">registry_data</span><span class="pi">:</span>
  
<span class="na">networks</span><span class="pi">:</span>
  <span class="na">devops-net</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">devops-net</span>
    <span class="na">driver</span><span class="pi">:</span> <span class="s">bridge</span>
</code></pre></div></div>

<h4 id="step-4-create-the-automated-startup-script">Step 4: Create the Automated Startup Script</h4>

<p>This script is the main entry point for starting, stopping, and managing the lab. Create a new file named <code class="language-plaintext highlighter-rouge">start-lab.sh</code> at the root of your <code class="language-plaintext highlighter-rouge">devops-course-lab</code> directory.</p>

<p><strong><code class="language-plaintext highlighter-rouge">start-lab.sh</code>:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># Startup Script for the DevOps CI/CD Lab</span>
<span class="c"># This script configures and starts all necessary services for the course.</span>

<span class="nb">set</span> <span class="nt">-e</span>

<span class="nb">echo</span> <span class="s2">"🚀 Starting the DevOps CI/CD Lab"</span>
<span class="nb">echo</span> <span class="s2">"========================================"</span>

<span class="c"># Colors for output</span>
<span class="nv">RED</span><span class="o">=</span><span class="s1">'\033[0;31m'</span>
<span class="nv">GREEN</span><span class="o">=</span><span class="s1">'\033[0;32m'</span>
<span class="nv">YELLOW</span><span class="o">=</span><span class="s1">'\033[1;33m'</span>
<span class="nv">BLUE</span><span class="o">=</span><span class="s1">'\033[0;34m'</span>
<span class="nv">NC</span><span class="o">=</span><span class="s1">'\033[0m'</span> <span class="c"># No Color</span>

<span class="c"># --- 1. Prerequisite Checks ---</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">1. Checking prerequisites...</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="k">if</span> <span class="o">!</span> <span class="nb">command</span> <span class="nt">-v</span> docker &amp;&gt; /dev/null<span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="s2">❌ Docker is not installed or not in your PATH.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">exit </span>1
<span class="k">fi</span>

<span class="c"># Function to find and use the correct docker compose command</span>
compose_cmd<span class="o">()</span> <span class="o">{</span>
    <span class="k">if </span><span class="nb">command</span> <span class="nt">-v</span> docker-compose &amp;&gt; /dev/null<span class="p">;</span> <span class="k">then
        </span>docker-compose <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
    <span class="k">elif </span>docker compose version &amp;&gt; /dev/null<span class="p">;</span> <span class="k">then
        </span>docker compose <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
    <span class="k">else
        </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="s2">❌ Docker Compose is not installed.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
        <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"Please install the Docker Compose V2 plugin or the standalone docker-compose."</span>
        <span class="nb">exit </span>1
    <span class="k">fi</span>
<span class="o">}</span>

<span class="k">if</span> <span class="o">!</span> docker info &amp;&gt; /dev/null<span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="s2">❌ Docker daemon is not running or not accessible.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">exit </span>1
<span class="k">fi

</span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"✅ </span><span class="k">${</span><span class="nv">GREEN</span><span class="k">}</span><span class="s2">Docker and Docker Compose are available.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="c"># Allow arguments to be passed to docker-compose, like 'logs' or 'down'</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"$#"</span> <span class="nt">-gt</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span>compose_cmd <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
    <span class="nb">exit </span>0
<span class="k">fi</span>

<span class="c"># --- 2. Clean Up Existing Resources ---</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">2. Cleaning up existing resources...</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="si">$(</span>compose_cmd ps <span class="nt">-q</span><span class="si">)</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"🧹 Shutting down existing services..."</span>
    compose_cmd down <span class="nt">--remove-orphans</span>
<span class="k">fi
</span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"✅ </span><span class="k">${</span><span class="nv">GREEN</span><span class="k">}</span><span class="s2">Cleanup complete.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="c"># --- 3. Build and Start Services ---</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">3. Building and starting services...</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"📦 Building custom Jenkins image (this may take a few minutes on first run)..."</span>
compose_cmd build <span class="nt">--no-cache</span>

<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"🚀 Starting services in detached mode..."</span>
compose_cmd up <span class="nt">-d</span>

<span class="c"># --- 4. Verifying Service Health ---</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">4. Verifying service status...</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"⏳ Waiting for services to initialize (this can take up to a minute)..."</span>

check_service<span class="o">()</span> <span class="o">{</span>
    <span class="nb">local </span><span class="nv">service_name</span><span class="o">=</span><span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">container_name</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">url</span><span class="o">=</span><span class="s2">"</span><span class="nv">$3</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">max_attempts</span><span class="o">=</span>20
    <span class="nb">local </span><span class="nv">attempt</span><span class="o">=</span>1
    
    <span class="nb">echo</span> <span class="nt">-n</span> <span class="nt">-e</span> <span class="s2">"🔍 Verifying </span><span class="nv">$service_name</span><span class="s2">..."</span>
    
    <span class="k">while</span> <span class="o">[</span> <span class="nv">$attempt</span> <span class="nt">-le</span> <span class="nv">$max_attempts</span> <span class="o">]</span><span class="p">;</span> <span class="k">do
        </span><span class="nv">http_status</span><span class="o">=</span><span class="si">$(</span>curl <span class="nt">-s</span> <span class="nt">-o</span> /dev/null <span class="nt">-L</span> <span class="nt">-w</span> <span class="s2">"%{http_code}"</span> <span class="s2">"</span><span class="nv">$url</span><span class="s2">"</span><span class="si">)</span>
        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$http_status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"200"</span> <span class="o">]</span> <span class="o">||</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$http_status</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"302"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
            </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">" </span><span class="k">${</span><span class="nv">GREEN</span><span class="k">}</span><span class="s2">Online.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
            <span class="k">return </span>0
        <span class="k">fi
        </span><span class="nb">echo</span> <span class="nt">-n</span> <span class="s2">"."</span>
        <span class="nb">sleep </span>5
        <span class="nv">attempt</span><span class="o">=</span><span class="k">$((</span>attempt <span class="o">+</span> <span class="m">1</span><span class="k">))</span>
    <span class="k">done
    
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">" </span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="s2">Offline.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"❌ </span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="nv">$service_name</span><span class="s2"> did not become available. Check logs with: ./start-lab.sh logs </span><span class="nv">$container_name</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="k">return </span>1
<span class="o">}</span>

<span class="c"># Verify key services</span>
<span class="nv">SERVICES_OK</span><span class="o">=</span><span class="nb">true</span>
<span class="c"># Gitea can be slow to start, so we wait a bit before checking</span>
<span class="nb">sleep </span>15 
check_service <span class="s2">"Gitea"</span> <span class="s2">"gitea"</span> <span class="s2">"http://localhost:3000"</span> <span class="o">||</span> <span class="nv">SERVICES_OK</span><span class="o">=</span><span class="nb">false
</span>check_service <span class="s2">"Jenkins"</span> <span class="s2">"jenkins"</span> <span class="s2">"http://localhost:8080/login"</span> <span class="o">||</span> <span class="nv">SERVICES_OK</span><span class="o">=</span><span class="nb">false</span>

<span class="c"># --- 5. Final Status and Information ---</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">5. Final lab status:</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
compose_cmd ps

<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$SERVICES_OK</span><span class="s2">"</span> <span class="o">=</span> <span class="nb">true</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="s2">🎉 </span><span class="k">${</span><span class="nv">GREEN</span><span class="k">}</span><span class="s2">Lab started successfully!</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">BLUE</span><span class="k">}</span><span class="s2">📋 Access Information:</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"┌──────────────┬─────────────────────────┬──────────────────────────────────┐"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"│ </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">Service</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">       │ </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">URL</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">                      │ </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">Notes</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">                               │"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"├──────────────┼─────────────────────────┼──────────────────────────────────┤"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"│ Gitea        │ http://localhost:3000   │ Create admin user on first visit │"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"│ Jenkins      │ http://localhost:8080   │ Get initial password from log    │"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"│ Docker Registry │ localhost:5000          │ For image pushes from pipeline   │"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"└──────────────┴─────────────────────────┴──────────────────────────────────┘"</span>
    
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">BLUE</span><span class="k">}</span><span class="s2">🎯 Next Steps:</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"1. Open your browser and set up an admin account on Gitea: </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">http://localhost:3000</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"2. Log in to Jenkins: </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">http://localhost:8080</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"   - Get the initial password with: </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"3. Configure Docker to trust the insecure registry (see course notes)."</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"4. Begin Module 1: Source Code Management."</span>
    
<span class="k">else
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="s2">❌ </span><span class="k">${</span><span class="nv">RED</span><span class="k">}</span><span class="s2">One or more services failed to start correctly.</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
    <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"Please check the logs for errors."</span>
<span class="k">fi

</span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">💡 Useful Commands:</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"- View all logs:      </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">./start-lab.sh logs</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"- View specific log:  </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">./start-lab.sh logs jenkins</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"- Stop the lab:       </span><span class="k">${</span><span class="nv">YELLOW</span><span class="k">}</span><span class="s2">./start-lab.sh down</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>

<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="se">\n</span><span class="s2">========================================"</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"🎓 </span><span class="k">${</span><span class="nv">GREEN</span><span class="k">}</span><span class="s2">Happy DevOps Learning!</span><span class="k">${</span><span class="nv">NC</span><span class="k">}</span><span class="s2">"</span>
</code></pre></div></div>

<h4 id="step-5-make-the-script-executable-and-launch-the-lab">Step 5: Make the Script Executable and Launch the Lab</h4>

<p>Before running the script, you must give it execution permissions.</p>

<ol>
  <li><strong>Make the script executable:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x start-lab.sh
</code></pre></div>    </div>
  </li>
  <li><strong>Launch the lab:</strong>
Now, you can start the entire lab with one simple command:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./start-lab.sh
</code></pre></div>    </div>
    <p>The script will guide you through the process, check for prerequisites, build the Jenkins image, start all containers, and verify their status. If successful, it will provide you with all the URLs and information you need to begin the course.</p>
  </li>
  <li>Launch the lab:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker-compose up <span class="nt">-d</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Docker Configuration for Local Registry (VERY IMPORTANT)</strong>:
Our registry is insecure (HTTP). We need to tell Docker to trust it. Edit (or create) the file <code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code> (on Linux/Mac) and add:
    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"insecure-registries"</span><span class="w"> </span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"localhost:5000"</span><span class="p">,</span><span class="w"> </span><span class="s2">"registry:5000"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
    <blockquote>
      <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">registry:5000</code> is necessary so that containers <em>inside</em> the Docker network (like Jenkins or Kubernetes/Kind) can access the registry by its service name.</p>
    </blockquote>

    <p>Then restart the Docker service: <code class="language-plaintext highlighter-rouge">sudo systemctl restart docker</code>.</p>
  </li>
</ol>

<h4 id="step-6-configure-docker-for-the-local-registry">Step 6: Configure Docker for the Local Registry</h4>

<p>One final manual step is required. Our local Docker Registry is insecure (HTTP), so you must configure your Docker daemon to trust it.</p>

<ul>
  <li>Edit (or create) the Docker daemon configuration file. It is usually located at <code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code> on Linux/Mac.</li>
  <li>Add the following content:
    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"insecure-registries"</span><span class="w"> </span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"localhost:5000"</span><span class="p">,</span><span class="w"> </span><span class="s2">"registry:5000"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
  <li>Save the file and <strong>restart your Docker daemon</strong>. On most Linux systems, this is done with <code class="language-plaintext highlighter-rouge">sudo systemctl restart docker</code>.</li>
</ul>

<p>You are now fully set up and ready to begin the course!</p>

<h3 id="sources--further-reading">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Book:</strong> <em>The Phoenix Project</em> by Gene Kim, Kevin Behr, and George Spafford.</li>
  <li><strong>Documentation:</strong> <a href="https://docs.docker.com/compose/">Docker Compose Overview</a></li>
</ul>

<hr />

<h2 id="module-01-source-code-management-with-gitea"><strong>Module 01: Source Code Management with Gitea</strong></h2>

<p>Every DevOps project starts with code. A version control system like Git is essential. Gitea is a lightweight alternative to GitLab or GitHub, perfect for our lab.</p>

<h3 id="key-concepts">Key Concepts</h3>

<ul>
  <li><strong>Git:</strong> A distributed version control system.</li>
  <li><strong>Repository:</strong> A folder containing your project and its Git history.</li>
  <li><strong>Commit / Branch / Merge:</strong> The basic Git operations for saving, isolating, and combining work.</li>
  <li><strong>Webhook:</strong> A mechanism that allows Gitea to notify another service (Jenkins) when an event occurs (e.g., a <code class="language-plaintext highlighter-rouge">push</code>).</li>
</ul>

<h3 id="workshop-configure-gitea-and-create-our-project">Workshop: Configure Gitea and Create Our Project</h3>

<ol>
  <li><strong>Access and Configuration:</strong>
    <ul>
      <li>Open your browser to <code class="language-plaintext highlighter-rouge">http://localhost:3000</code>.</li>
      <li>Follow the installation wizard.
        <ul>
          <li>Database Type: <code class="language-plaintext highlighter-rouge">SQLite3</code>.</li>
          <li><strong>Gitea Base URL:</strong> <code class="language-plaintext highlighter-rouge">http://gitea:3000</code> (important for internal Docker communication).</li>
          <li>Create your admin account.</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>Create a New Repository:</strong>
    <ul>
      <li>Log in, click the <code class="language-plaintext highlighter-rouge">+</code> icon -&gt; “New Repository”.</li>
      <li>Repository Name: <code class="language-plaintext highlighter-rouge">hello-devops</code>.</li>
      <li>Click “Create Repository”.</li>
    </ul>
  </li>
  <li><strong>Create Our Base Application:</strong>
    <ul>
      <li>In a new folder on your machine, create a file named <code class="language-plaintext highlighter-rouge">app.py</code>:
        <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">flask</span> <span class="kn">import</span> <span class="n">Flask</span>
<span class="kn">import</span> <span class="n">os</span>

<span class="n">app</span> <span class="o">=</span> <span class="nc">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="c1"># An environment variable to demonstrate configuration
</span>    <span class="n">version</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">APP_VERSION</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">v1.0</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Hello, DevOps World! This is version: </span><span class="si">{</span><span class="n">version</span><span class="si">}</span><span class="sh">"</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">'</span><span class="s">__main__</span><span class="sh">'</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="sh">'</span><span class="s">0.0.0.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">5000</span><span class="p">)</span>
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
  <li><strong>Push the Code to Gitea:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Replace &lt;your_user&gt; with your Gitea username</span>
git init
git add app.py
git commit <span class="nt">-m</span> <span class="s2">"Initial commit"</span>
git branch <span class="nt">-M</span> main
git remote add origin http://localhost:3000/&lt;your_user&gt;/hello-devops.git
git push <span class="nt">-u</span> origin main
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="sources--further-reading-1">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Documentation:</strong> <a href="https://docs.gitea.io/">Gitea Documentation</a></li>
  <li><strong>Tutorial:</strong> <a href="https://git-scm.com/book/en/v2">Pro Git Book</a></li>
</ul>

<hr />

<h2 id="module-02-containerization-with-docker"><strong>Module 02: Containerization with Docker</strong></h2>

<p>Docker allows us to “package” an application and its dependencies into a <strong>container</strong>. This ensures the application will run the same way everywhere.</p>

<h3 id="key-concepts-1">Key Concepts</h3>

<ul>
  <li><strong>Docker Image:</strong> A read-only template (the “recipe”).</li>
  <li><strong>Docker Container:</strong> A runnable instance of an image (the “prepared dish”).</li>
  <li><strong>Dockerfile:</strong> A text file containing instructions to build an image.</li>
  <li><strong>Docker Registry:</strong> An image storage service (like our local registry).</li>
</ul>

<pre><code class="language-mermaid">graph TD
    A[Dockerfile] -- docker build --&gt; B(Docker Image);
    B -- docker run --&gt; C{Container};
    B -- docker push --&gt; D[Docker Registry];
    D -- docker pull --&gt; B;
</code></pre>

<h3 id="workshop-dockerizing-our-application">Workshop: Dockerizing Our Application</h3>

<ol>
  <li>
    <p><strong>Create the <code class="language-plaintext highlighter-rouge">Dockerfile</code>:</strong> At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create this file:</p>

    <div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use a lightweight base image</span>
<span class="k">FROM</span><span class="s"> python:3.9-slim</span>

<span class="c"># Set the working directory</span>
<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="c"># Install dependencies</span>
<span class="k">COPY</span><span class="s"> requirements.txt .</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="c"># Copy the application code</span>
<span class="k">COPY</span><span class="s"> app.py .</span>

<span class="c"># Expose the application port</span>
<span class="k">EXPOSE</span><span class="s"> 5000</span>

<span class="c"># Command to start the application</span>
<span class="k">CMD</span><span class="s"> ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Create <code class="language-plaintext highlighter-rouge">requirements.txt</code>:</strong>
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flask==2.1.2
gunicorn==20.1.0
Werkzeug==2.3.7
</code></pre></div>    </div>
    <blockquote>
      <p><strong>Note:</strong> We use <code class="language-plaintext highlighter-rouge">gunicorn</code>, a robust WSGI server, which is more suitable for “production” than Flask’s built-in development server.</p>
    </blockquote>
  </li>
  <li><strong>Build and Test the Image:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Build the image</span>
docker build <span class="nt">-t</span> localhost:5000/hello-devops:1.0 <span class="nb">.</span>

<span class="c"># Run a container to test it</span>
docker run <span class="nt">-d</span> <span class="nt">--name</span> test-app <span class="nt">-p</span> 8001:5000 <span class="nt">-e</span> <span class="nv">APP_VERSION</span><span class="o">=</span>v1.0-test localhost:5000/hello-devops:1.0

<span class="c"># Check your browser at http://localhost:8001</span>
<span class="c"># Stop and remove the test container</span>
docker stop test-app <span class="o">&amp;&amp;</span> docker <span class="nb">rm </span>test-app
</code></pre></div>    </div>
  </li>
  <li><strong>Push the Image to Our Local Registry:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push localhost:5000/hello-devops:1.0
</code></pre></div>    </div>
  </li>
  <li><strong>Push the New Files to Gitea:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add Dockerfile requirements.txt
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Dockerfile for the application"</span>
git push origin main
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="sources--further-reading-2">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Documentation:</strong> <a href="https://docs.docker.com/get-started/">Docker Get Started</a></li>
  <li><strong>Best Practices:</strong> <a href="https://docs.docker.com/develop/develop-images/dockerfile_best-practices/">Best practices for writing Dockerfiles</a></li>
</ul>

<hr />

<h2 id="module-03-continuous-integration-ci-with-jenkins"><strong>Module 03: Continuous Integration (CI) with Jenkins</strong></h2>

<p>Continuous Integration (CI) automates the building and testing of code with every change. Jenkins is the perfect tool to orchestrate this process.</p>

<h3 id="key-concepts-2">Key Concepts</h3>

<ul>
  <li><strong>Pipeline:</strong> A set of steps (build, test, push) executed by Jenkins.</li>
  <li><strong>Jenkinsfile:</strong> The definition of the pipeline as code, versioned with the application.</li>
  <li><strong>Agent:</strong> A machine (or container) where Jenkins executes tasks.</li>
</ul>

<h3 id="workshop-create-a-ci-pipeline-with-jenkins">Workshop: Create a CI Pipeline with Jenkins</h3>

<ol>
  <li><strong>Initial Jenkins Setup:</strong>
    <ul>
      <li>Access Jenkins: <code class="language-plaintext highlighter-rouge">http://localhost:8080</code>.</li>
      <li>Get the initial admin password: <code class="language-plaintext highlighter-rouge">docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword</code>.</li>
      <li>Install suggested plugins and create your user.</li>
      <li><strong>Install the <code class="language-plaintext highlighter-rouge">Docker Pipeline</code> and <code class="language-plaintext highlighter-rouge">Gitea</code> plugins</strong> via “Manage Jenkins” &gt; “Manage Plugins”.</li>
    </ul>
  </li>
  <li>
    <p><strong>Create the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</strong>
At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>.</p>

    <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>

    <span class="n">environment</span> <span class="o">{</span>
        <span class="n">REGISTRY_URL</span> <span class="o">=</span> <span class="s1">'registry:5000'</span> <span class="c1">// Jenkins communicates via Docker service name</span>
        <span class="n">IMAGE_NAME</span> <span class="o">=</span> <span class="s2">"hello-devops"</span>
        <span class="n">IMAGE_TAG</span> <span class="o">=</span> <span class="s2">"build-${env.BUILD_NUMBER}"</span> <span class="c1">// Use build number for a unique tag</span>
    <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">checkout</span> <span class="n">scm</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build &amp; Push Docker Image'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="c1">// 1. Build the image and store the resulting 'Image' object in a variable</span>
                    <span class="kt">def</span> <span class="n">customImage</span> <span class="o">=</span> <span class="n">docker</span><span class="o">.</span><span class="na">build</span><span class="o">(</span><span class="s2">"${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span><span class="o">,</span> <span class="s2">"."</span><span class="o">)</span>
                        
                    <span class="c1">// 2. Call the .push() method on the 'Image' object</span>
                    <span class="n">customImage</span><span class="o">.</span><span class="na">push</span><span class="o">()</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="n">post</span> <span class="o">{</span>
        <span class="n">always</span> <span class="o">{</span>
            <span class="c1">// Clean up the image on the Jenkins agent to save space</span>
            <span class="n">echo</span> <span class="s2">"Cleaning up local Docker image..."</span>
            <span class="n">sh</span> <span class="s2">"docker rmi ${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG} || true"</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Push the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to Gitea:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add Jenkinsfile
git commit <span class="nt">-m</span> <span class="s2">"ci: Add Jenkinsfile for CI pipeline"</span>
git push origin main
</code></pre></div>    </div>
  </li>
  <li><strong>Create the Pipeline in Jenkins:</strong>
    <ul>
      <li>“New Item” &gt; Name: <code class="language-plaintext highlighter-rouge">hello-devops-pipeline</code> &gt; Type: <code class="language-plaintext highlighter-rouge">Pipeline</code> &gt; OK.</li>
      <li>In the “Pipeline” section, choose “Pipeline script from SCM”.</li>
      <li>SCM: <code class="language-plaintext highlighter-rouge">Git</code>.</li>
      <li>Repository URL: <code class="language-plaintext highlighter-rouge">http://gitea:3000/&lt;your_user&gt;/hello-devops.git</code>.</li>
      <li>Script Path: <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>.</li>
      <li>Save.</li>
    </ul>
  </li>
  <li><strong>Run and Automate:</strong>
    <ul>
      <li>Click “Build Now” to test it.</li>
      <li><strong>Configure the Webhook in Gitea:</strong>
        <ul>
          <li>Repository Settings &gt; Webhooks &gt; Add Webhook &gt; Gitea.</li>
          <li>Target URL: <code class="language-plaintext highlighter-rouge">http://jenkins:8080/gitea-webhook/post</code>.</li>
          <li>Add the webhook.</li>
        </ul>
      </li>
      <li>Now, every <code class="language-plaintext highlighter-rouge">git push</code> will automatically trigger a build!</li>
    </ul>
  </li>
</ol>

<h3 id="sources--further-reading-3">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Documentation:</strong> <a href="https://www.jenkins.io/doc/book/pipeline/">Jenkins User Handbook - Pipeline</a></li>
</ul>

<hr />

<h2 id="module-04-orchestration-with-kubernetes-via-kind"><strong>Module 04: Orchestration with Kubernetes (via Kind)</strong></h2>

<p>Kubernetes (K8s) is a container orchestration system. It automates the deployment, scaling, and management of containerized applications. <strong>Kind (Kubernetes in Docker)</strong> lets us create a local K8s cluster using Docker containers as “nodes”.</p>

<h3 id="key-concepts-3">Key Concepts</h3>

<ul>
  <li><strong>Cluster:</strong> A set of machines (nodes) that run containerized applications.</li>
  <li><strong>Node:</strong> A worker machine (VM or physical) in a cluster.</li>
  <li><strong>Pod:</strong> The smallest deployable unit in K8s. A pod holds one or more containers.</li>
  <li><strong>Deployment:</strong> An object that manages a set of replicated pods, ensuring a certain number are always running.</li>
  <li><strong>Service:</strong> An abstraction that exposes a set of pods as a network service with a stable IP address and port.</li>
  <li><strong>kubectl:</strong> The command-line tool for interacting with a K8s cluster.</li>
</ul>

<h3 id="workshop-create-a-local-cluster-and-deploy-manually">Workshop: Create a Local Cluster and Deploy Manually</h3>

<ol>
  <li><strong>Prerequisites:</strong> Ensure you have installed <code class="language-plaintext highlighter-rouge">kind</code> and <code class="language-plaintext highlighter-rouge">kubectl</code>.
    <ul>
      <li><a href="https://kind.sigs.k8s.io/docs/user/quick-start/#installation">Install Kind</a></li>
      <li><a href="https://kubernetes.io/docs/tasks/tools/install-kubectl/">Install kubectl</a></li>
    </ul>
  </li>
  <li>
    <p><strong>Create a Kind Configuration File:</strong>
Create a file named <code class="language-plaintext highlighter-rouge">kind-config.yaml</code>. This configuration is crucial for allowing the Kind cluster to communicate with our local Docker registry.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">kind</span><span class="pi">:</span> <span class="s">Cluster</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">kind.x-k8s.io/v1alpha4</span>
<span class="c1"># Allows the Kind cluster to connect to our lab's network</span>
<span class="na">containerdConfigPatches</span><span class="pi">:</span>
<span class="pi">-</span> <span class="pi">|-</span>
  <span class="s">[plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry:5000"]</span>
    <span class="s">endpoint = ["http://registry:5000"]</span>
<span class="na">nodes</span><span class="pi">:</span>
<span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">control-plane</span>
<span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">worker</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Launch the Kind Cluster:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create the cluster</span>
kind create cluster <span class="nt">--config</span> kind-config.yaml
    
<span class="c"># Connect the cluster to our lab's network</span>
docker network connect devops-net kind-control-plane
docker network connect devops-net kind-worker

<span class="c"># Verify that the nodes are ready</span>
kubectl get nodes
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Create Kubernetes Deployment Manifests:</strong>
In your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a <code class="language-plaintext highlighter-rouge">k8s</code> folder. Inside, create <code class="language-plaintext highlighter-rouge">deployment.yaml</code>.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/deployment.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># We want 2 instances of our app</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-container</span>
        <span class="c1"># IMPORTANT: We use the internal registry URL</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s">registry:5000/hello-devops:1.0</span> 
        <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">5000</span>
        <span class="na">env</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">APP_VERSION</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">v1.0-k8s"</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Create a Service to Expose the Deployment:</strong>
Create <code class="language-plaintext highlighter-rouge">service.yaml</code> in the <code class="language-plaintext highlighter-rouge">k8s</code> folder.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/service.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">NodePort</span> <span class="c1"># Exposes the service on a port of each node</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span> <span class="c1"># Targets pods with this label</span>
  <span class="na">ports</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">80</span> <span class="c1"># Port of the service inside the cluster</span>
    <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span> <span class="c1"># Port of the containers</span>
    <span class="na">nodePort</span><span class="pi">:</span> <span class="m">30007</span> <span class="c1"># External port to access the service</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Deploy to the Cluster:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Apply the manifests</span>
kubectl apply <span class="nt">-f</span> k8s/deployment.yaml
kubectl apply <span class="nt">-f</span> k8s/service.yaml

<span class="c"># Check the deployment</span>
kubectl get deployments
kubectl get pods
kubectl get services

<span class="c"># Access the application</span>
<span class="c"># The service is exposed on port 30007 of each node.</span>
<span class="c"># The node's IP is localhost since Kind runs on our machine.</span>
curl http://localhost:30007 
</code></pre></div>    </div>
    <p>You should see <code class="language-plaintext highlighter-rouge">Hello, DevOps World! This is version: v1.0-k8s</code>.</p>
  </li>
  <li><strong>Push the Manifests to Gitea:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add k8s/
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Kubernetes manifests"</span>
git push origin main
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="sources--further-reading-4">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Documentation:</strong> <a href="https://kubernetes.io/docs/home/">Kubernetes Documentation</a></li>
  <li><strong>Documentation:</strong> <a href="https://kind.sigs.k8s.io/docs/user/quick-start/">Kind Quick Start</a></li>
</ul>

<hr />

<h2 id="module-05-continuous-deployment-cd-to-kubernetes"><strong>Module 05: Continuous Deployment (CD) to Kubernetes</strong></h2>

<p>Now that our CI pipeline builds and pushes the image, the next step is to automatically deploy this new image to our Kubernetes cluster. This is Continuous Deployment (CD).</p>

<h3 id="key-concepts-4">Key Concepts</h3>

<ul>
  <li><strong>Continuous Deployment:</strong> The practice of automating the deployment of new application versions to production (or in our case, our local cluster).</li>
  <li><strong>Kubernetes Credentials:</strong> Jenkins needs permission to talk to our K8s cluster. We will use the <code class="language-plaintext highlighter-rouge">kubeconfig</code> file.</li>
</ul>

<h3 id="workshop-extend-the-jenkins-pipeline-for-deployment">Workshop: Extend the Jenkins Pipeline for Deployment</h3>

<ol>
  <li><strong>Prepare Credentials for Jenkins:</strong>
    <ul>
      <li>The <code class="language-plaintext highlighter-rouge">kubeconfig</code> file contains the connection information for your cluster. It’s usually located at <code class="language-plaintext highlighter-rouge">~/.kube/config</code>.</li>
      <li><strong>In Jenkins:</strong> Go to “Manage Jenkins” &gt; “Manage Credentials”.</li>
      <li>“Add Credentials” &gt; Kind: <code class="language-plaintext highlighter-rouge">Secret file</code>.</li>
      <li>Upload your <code class="language-plaintext highlighter-rouge">~/.kube/config</code> file.</li>
      <li>ID: <code class="language-plaintext highlighter-rouge">kubeconfig-kind</code>.</li>
      <li>Save.</li>
    </ul>
  </li>
  <li>
    <p><strong>Install <code class="language-plaintext highlighter-rouge">kubectl</code> on the Jenkins Agent:</strong>
Our base Jenkins container doesn’t have <code class="language-plaintext highlighter-rouge">kubectl</code>. The easiest way is to install it at the start of the pipeline.</p>
  </li>
  <li>
    <p><strong>Update the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</strong>
Modify your <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to add a new deployment stage.</p>

    <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>

    <span class="n">environment</span> <span class="o">{</span>
        <span class="n">REGISTRY_URL</span> <span class="o">=</span> <span class="s1">'registry:5000'</span>
        <span class="n">IMAGE_NAME</span> <span class="o">=</span> <span class="s2">"hello-devops"</span>
        <span class="n">IMAGE_TAG</span> <span class="o">=</span> <span class="s2">"build-${env.BUILD_NUMBER}"</span>
        <span class="n">KUBECONFIG_CREDENTIAL_ID</span> <span class="o">=</span> <span class="s1">'kubeconfig-kind'</span>
        <span class="n">K8S_DEPLOYMENT_NAME</span> <span class="o">=</span> <span class="s1">'hello-devops-deployment'</span>
    <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build &amp; Push Docker Image'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="kt">def</span> <span class="n">customImage</span> <span class="o">=</span> <span class="n">docker</span><span class="o">.</span><span class="na">build</span><span class="o">(</span><span class="s2">"${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span><span class="o">,</span> <span class="s2">"."</span><span class="o">)</span>
                    <span class="n">customImage</span><span class="o">.</span><span class="na">push</span><span class="o">()</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Install Kubectl'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">sh</span> <span class="s1">'''
                if ! command -v kubectl &amp;&gt; /dev/null
                then
                    echo "Kubectl not found. Installing..."
                    curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
                    install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
                fi
                kubectl version --client
                '''</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="c1">// Use the kubeconfig credentials</span>
                <span class="n">withCredentials</span><span class="o">([</span><span class="n">file</span><span class="o">(</span><span class="nl">credentialsId:</span> <span class="n">KUBECONFIG_CREDENTIAL_ID</span><span class="o">,</span> <span class="nl">variable:</span> <span class="s1">'KUBECONFIG_FILE'</span><span class="o">)])</span> <span class="o">{</span>
                    <span class="n">script</span> <span class="o">{</span>
                        <span class="c1">// Set the environment variable so kubectl uses this file</span>
                        <span class="n">sh</span> <span class="s2">"export KUBECONFIG=${KUBECONFIG_FILE}"</span>

                        <span class="c1">// Update the deployment's image with the new version</span>
                        <span class="c1">// Add --record to keep track of the command</span>
                        <span class="n">sh</span> <span class="s2">"kubectl set image deployment/${K8S_DEPLOYMENT_NAME} hello-devops-container=${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG} --record"</span>

                        <span class="c1">// Wait for the deployment to complete</span>
                        <span class="n">sh</span> <span class="s2">"kubectl rollout status deployment/${K8S_DEPLOYMENT_NAME}"</span>

                        <span class="n">echo</span> <span class="s2">"Deployment successful!"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
    <span class="n">post</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div>    </div>
    <blockquote>
      <p><strong>Note:</strong> The <code class="language-plaintext highlighter-rouge">kubectl set image</code> command is a simple, imperative way to update an image. In more advanced scenarios, tools like Helm or Kustomize are used for a more declarative approach.</p>
    </blockquote>
  </li>
  <li><strong>Push the Changes and Watch the Magic:</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">git add Jenkinsfile</code></li>
      <li><code class="language-plaintext highlighter-rouge">git commit -m "ci: Add CD stage to deploy to Kubernetes"</code></li>
      <li><code class="language-plaintext highlighter-rouge">git push origin main</code></li>
    </ul>

    <p>The webhook will trigger Jenkins. Watch the pipeline: it will build, push the image, install kubectl, and then update your K8s deployment with the brand-new image!</p>
  </li>
  <li><strong>Verification:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Watch the pods, you'll see the old ones terminate and new ones start</span>
kubectl get pods <span class="nt">-w</span>

<span class="c"># Check the deployment history</span>
kubectl rollout <span class="nb">history </span>deployment/hello-devops-deployment
</code></pre></div>    </div>
  </li>
</ol>

<hr />

<h2 id="module-06-reverse-proxy--load-balancing-nginx-ingress"><strong>Module 06: Reverse Proxy &amp; Load Balancing (Nginx Ingress)</strong></h2>

<p>Currently, we access our service via a <code class="language-plaintext highlighter-rouge">NodePort</code> (e.g., <code class="language-plaintext highlighter-rouge">30007</code>), which isn’t ideal. A <strong>reverse proxy</strong> acts as an entry point to your cluster. It receives requests and forwards them to the appropriate services.</p>

<h3 id="key-concepts-5">Key Concepts</h3>

<ul>
  <li><strong>Reverse Proxy:</strong> A server that sits in front of web servers and forwards client requests to the appropriate server. It can handle SSL, compression, caching, etc.</li>
  <li><strong>Load Balancing:</strong> Distributing incoming traffic across multiple servers (or pods) to improve performance and availability.</li>
  <li><strong>Ingress Controller (Kubernetes):</strong> A specific type of reverse proxy for Kubernetes that manages external access to cluster services, usually via HTTP/HTTPS.</li>
</ul>

<h3 id="architecture-with-an-ingress-controller">Architecture with an Ingress Controller</h3>

<pre><code class="language-mermaid">graph LR
    User -- http://hello.devops.local --&gt; IngressController(Nginx Ingress Controller&lt;br&gt;on K8s node port 80/443);
    IngressController -- Routing Rule --&gt; Service[hello-devops-service];
    Service -- Forwards to --&gt; Pod1[Pod hello-devops];
    Service -- Forwards to --&gt; Pod2[Pod hello-devops];
</code></pre>

<h3 id="workshop-deploy-an-nginx-ingress">Workshop: Deploy an Nginx Ingress</h3>

<ol>
  <li>
    <p><strong>Install the Nginx Ingress Controller on Kind:</strong>
Kind has specific documentation for this.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml

<span class="c"># Wait for the ingress controller pods to be ready</span>
kubectl <span class="nb">wait</span> <span class="nt">--namespace</span> ingress-nginx <span class="se">\</span>
  <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="se">\</span>
  <span class="nt">--selector</span><span class="o">=</span>app.kubernetes.io/component<span class="o">=</span>controller <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>90s
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Modify our Kubernetes Service:</strong>
The <code class="language-plaintext highlighter-rouge">NodePort</code> type is no longer needed. We’ll change it to <code class="language-plaintext highlighter-rouge">ClusterIP</code> (the default type), which is only accessible from within the cluster. Modify <code class="language-plaintext highlighter-rouge">k8s/service.yaml</code>:</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/service.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="c1"># type: ClusterIP (can be omitted as it's the default)</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
  <span class="na">ports</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
    <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Create an Ingress Resource:</strong>
Create <code class="language-plaintext highlighter-rouge">k8s/ingress.yaml</code>. This is the rule that tells Nginx how to route traffic.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/ingress.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-ingress</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">host</span><span class="pi">:</span> <span class="s2">"</span><span class="s">hello.devops.local"</span>
    <span class="na">http</span><span class="pi">:</span>
      <span class="na">paths</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
        <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
        <span class="na">backend</span><span class="pi">:</span>
          <span class="na">service</span><span class="pi">:</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
            <span class="na">port</span><span class="pi">:</span>
              <span class="na">number</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Modify Your Local <code class="language-plaintext highlighter-rouge">hosts</code> File:</strong>
To make your browser aware that <code class="language-plaintext highlighter-rouge">hello.devops.local</code> points to your local cluster, add this line to your <code class="language-plaintext highlighter-rouge">/etc/hosts</code> file (on Linux/Mac) or <code class="language-plaintext highlighter-rouge">C:\Windows\System32\drivers\etc\hosts</code> (on Windows).
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>127.0.0.1 hello.devops.local
</code></pre></div>    </div>
  </li>
  <li><strong>Deploy and Test:</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Apply the service changes and the new ingress</span>
kubectl apply <span class="nt">-f</span> k8s/service.yaml
kubectl apply <span class="nt">-f</span> k8s/ingress.yaml

<span class="c"># Check that the ingress is created</span>
kubectl get ingress
</code></pre></div>    </div>
    <p>Open your browser to <code class="language-plaintext highlighter-rouge">http://hello.devops.local</code>. It works! Nginx received the request and forwarded it to your service.</p>
  </li>
  <li><strong>Push to Gitea:</strong>
Don’t forget to version your new manifests!
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add k8s/
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Nginx Ingress for clean routing"</span>
git push origin main
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="sources--further-reading-5">Sources &amp; Further Reading</h3>
<ul>
  <li><strong>Documentation:</strong> <a href="https://kubernetes.io/docs/concepts/services-networking/ingress/">Kubernetes Ingress</a></li>
  <li><strong>Documentation:</strong> <a href="https://kubernetes.github.io/ingress-nginx/">Nginx Ingress Controller</a></li>
</ul>

<hr />

<h2 id="module-07-infrastructure-as-code-iac-with-terraform"><strong>Module 07: Infrastructure as Code (IaC) with Terraform</strong></h2>

<p>Infrastructure as Code (IaC) is the management and provisioning of infrastructure (servers, networks, etc.) through configuration files, rather than manual configuration. Terraform is the leading tool in this space.</p>

<h3 id="key-concepts-6">Key Concepts</h3>
<ul>
  <li><strong>Provider:</strong> A plugin that allows Terraform to interact with a specific API (AWS, Docker, Kubernetes…).</li>
  <li><strong>Resource:</strong> A block of code that describes an infrastructure component (e.g., a Docker network, a container).</li>
  <li><strong>State File:</strong> A file (usually <code class="language-plaintext highlighter-rouge">terraform.tfstate</code>) where Terraform records the state of the infrastructure it manages.</li>
  <li><strong>Plan:</strong> The <code class="language-plaintext highlighter-rouge">terraform plan</code> command shows what Terraform will create, modify, or destroy.</li>
  <li><strong>Apply:</strong> The <code class="language-plaintext highlighter-rouge">terraform apply</code> command executes the actions from the plan.</li>
</ul>

<h3 id="workshop-manage-our-docker-network-with-terraform">Workshop: Manage Our Docker Network with Terraform</h3>

<p>Instead of letting <code class="language-plaintext highlighter-rouge">docker-compose</code> create our <code class="language-plaintext highlighter-rouge">devops-net</code> network, we’ll manage it with Terraform to illustrate the IaC concept.</p>

<ol>
  <li>
    <p><strong>Prerequisites:</strong> <a href="https://learn.hashicorp.com/tutorials/terraform/install-cli">Install Terraform</a>.</p>
  </li>
  <li>
    <p><strong>Create Terraform Files:</strong>
At the root of your <code class="language-plaintext highlighter-rouge">devops-course-lab</code> project, create a <code class="language-plaintext highlighter-rouge">terraform</code> folder. Inside, create <code class="language-plaintext highlighter-rouge">main.tf</code>.</p>

    <div class="language-terraform highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># terraform/main.tf</span>

<span class="c1"># Declare required providers</span>
<span class="k">terraform</span> <span class="p">{</span>
  <span class="nx">required_providers</span> <span class="p">{</span>
    <span class="nx">docker</span> <span class="o">=</span> <span class="p">{</span>
      <span class="nx">source</span>  <span class="o">=</span> <span class="s2">"kreuzwerker/docker"</span>
      <span class="nx">version</span> <span class="o">=</span> <span class="s2">"~&gt; 2.16.0"</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="c1"># Configure the Docker provider</span>
<span class="k">provider</span> <span class="s2">"docker"</span> <span class="p">{}</span>

<span class="c1"># Define a resource of type "Docker network"</span>
<span class="k">resource</span> <span class="s2">"docker_network"</span> <span class="s2">"devops_network"</span> <span class="p">{</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"devops-net"</span>
  <span class="nx">driver</span> <span class="o">=</span> <span class="s2">"bridge"</span>
<span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Modify <code class="language-plaintext highlighter-rouge">docker-compose.yml</code>:</strong>
We need to tell <code class="language-plaintext highlighter-rouge">docker-compose</code> to use the external network that Terraform will create.
Modify your <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> at the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code>.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ... (services)</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">devops-net</span><span class="pi">:</span>
    <span class="c1"># Indicates that this network is external and already created</span>
    <span class="na">external</span><span class="pi">:</span> <span class="kc">true</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Run Terraform:</strong>
    <ul>
      <li>Stop and remove your current containers and networks to start fresh:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># In the devops-course-lab folder</span>
docker-compose down
docker network <span class="nb">rm </span>devops-net
</code></pre></div>        </div>
      </li>
      <li>Navigate into the <code class="language-plaintext highlighter-rouge">terraform</code> folder and run the commands:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Initialize Terraform and download the Docker provider</span>
terraform init

<span class="c"># Show what Terraform is going to do</span>
terraform plan

<span class="c"># Apply the changes (answer "yes")</span>
terraform apply
</code></pre></div>        </div>
        <p>Terraform has now created the <code class="language-plaintext highlighter-rouge">devops-net</code> network.</p>
      </li>
    </ul>
  </li>
  <li><strong>Relaunch the Lab:</strong>
Return to the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code> and restart <code class="language-plaintext highlighter-rouge">docker-compose</code>.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker-compose up <span class="nt">-d</span>
</code></pre></div>    </div>
    <p>The containers will start and attach to the Terraform-managed network!</p>
  </li>
</ol>

<h3 id="sources--further-reading-6">Sources &amp; Further Reading</h3>

<ul>
  <li><strong>Documentation:</strong> <a href="https://learn.hashicorp.com/tutorials/terraform/infrastructure-as-code">Introduction to Terraform</a></li>
  <li><strong>Provider:</strong> <a href="https://registry.terraform.io/providers/kreuzwerker/docker/latest/docs">Terraform Docker Provider</a></li>
</ul>

<hr />

<h2 id="module-08-configuration-management-with-ansible"><strong>Module 08: Configuration Management with Ansible</strong></h2>

<p>While Terraform is excellent for <em>provisioning</em> infrastructure (creating servers), Ansible is excellent for <em>configuring</em> it (installing software, managing config files, etc.).</p>

<h3 id="key-concepts-7">Key Concepts</h3>
<ul>
  <li><strong>Inventory:</strong> A list of servers (hosts) that Ansible should manage.</li>
  <li><strong>Playbook:</strong> A YAML file that describes a set of tasks to execute on the hosts.</li>
  <li><strong>Task:</strong> A single action that Ansible executes (e.g., install a package, copy a file).</li>
  <li><strong>Module:</strong> A tool that Ansible uses to accomplish a task (e.g., <code class="language-plaintext highlighter-rouge">apt</code>, <code class="language-plaintext highlighter-rouge">copy</code>, <code class="language-plaintext highlighter-rouge">service</code>).</li>
  <li><strong>Agentless:</strong> Ansible does not require an agent on the target machines. It typically connects via SSH.</li>
</ul>

<h3 id="workshop-use-ansible-to-configure-a-file-on-our-jenkins-container">Workshop: Use Ansible to Configure a File on our Jenkins Container</h3>

<p>We will simulate server configuration by using Ansible to create a file inside the Jenkins container.</p>

<ol>
  <li>
    <p><strong>Prerequisites:</strong> <a href="https://docs.ansible.com/ansible/latest/installation_guide/intro_installation.html">Install Ansible</a>.</p>
  </li>
  <li>
    <p><strong>Create Ansible Files:</strong>
At the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code>, create an <code class="language-plaintext highlighter-rouge">ansible</code> folder.</p>

    <ul>
      <li><strong>Create an inventory <code class="language-plaintext highlighter-rouge">inventory.ini</code>:</strong>
        <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ansible/inventory.ini
</span><span class="nn">[jenkins_server]</span>
<span class="c"># Target the container directly by its name using the docker connection
</span><span class="err">jenkins</span> <span class="py">ansible_connection</span><span class="p">=</span><span class="s">docker</span>
</code></pre></div>        </div>
      </li>
      <li><strong>Create a playbook <code class="language-plaintext highlighter-rouge">configure-jenkins.yml</code>:</strong>
        <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># ansible/configure-jenkins.yml</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure Jenkins Container</span>
    <span class="na">hosts</span><span class="pi">:</span> <span class="s">jenkins_server</span>
    <span class="na">become</span><span class="pi">:</span> <span class="s">yes</span> <span class="c1"># To execute tasks as root</span>

    <span class="na">tasks</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create a welcome message file</span>
        <span class="na">copy</span><span class="pi">:</span>
            <span class="na">content</span><span class="pi">:</span> <span class="s2">"</span><span class="s">This</span><span class="nv"> </span><span class="s">file</span><span class="nv"> </span><span class="s">was</span><span class="nv"> </span><span class="s">created</span><span class="nv"> </span><span class="s">by</span><span class="nv"> </span><span class="s">Ansible</span><span class="nv"> </span><span class="s">on</span><span class="nv"> </span><span class="s">."</span>
            <span class="na">dest</span><span class="pi">:</span> <span class="s">/var/jenkins_home/ansible_managed.txt</span>
            <span class="na">owner</span><span class="pi">:</span> <span class="s">jenkins</span>
            <span class="na">group</span><span class="pi">:</span> <span class="s">jenkins</span>
            <span class="na">mode</span><span class="pi">:</span> <span class="s1">'</span><span class="s">0644'</span>

        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure a package is installed (example)</span>
        <span class="na">apt</span><span class="pi">:</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">cowsay</span>
            <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
            <span class="na">update_cache</span><span class="pi">:</span> <span class="s">yes</span>
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
  <li><strong>Run the Ansible Playbook:</strong>
In your terminal, from the <code class="language-plaintext highlighter-rouge">ansible</code> folder:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># The command to run the playbook</span>
ansible-playbook <span class="nt">-i</span> inventory.ini configure-jenkins.yml
</code></pre></div>    </div>
  </li>
  <li><strong>Verify the Result:</strong>
Connect to the Jenkins container and check that the file was created and the package is installed.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check the file</span>
docker <span class="nb">exec </span>jenkins <span class="nb">cat</span> /var/jenkins_home/ansible_managed.txt

<span class="c"># Check the installed package</span>
docker <span class="nb">exec </span>jenkins cowsay <span class="s2">"Ansible was here!"</span>
</code></pre></div>    </div>
    <p>You’ll see the welcome message and the ASCII cow!</p>
  </li>
</ol>

<h3 id="sources--further-reading-7">Sources &amp; Further Reading</h3>
<ul>
  <li><strong>Documentation:</strong> <a href="https://docs.ansible.com/ansible/latest/index.html">Ansible Documentation</a></li>
  <li><strong>Connection Plugin:</strong> <a href="https://docs.ansible.com/ansible/latest/collections/community/docker/docker_connection.html">Ansible Docker Connection</a></li>
</ul>

<hr />

<h2 id="module-09-integrating-security-devsecops"><strong>Module 09: Integrating Security (DevSecOps)</strong></h2>

<p>DevSecOps is an extension of the DevOps culture that aims to integrate security into every stage of the software lifecycle. The idea is to “<strong>Shift Left</strong>,” meaning security is integrated as early as possible in the development process, rather than being a final check before production.</p>

<h3 id="key-concepts-8">Key Concepts</h3>

<ul>
  <li><strong>Shift Left:</strong> Detecting and fixing security flaws early in the cycle (during coding, building), where it is cheapest and fastest.</li>
  <li><strong>SAST (Static Application Security Testing):</strong> Analyzing source code without executing it to find vulnerabilities (e.g., SQL injections, hardcoded passwords).</li>
  <li><strong>SCA (Software Composition Analysis):</strong> Analyzing the project’s open-source dependencies to find known vulnerabilities (CVEs).</li>
  <li><strong>Container Image Scanning:</strong> Analyzing the final Docker image to find vulnerabilities in OS packages and libraries.</li>
</ul>

<h3 id="secure-pipeline-architecture">Secure Pipeline Architecture</h3>

<pre><code class="language-mermaid">graph TD
    subgraph "Jenkins Pipeline"
        direction LR
        A[Checkout] --&gt; B[SAST&lt;br&gt;SonarQube];
        B --&gt; C[Build Image];
        C --&gt; D[SCA &amp; Image Scan&lt;br&gt;Trivy];
        D --&gt; E{Push to Registry};
        E --&gt; F[Deploy to K8s];
    end
    Dev[git push] --&gt; A;
    B -- Sends report --&gt; SonarQubeUI[SonarQube UI];
    D -- Shows flaws --&gt; Console[Jenkins Console];
</code></pre>

<h3 id="workshop-securing-our-cicd-pipeline">Workshop: Securing our CI/CD Pipeline</h3>

<h4 id="1-add-sonarqube-to-the-lab">1. Add SonarQube to the Lab</h4>

<p>SonarQube is a leading platform for code quality and security analysis.
Add these services to your <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> file and restart with <code class="language-plaintext highlighter-rouge">docker-compose up -d</code>.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#... inside the services section:</span>

  <span class="na">sonarqube</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">sonarqube:lts-community</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">sonarqube</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">9000:9000"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.url=jdbc:postgresql://sonardb:5432/sonar</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.username=sonar</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.password=sonar</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonarqube_conf:/opt/sonarqube/conf</span>
      <span class="pi">-</span> <span class="s">sonarqube_data:/opt/sonarqube/data</span>
      <span class="pi">-</span> <span class="s">sonarqube_extensions:/opt/sonarqube/extensions</span>
      <span class="pi">-</span> <span class="s">sonarqube_logs:/opt/sonarqube/logs</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonardb</span>

  <span class="na">sonardb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">postgres:12</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">sonardb</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">POSTGRES_USER=sonar</span>
      <span class="pi">-</span> <span class="s">POSTGRES_PASSWORD=sonar</span>
      <span class="pi">-</span> <span class="s">POSTGRES_DB=sonar</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">postgresql:/var/lib/postgresql</span>
      <span class="pi">-</span> <span class="s">postgresql_data:/var/lib/postgresql/data</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

<span class="c1">#... at the end of the file, add the volumes:</span>
<span class="na">volumes</span><span class="pi">:</span>
  <span class="c1">#... (jenkins_home, etc.)</span>
  <span class="na">sonarqube_conf</span><span class="pi">:</span>
  <span class="na">sonarqube_data</span><span class="pi">:</span>
  <span class="na">sonarqube_extensions</span><span class="pi">:</span>
  <span class="na">sonarqube_logs</span><span class="pi">:</span>
  <span class="na">postgresql</span><span class="pi">:</span>
  <span class="na">postgresql_data</span><span class="pi">:</span>
</code></pre></div></div>

<h4 id="2-configure-sonarqube-and-jenkins">2. Configure SonarQube and Jenkins</h4>

<ol>
  <li><strong>SonarQube:</strong>
    <ul>
      <li>Access <code class="language-plaintext highlighter-rouge">http://localhost:9000</code> (it might take a few minutes to start).</li>
      <li>Log in with <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code>. Change the password.</li>
      <li>Go to “Administration” &gt; “Projects” &gt; “Management” and create a project manually. Name it <code class="language-plaintext highlighter-rouge">hello-devops</code>.</li>
      <li>Go to your profile (top right) &gt; “My Account” &gt; “Security”. Generate a token, name it <code class="language-plaintext highlighter-rouge">jenkins-token</code>, and copy it.</li>
    </ul>
  </li>
  <li><strong>Jenkins:</strong>
    <ul>
      <li>Install the <strong>SonarQube Scanner</strong> plugin.</li>
      <li>Go to “Manage Jenkins” &gt; “Configure System”. In the “SonarQube servers” section, add a server:
        <ul>
          <li>Name: <code class="language-plaintext highlighter-rouge">sonarqube</code></li>
          <li>Server URL: <code class="language-plaintext highlighter-rouge">http://sonarqube:9000</code></li>
          <li>Server authentication token: “Add” &gt; “Jenkins” &gt; Kind: <code class="language-plaintext highlighter-rouge">Secret text</code>, paste your token, ID: <code class="language-plaintext highlighter-rouge">sonarqube-token</code>.</li>
        </ul>
      </li>
      <li>Go to “Manage Jenkins” &gt; “Global Tool Configuration”. Under “SonarQube Scanner”, “Add SonarQube Scanner”, and choose automatic installation.</li>
    </ul>
  </li>
</ol>

<h4 id="3-update-the-application-and-pipeline">3. Update the Application and Pipeline</h4>

<ol>
  <li><strong>Add a SonarQube configuration file:</strong>
At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a file named <code class="language-plaintext highlighter-rouge">sonar-project.properties</code>:
    <div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">sonar.projectKey</span><span class="p">=</span><span class="s">hello-devops</span>
<span class="py">sonar.projectName</span><span class="p">=</span><span class="s">hello-devops</span>
<span class="py">sonar.projectVersion</span><span class="p">=</span><span class="s">1.0</span>
<span class="py">sonar.sources</span><span class="p">=</span><span class="s">.</span>
<span class="py">sonar.python.version</span><span class="p">=</span><span class="s">3.9</span>
<span class="py">sonar.sourceEncoding</span><span class="p">=</span><span class="s">UTF-8</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Modify the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to add security stages:</strong>
We will add a stage for SonarQube (SAST) and one for Trivy (Image Scanning).</p>

    <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>
    <span class="n">tools</span> <span class="o">{</span>
        <span class="c1">// Ensure the SonarQube Scanner tool is available</span>
        <span class="c1">// Name must match the one in Global Tool Configuration</span>
        <span class="n">sonarqube</span> <span class="s1">'SonarQubeScanner'</span> 
    <span class="o">}</span>
    <span class="n">environment</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="c1">// NEW STAGE: SAST</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Static Code Analysis'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="c1">// Use the SonarQube configuration defined in Jenkins</span>
                    <span class="n">withSonarQubeEnv</span><span class="o">(</span><span class="s1">'sonarqube'</span><span class="o">)</span> <span class="o">{</span>
                        <span class="c1">// 'scannerHome' is injected by the plugin</span>
                        <span class="n">sh</span> <span class="s2">"${scannerHome}/bin/sonar-scanner"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="c1">// NEW STAGE: Quality Gate</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'SonarQube Quality Gate'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="c1">// Wait for the analysis result and fail the pipeline if the Quality Gate is not passed</span>
                <span class="n">timeout</span><span class="o">(</span><span class="nl">time:</span> <span class="mi">1</span><span class="o">,</span> <span class="nl">unit:</span> <span class="s1">'HOURS'</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">waitForQualityGate</span> <span class="nl">abortPipeline:</span> <span class="kc">true</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
            
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build &amp; Push Docker Image'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="c1">// NEW STAGE: IMAGE SCANNING</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Vulnerability Scan (Image)'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="c1">// Use the official Trivy image to scan our image</span>
                    <span class="c1">// Fail the pipeline if a CRITICAL vulnerability is found</span>
                    <span class="n">sh</span> <span class="s2">"""
                    docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
                    aquasec/trivy:latest image --exit-code 1 --severity CRITICAL ${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}
                    """</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
            
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
    <span class="o">}</span>
    <span class="n">post</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="4-push-changes-and-analyze">4. Push Changes and Analyze</h4>

<ol>
  <li><code class="language-plaintext highlighter-rouge">git add sonar-project.properties Jenkinsfile</code></li>
  <li><code class="language-plaintext highlighter-rouge">git commit -m "feat(ci): Integrate SonarQube and Trivy scans"</code></li>
  <li><code class="language-plaintext highlighter-rouge">git push origin main</code></li>
</ol>

<p>Observe the new pipeline. You can now go to <code class="language-plaintext highlighter-rouge">http://localhost:9000</code> to see the full code analysis report, and check the Jenkins console for Trivy’s vulnerability report.</p>

<h3 id="sources--further-reading-8">Sources &amp; Further Reading</h3>
<ul>
  <li><strong>Documentation:</strong> <a href="https://docs.sonarqube.org/latest/analysis/scan/sonarscanner-for-jenkins/">SonarQube Scanner for Jenkins</a></li>
  <li><strong>Documentation:</strong> <a href="https://aquasecurity.github.io/trivy/v0.25.3/integrations/ci/jenkins/">Trivy - CI Integration</a></li>
</ul>

<hr />

<h2 id="module-10-monitoring--observability"><strong>Module 10: Monitoring &amp; Observability</strong></h2>

<p>Deploying is good, but knowing what’s happening is better. Monitoring and observability allow us to watch the health and performance of our application and infrastructure.</p>

<h3 id="key-concepts-9">Key Concepts</h3>

<ul>
  <li><strong>Monitoring:</strong> Collecting, processing, and displaying quantitative data about a system. It answers the question: “Is the system working?”.</li>
  <li><strong>Observability:</strong> The ability to understand a system’s internal state from its external outputs. It answers the question: “Why isn’t the system working?”.</li>
  <li><strong>The 3 Pillars of Observability:</strong>
    <ol>
      <li><strong>Metrics:</strong> Numerical measurements aggregated over time intervals (e.g., CPU usage, requests per second). Tool: <strong>Prometheus</strong>.</li>
      <li><strong>Logs:</strong> Records of discrete, timestamped events (e.g., application error, user request). Tool: <strong>Loki</strong>.</li>
      <li><strong>Traces:</strong> A representation of a request’s journey through the different services of a microservices architecture.</li>
    </ol>
  </li>
</ul>

<h3 id="our-monitoring-stack-architecture-the-prom-stack">Our Monitoring Stack Architecture (The “Prom Stack”)</h3>

<pre><code class="language-mermaid">graph TD
    subgraph "Kubernetes Cluster"
        Prometheus[Prometheus] -- Scrapes --&gt; AppMetrics(App Pod&lt;br&gt;/metrics);
        Prometheus -- Scrapes --&gt; KubeMetrics[Kube-State-Metrics];
        Promtail[Promtail Agent] -- Collects Logs --&gt; AppLogs(App Pod Logs);
        Promtail -- Sends Logs --&gt; Loki[Loki];
    end
    Grafana[Grafana] -- Queries --&gt; Prometheus;
    Grafana -- Queries --&gt; Loki;
    User[User] -- Visualizes --&gt; Grafana;
</code></pre>

<h3 id="workshop-set-up-prometheus-loki-and-grafana">Workshop: Set up Prometheus, Loki, and Grafana</h3>

<h4 id="1-instrument-the-application-to-expose-metrics">1. Instrument the Application to Expose Metrics</h4>

<p>Our application needs to expose a <code class="language-plaintext highlighter-rouge">/metrics</code> endpoint that Prometheus can read.</p>

<ol>
  <li><strong>Update <code class="language-plaintext highlighter-rouge">requirements.txt</code>:</strong>
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flask==2.1.2
gunicorn==20.1.0
prometheus-client==0.14.1
Werkzeug==2.3.7
</code></pre></div>    </div>
  </li>
  <li><strong>Modify <code class="language-plaintext highlighter-rouge">app.py</code>:</strong>
    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">flask</span> <span class="kn">import</span> <span class="n">Flask</span><span class="p">,</span> <span class="n">Response</span>
<span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">prometheus_client</span> <span class="kn">import</span> <span class="n">Counter</span><span class="p">,</span> <span class="n">generate_latest</span><span class="p">,</span> <span class="n">REGISTRY</span>

<span class="n">app</span> <span class="o">=</span> <span class="nc">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>

<span class="c1"># Create a counter for HTTP requests
</span><span class="n">http_requests_total</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">(</span><span class="sh">'</span><span class="s">http_requests_total</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">Total number of HTTP requests</span><span class="sh">'</span><span class="p">)</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="n">http_requests_total</span><span class="p">.</span><span class="nf">inc</span><span class="p">()</span> <span class="c1"># Increment the counter on each request
</span>    <span class="n">version</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">APP_VERSION</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">v2.0</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">&lt;h1&gt;Hello from the new automated pipeline!&lt;/h1&gt;&lt;p&gt;Version: </span><span class="si">{</span><span class="n">version</span><span class="si">}</span><span class="s">&lt;/p&gt;</span><span class="sh">"</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/metrics</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">metrics</span><span class="p">():</span>
    <span class="c1"># Expose metrics in the Prometheus format
</span>    <span class="k">return</span> <span class="nc">Response</span><span class="p">(</span><span class="nf">generate_latest</span><span class="p">(</span><span class="n">REGISTRY</span><span class="p">),</span> <span class="n">mimetype</span><span class="o">=</span><span class="sh">'</span><span class="s">text/plain</span><span class="sh">'</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">'</span><span class="s">__main__</span><span class="sh">'</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="sh">'</span><span class="s">0.0.0.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">5000</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="2-deploy-the-monitoring-stack-in-kubernetes">2. Deploy the Monitoring Stack in Kubernetes</h4>

<p>Manually installing each component is complex. We’ll use community Helm charts, which greatly simplify this process. You’ll need to <a href="https://helm.sh/docs/intro/install/">install Helm</a> first.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Add the Prometheus Community Helm repo</span>
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

<span class="c"># Install kube-prometheus-stack (includes Prometheus, Grafana, Alertmanager...)</span>
<span class="c"># into a dedicated 'monitoring' namespace</span>
kubectl create namespace monitoring
helm <span class="nb">install </span>prometheus prometheus-community/kube-prometheus-stack <span class="nt">--namespace</span> monitoring

<span class="c"># Install Loki and Promtail</span>
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update
helm <span class="nb">install </span>loki grafana/loki-stack <span class="nt">--namespace</span> monitoring
</code></pre></div></div>

<h4 id="3-configure-prometheus-to-scrape-our-application">3. Configure Prometheus to Scrape Our Application</h4>

<p>The <code class="language-plaintext highlighter-rouge">kube-prometheus-stack</code> uses <code class="language-plaintext highlighter-rouge">ServiceMonitor</code> resources to automatically discover services to scrape.</p>

<ol>
  <li><strong>Update your <code class="language-plaintext highlighter-rouge">k8s/service.yaml</code></strong> to add a name to the port, which is required by the <code class="language-plaintext highlighter-rouge">ServiceMonitor</code>.
    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># k8s/service.yaml</span>
    <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
    <span class="na">metadata</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
    <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span> <span class="c1"># Important label for discovery</span>
    <span class="na">spec</span><span class="pi">:</span>
    <span class="na">selector</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
    <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span> <span class="c1"># Name the port</span>
        <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
        <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
        <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Create a <code class="language-plaintext highlighter-rouge">k8s/servicemonitor.yaml</code> manifest:</strong>
    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">monitoring.coreos.com/v1</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">ServiceMonitor</span>
    <span class="na">metadata</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-monitor</span>
    <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span> <span class="c1"># Make sure it's in the same namespace as your service</span>
    <span class="na">labels</span><span class="pi">:</span>
        <span class="na">release</span><span class="pi">:</span> <span class="s">prometheus</span> <span class="c1"># This label is crucial for Prometheus to find it</span>
    <span class="na">spec</span><span class="pi">:</span>
    <span class="na">selector</span><span class="pi">:</span>
        <span class="na">matchLabels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span> <span class="c1"># Target services with this label</span>
    <span class="na">endpoints</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">port</span><span class="pi">:</span> <span class="s">http</span> <span class="c1"># Scrape the port named 'http'</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/metrics</span> <span class="c1"># On this path</span>
        <span class="na">interval</span><span class="pi">:</span> <span class="s">15s</span> <span class="c1"># Every 15 seconds</span>
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="4-access-grafana-and-visualize">4. Access Grafana and Visualize</h4>

<ol>
  <li><code class="language-plaintext highlighter-rouge">git add .</code> and <code class="language-plaintext highlighter-rouge">git commit/push</code> to deploy your instrumented application and new manifests.</li>
  <li>Expose Grafana on your local machine:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">--namespace</span> monitoring port-forward svc/prometheus-grafana 8082:80
</code></pre></div>    </div>
  </li>
  <li>Open <code class="language-plaintext highlighter-rouge">http://localhost:8082</code>.
    <ul>
      <li>Login: <code class="language-plaintext highlighter-rouge">admin</code>.</li>
      <li>Password: <code class="language-plaintext highlighter-rouge">prom-operator</code> (default, should be changed). You can retrieve it with <code class="language-plaintext highlighter-rouge">kubectl get secret --namespace monitoring prometheus-grafana -o jsonpath="{.data.admin-password}" | base64 --decode ; echo</code>.</li>
    </ul>
  </li>
  <li><strong>Explore:</strong>
    <ul>
      <li>Go to “Dashboards”. There are already pre-configured dashboards for Kubernetes!</li>
      <li>Go to “Explore”. Choose the “Prometheus” data source. Type <code class="language-plaintext highlighter-rouge">http_requests_total</code> and run. You’ll see your application’s counter!</li>
      <li>Change the data source to “Loki”. In the “Log browser”, select <code class="language-plaintext highlighter-rouge">app="hello-devops"</code>. You’ll see your application’s logs directly in Grafana.</li>
    </ul>
  </li>
</ol>

<h3 id="sources--further-reading-9">Sources &amp; Further Reading</h3>
<ul>
  <li><strong>Documentation:</strong> <a href="https://github.com/prometheus/client_python">prometheus-client for Python</a></li>
  <li><strong>Stack:</strong> <a href="https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack">kube-prometheus-stack</a></li>
  <li><strong>Stack:</strong> <a href="https://github.com/grafana/helm-charts/tree/main/charts/loki-stack">Loki Helm Chart</a></li>
</ul>

<hr />

<h2 id="module-11-final-project---the-complete-a-to-z-pipeline"><strong>Module 11: Final Project - The Complete A-to-Z Pipeline</strong></h2>

<p>Let’s recap everything we’ve learned by creating a complete workflow. The goal is to simulate a developer making a change and seeing it automatically propagate all the way to our Kubernetes environment, passing security checks along the way.</p>

<h3 id="the-scenario">The Scenario</h3>

<p>A developer needs to change the application’s message and update the version number. After they push their code, the CI/CD/CS (Continuous Security) chain must automatically:</p>
<ol>
  <li>Verify the code’s quality and security with SonarQube.</li>
  <li>Build a new Docker image with a unique tag.</li>
  <li>Scan the image for vulnerabilities with Trivy.</li>
  <li>If all checks pass, push the image to our local registry.</li>
  <li>Deploy the application to Kubernetes using a declarative approach.</li>
  <li>All of this must happen without any manual intervention after the <code class="language-plaintext highlighter-rouge">git push</code>.</li>
</ol>

<h3 id="final-workflow-architecture">Final Workflow Architecture</h3>

<pre><code class="language-mermaid">graph TD
    A[Developer&lt;br&gt;git push] --&gt; B{Gitea};
    B -- Webhook --&gt; C[Jenkins];
    C -- git clone --&gt; B;
    C -- SAST Scan --&gt; SonarQube;
    C -- docker build --&gt; D(Image v2.0);
    C -- Image Scan --&gt; Trivy;
    D -- docker push --&gt; E[Local Registry];
    C -- kubectl apply --&gt; F{Kubernetes Cluster};
    F -- docker pull --&gt; E;
    F -- Deploys --&gt; G[New Pods&lt;br&gt;with image v2.0];
    H[User] -- http://hello.devops.local --&gt; I[Nginx Ingress];
    I --&gt; G;
</code></pre>

<h3 id="workshop-the-final-push">Workshop: The Final Push</h3>

<ol>
  <li>
    <p><strong>Improve the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> for Declarative Deployment:</strong>
Our <code class="language-plaintext highlighter-rouge">kubectl set image</code> command is good, but it doesn’t apply other changes from our manifests. A better practice is to apply the manifest files directly.</p>

    <p>Modify the <code class="language-plaintext highlighter-rouge">Deploy to Kubernetes</code> stage in your <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</p>

    <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ...</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">withCredentials</span><span class="o">([</span><span class="n">file</span><span class="o">(</span><span class="nl">credentialsId:</span> <span class="n">KUBECONFIG_CREDENTIAL_ID</span><span class="o">,</span> <span class="nl">variable:</span> <span class="s1">'KUBECONFIG_FILE'</span><span class="o">)])</span> <span class="o">{</span>
                    <span class="n">script</span> <span class="o">{</span>
                        <span class="n">sh</span> <span class="s2">"export KUBECONFIG=${KUBECONFIG_FILE}"</span>

                        <span class="c1">// 1. Replace the image tag placeholder in the manifest</span>
                        <span class="c1">// We use sed to replace a placeholder we're about to add</span>
                        <span class="n">sh</span> <span class="s2">"sed -i 's|IMAGE_TAG_PLACEHOLDER|${IMAGE_TAG}|g' k8s/deployment.yaml"</span>
                            
                        <span class="c1">// 2. Apply all manifests from the k8s folder</span>
                        <span class="n">sh</span> <span class="s2">"kubectl apply -f k8s/"</span>

                        <span class="c1">// 3. Wait for the deployment</span>
                        <span class="n">sh</span> <span class="s2">"kubectl rollout status deployment/${K8S_DEPLOYMENT_NAME}"</span>
                        <span class="n">echo</span> <span class="s2">"Deployment successful!"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
<span class="c1">// ...</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Update <code class="language-plaintext highlighter-rouge">deployment.yaml</code> with a Placeholder:</strong>
Change the image line in <code class="language-plaintext highlighter-rouge">k8s/deployment.yaml</code> so <code class="language-plaintext highlighter-rouge">sed</code> can replace it.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/deployment.yaml (final modification)</span>
<span class="c1"># ...</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-container</span>
        <span class="c1"># Here's our placeholder!</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s">registry:5000/hello-devops:IMAGE_TAG_PLACEHOLDER</span>
<span class="c1"># ...</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Commit and Push:</strong>
This is the moment of truth. Gather all your changes and push them.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"refactor(ci): Use declarative deploy and update app to v2.0"</span>
git push origin main
</code></pre></div>    </div>
  </li>
  <li><strong>Observe and Verify:</strong>
    <ul>
      <li>Go to Jenkins and watch the pipeline run through all the stages: SAST, build, image scan, deploy.</li>
      <li>Once finished, open <code class="language-plaintext highlighter-rouge">http://hello.devops.local</code> in your browser.</li>
      <li>You should see your new application message!</li>
      <li>You can also verify the image used by the pods:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.items[0].spec.containers[0].image}'</span>
</code></pre></div>        </div>
        <p>This will display something like <code class="language-plaintext highlighter-rouge">registry:5000/hello-devops:build-X</code>.</p>
      </li>
    </ul>
  </li>
</ol>

<p><strong>Congratulations! You have set up and executed a complete, secure, and observable CI/CD pipeline, from code to production, entirely on your local machine.</strong></p>

<hr />

<h2 id="module-12-conclusion--next-steps"><strong>Module 12: Conclusion &amp; Next Steps</strong></h2>

<p>You have come a long way, from basic code management to a fully automated continuous deployment pipeline. You have gained practical skills in the most fundamental tools of the DevOps ecosystem.</p>

<h3 id="what-youve-accomplished">What You’ve Accomplished:</h3>

<ul>
  <li><strong>Set up a local DevOps lab</strong> with Docker Compose.</li>
  <li><strong>Versioned an application</strong> with Gitea.</li>
  <li><strong>Containerized the application</strong> with Docker.</li>
  <li><strong>Created a Continuous Integration (CI) pipeline</strong> with Jenkins that builds and stores artifacts.</li>
  <li><strong>Deployed a local Kubernetes cluster</strong> with Kind.</li>
  <li><strong>Implemented Continuous Deployment (CD)</strong> to automatically push changes to Kubernetes.</li>
  <li><strong>Exposed your application professionally</strong> with an Nginx Ingress.</li>
  <li><strong>Understood the principles of Infrastructure as Code</strong> with Terraform.</li>
  <li><strong>Discovered configuration management</strong> with Ansible.</li>
  <li><strong>Integrated security into the pipeline</strong> (<strong>DevSecOps</strong>) with SonarQube and Trivy.</li>
  <li><strong>Set up a professional-grade monitoring and logging stack</strong> with Prometheus, Grafana, and Loki.</li>
</ul>]]></content><author><name></name></author><category term="course" /><category term="Kubernetes" /><category term="docker" /><category term="ansible" /><category term="terraform" /><category term="jenkin" /><category term="gitea" /><summary type="html"><![CDATA[Welcome to this comprehensive course on DevOps culture and tooling. The goal is to guide you, step-by-step, from theory to practice, empowering you to build, secure, and automate a full Continuous Integration and Continuous Deployment (CI/CD) pipeline.]]></summary></entry><entry><title type="html">Setting Up a Kubernetes Cluster with Kubeadm and CRI-O</title><link href="https://menraromial.com/blog/2025/kubernetes-with-kubeadm-and-cri.io/" rel="alternate" type="text/html" title="Setting Up a Kubernetes Cluster with Kubeadm and CRI-O" /><published>2025-03-25T16:40:16+00:00</published><updated>2025-03-25T16:40:16+00:00</updated><id>https://menraromial.com/blog/2025/kubernetes-with-kubeadm-and-cri.io</id><content type="html" xml:base="https://menraromial.com/blog/2025/kubernetes-with-kubeadm-and-cri.io/"><![CDATA[<h2 id="prerequisites">Prerequisites</h2>
<p>Before setting up a Kubernetes cluster using Kubeadm and CRI-O, ensure that you meet the following requirements:</p>

<h3 id="hardware-requirements">Hardware Requirements</h3>
<ul>
  <li>At least <strong>2 GB of RAM</strong> per node (4 GB recommended for the master node)</li>
  <li>At least <strong>2 CPUs</strong> per node</li>
  <li><strong>20 GB of free disk space</strong></li>
  <li>A stable <strong>internet connection</strong></li>
  <li>All nodes should be able to communicate with each other</li>
</ul>

<h3 id="software-requirements">Software Requirements</h3>
<ul>
  <li>A supported <strong>Linux distribution</strong> (Ubuntu 20.04+ or Debian 10+ recommended)</li>
  <li>User access with <strong>sudo privileges</strong></li>
  <li>Basic understanding of <strong>Linux command-line interface (CLI)</strong></li>
  <li>Knowledge of <strong>networking concepts</strong></li>
</ul>

<h2 id="step-1-configure-kernel-parameters">Step 1: Configure Kernel Parameters</h2>
<p>Kubernetes requires some kernel parameters to be set to allow IPtables to see bridged traffic.</p>

<h3 id="load-required-kernel-modules">Load Required Kernel Modules</h3>
<p>Run the following commands on all nodes:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
</span><span class="no">EOF

</span><span class="nb">sudo </span>modprobe overlay
<span class="nb">sudo </span>modprobe br_netfilter
</code></pre></div></div>

<h3 id="set-kernel-parameters-for-kubernetes">Set Kernel Parameters for Kubernetes</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
</span><span class="no">EOF
</span></code></pre></div></div>

<p>Apply the changes:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sysctl <span class="nt">--system</span>
</code></pre></div></div>

<h2 id="step-2-disable-swap">Step 2: Disable Swap</h2>
<p>Kubernetes does not work well with swap enabled. Disable it using:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>swapoff <span class="nt">-a</span>
</code></pre></div></div>

<p>To make this change persistent, remove any swap entries from <code class="language-plaintext highlighter-rouge">/etc/fstab</code>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/fstab
</code></pre></div></div>

<h2 id="step-3-install-cri-o-container-runtime-interface---open">Step 3: Install CRI-O (Container Runtime Interface - Open)</h2>

<h3 id="add-cri-o-repository-and-install">Add CRI-O Repository and Install</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> software-properties-common gpg curl apt-transport-https ca-certificates

curl <span class="nt">-fsSL</span> https://pkgs.k8s.io/addons:/cri-o:/prerelease:/main/deb/Release.key | <span class="se">\</span>
    gpg <span class="nt">--dearmor</span> | <span class="nb">sudo tee</span> /etc/apt/keyrings/cri-o-apt-keyring.gpg <span class="o">&gt;</span>/dev/null
<span class="nb">echo</span> <span class="s2">"deb [signed-by=/etc/apt/keyrings/cri-o-apt-keyring.gpg] https://pkgs.k8s.io/addons:/cri-o:/prerelease:/main/deb/ /"</span> | <span class="se">\</span>
    <span class="nb">sudo tee</span> /etc/apt/sources.list.d/cri-o.list

<span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> cri-o
</code></pre></div></div>

<h3 id="enable-and-start-cri-o-service">Enable and Start CRI-O Service</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl daemon-reload
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>crio <span class="nt">--now</span>
<span class="nb">sudo </span>systemctl start crio.service
</code></pre></div></div>

<h2 id="step-4-install-kubernetes-components">Step 4: Install Kubernetes Components</h2>

<h3 id="add-kubernetes-repository">Add Kubernetes Repository</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">KUBERNETES_VERSION</span><span class="o">=</span>1.30
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/apt/keyrings
curl <span class="nt">-fsSL</span> https://pkgs.k8s.io/core:/stable:/v<span class="nv">$KUBERNETES_VERSION</span>/deb/Release.key | <span class="se">\</span>
    gpg <span class="nt">--dearmor</span> | <span class="nb">sudo tee</span> /etc/apt/keyrings/kubernetes-apt-keyring.gpg <span class="o">&gt;</span> /dev/null

<span class="nb">echo</span> <span class="s2">"deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v</span><span class="nv">$KUBERNETES_VERSION</span><span class="s2">/deb/ /"</span> | <span class="se">\</span>
    <span class="nb">sudo tee</span> /etc/apt/sources.list.d/kubernetes.list
</code></pre></div></div>

<h3 id="install-kubeadm-kubelet-and-kubectl">Install Kubeadm, Kubelet, and Kubectl</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nv">kubelet</span><span class="o">=</span>1.30.0-1.1 <span class="nv">kubectl</span><span class="o">=</span>1.30.0-1.1 <span class="nv">kubeadm</span><span class="o">=</span>1.30.0-1.1
</code></pre></div></div>

<h3 id="configure-kubelet-node-ip">Configure Kubelet Node IP</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> jq
<span class="nv">local_ip</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>ip <span class="nt">--json</span> addr show eno1 | jq <span class="nt">-r</span> <span class="s1">'.[0].addr_info[] | select(.family == "inet") | .local'</span><span class="si">)</span><span class="s2">"</span>

<span class="nb">sudo tee</span> /etc/default/kubelet <span class="o">&gt;</span> /dev/null <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
KUBELET_EXTRA_ARGS=--node-ip=</span><span class="nv">$local_ip</span><span class="sh">
</span><span class="no">EOF
</span></code></pre></div></div>

<p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">eno1</code> and <code class="language-plaintext highlighter-rouge">enp85s0</code> are network interface names that may vary depending on the system. You can check your network interfaces using:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ifconfig <span class="nt">-a</span>
</code></pre></div></div>
<p>or</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip a
</code></pre></div></div>
<p>Adjust the interface name accordingly based on the output of these commands.</p>

<h2 id="step-5-initialize-kubernetes-on-the-master-node">Step 5: Initialize Kubernetes on the Master Node</h2>

<p>Set the master node IP and hostname:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">IPADDR</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>ip <span class="nt">--json</span> addr show enp85s0 | jq <span class="nt">-r</span> <span class="s1">'.[0].addr_info[] | select(.family == "inet") | .local'</span><span class="si">)</span><span class="s2">"</span>
<span class="nv">NODENAME</span><span class="o">=</span><span class="si">$(</span><span class="nb">hostname</span> <span class="nt">-s</span><span class="si">)</span>
<span class="nv">POD_CIDR</span><span class="o">=</span><span class="s2">"192.168.0.0/16"</span>
</code></pre></div></div>

<p>Initialize Kubernetes:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm init <span class="nt">--apiserver-advertise-address</span><span class="o">=</span><span class="nv">$IPADDR</span> <span class="nt">--apiserver-cert-extra-sans</span><span class="o">=</span><span class="nv">$IPADDR</span> <span class="nt">--pod-network-cidr</span><span class="o">=</span><span class="nv">$POD_CIDR</span> <span class="nt">--node-name</span> <span class="nv">$NODENAME</span> <span class="nt">--ignore-preflight-errors</span> Swap <span class="nt">--cri-socket</span> unix:///var/run/crio/crio.sock 
</code></pre></div></div>

<p>Set up kubeconfig for the root user:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> <span class="nv">$HOME</span>/.kube
<span class="nb">cp</span> <span class="nt">-i</span> /etc/kubernetes/admin.conf <span class="nv">$HOME</span>/.kube/config
<span class="nb">chown</span> <span class="si">$(</span><span class="nb">id</span> <span class="nt">-u</span><span class="si">)</span>:<span class="si">$(</span><span class="nb">id</span> <span class="nt">-g</span><span class="si">)</span> <span class="nv">$HOME</span>/.kube/config
</code></pre></div></div>

<h2 id="step-6-deploy-network-and-monitoring-components">Step 6: Deploy Network and Monitoring Components</h2>

<h3 id="install-calico-network-plugin">Install Calico Network Plugin</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://docs.projectcalico.org/manifests/calico.yaml
</code></pre></div></div>

<h3 id="install-metrics-server">Install Metrics Server</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/techiescamp/kubeadm-scripts/main/manifests/metrics-server.yaml
</code></pre></div></div>

<h3 id="install-ingress-controller">Install Ingress Controller</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v0.49.0/deploy/static/provider/baremetal/deploy.yaml
</code></pre></div></div>

<h2 id="step-7-join-worker-nodes-to-the-cluster">Step 7: Join Worker Nodes to the Cluster</h2>

<p>Generate a join command on the master node:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubeadm token create <span class="nt">--print-join-command</span>
</code></pre></div></div>

<p>Run the output command on worker nodes to join them to the cluster.</p>

<h2 id="step-8-verify-cluster-status">Step 8: Verify Cluster Status</h2>

<h3 id="check-nodes">Check Nodes</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div>

<h3 id="check-all-pods-in-a-specific-node">Check All Pods in a Specific Node</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--all-namespaces</span> <span class="nt">--field-selector</span> spec.nodeName<span class="o">=</span>&lt;NODE_NAME&gt;
</code></pre></div></div>
<h2 id="troubleshooting-tips">Troubleshooting Tips</h2>
<ul>
  <li>Always check system logs: <code class="language-plaintext highlighter-rouge">journalctl -u kubelet</code></li>
  <li>Verify node status: <code class="language-plaintext highlighter-rouge">kubectl get nodes</code></li>
  <li>Check pod status: <code class="language-plaintext highlighter-rouge">kubectl get pods --all-namespaces</code></li>
</ul>

<h2 id="security-considerations">Security Considerations</h2>
<ul>
  <li>Regularly update Kubernetes and CRI-O</li>
  <li>Implement RBAC (Role-Based Access Control)</li>
  <li>Use network policies</li>
  <li>Regularly scan containers for vulnerabilities</li>
</ul>

<p><strong>Note</strong>: Always refer to the official Kubernetes and CRI-O documentation for the most up-to-date information and best practices.</p>]]></content><author><name></name></author><category term="tutorial" /><category term="Kubernetes" /><category term="kubeadm" /><category term="cri.io" /><category term="beginners" /><summary type="html"><![CDATA[Learn how to set up a production-ready Kubernetes cluster using Kubeadm and CRI-O. This step-by-step guide covers prerequisites, installation, configuration, and best practices for deploying and managing your cluster effectively.]]></summary></entry><entry><title type="html">Setting Up a K3s Cluster with a Hello-World Deployment</title><link href="https://menraromial.com/blog/2024/setup-k3s-cluster/" rel="alternate" type="text/html" title="Setting Up a K3s Cluster with a Hello-World Deployment" /><published>2024-07-07T14:30:00+00:00</published><updated>2024-07-07T14:30:00+00:00</updated><id>https://menraromial.com/blog/2024/setup-k3s-cluster</id><content type="html" xml:base="https://menraromial.com/blog/2024/setup-k3s-cluster/"><![CDATA[<p>This tutorial will guide you through setting up a K3s cluster with one master node and two worker nodes, deploying a simple “Hello-World” application, and utilizing various <code class="language-plaintext highlighter-rouge">kubectl</code> commands to manage and inspect Kubernetes resources.</p>

<h2 id="prerequisites">Prerequisites</h2>

<ul>
  <li>Three Linux machines (or VMs) with access to each other.</li>
  <li>Basic understanding of Linux commands and Kubernetes concepts.</li>
</ul>

<h2 id="step-1-installing-k3s">Step 1: Installing K3s</h2>

<h3 id="install-k3s-on-the-master-node">Install K3s on the Master Node</h3>

<ol>
  <li>
    <p><strong>SSH into the Master Node:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> ssh user@master-node-ip
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Install K3s:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> curl <span class="nt">-sfL</span> https://get.k3s.io |  <span class="nv">INSTALL_K3S_VERSION</span><span class="o">=</span>v1.27.12+k3s1 sh <span class="nt">-s</span> - <span class="nt">--disable</span> traefik <span class="nt">--write-kubeconfig-mode</span> 644 <span class="nt">--node-name</span> &lt;NODE_NAME&gt;
</code></pre></div>    </div>
  </li>
</ol>

<p>The command installs K3s version <code class="language-plaintext highlighter-rouge">v1.27.12+k3s1</code> on a node named <code class="language-plaintext highlighter-rouge">&lt;NODE_NAME&gt;</code>, disables the default Traefik ingress controller, and sets the permissions for the kubeconfig file to <code class="language-plaintext highlighter-rouge">644</code>.</p>

<ol>
  <li>
    <p><strong>Verify Installation:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get nodes
</code></pre></div>    </div>

    <p>You should see the master node listed.</p>
  </li>
</ol>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/97n9mbhc57emcmzbl4j9.png" alt="Image description" /></p>

<h3 id="install-k3s-on-the-worker-nodes">Install K3s on the Worker Nodes</h3>

<ol>
  <li>
    <p><strong>SSH into each Worker Node:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> ssh user@worker-node-ip
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Get the token from the master node</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nb">cat</span> /var/lib/rancher/k3s/server/node-token
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Install K3s on Worker Nodes:</strong></p>

    <p>On each worker node, run:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> curl <span class="nt">-sfL</span> https://get.k3s.io | <span class="nv">INSTALL_K3S_VERSION</span><span class="o">=</span>v1.27.12+k3s1  <span class="nv">K3S_NODE_NAME</span><span class="o">=</span>YOUR_NODE_NAME <span class="nv">K3S_URL</span><span class="o">=</span>https://master-node-ip:6443 <span class="nv">K3S_TOKEN</span><span class="o">=</span>YOUR_NODE_TOKEN sh -
</code></pre></div>    </div>

    <p>You can find the <code class="language-plaintext highlighter-rouge">YOUR_NODE_TOKEN</code> value on the master node at <code class="language-plaintext highlighter-rouge">/var/lib/rancher/k3s/server/node-token</code>.</p>
  </li>
</ol>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/0w2dhl0ynwvsr5r19e2j.png" alt="Image description" /></p>

<ol>
  <li>
    <p><strong>Verify Worker Nodes Join the Cluster:</strong></p>

    <p>On the master node, run:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get nodes
</code></pre></div>    </div>

    <p>You should see all nodes (master and workers) listed.</p>
  </li>
</ol>

<h2 id="step-2-verifying-node-status">Step 2: Verifying Node Status</h2>

<p>To check the status of the nodes:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div>

<p>You should see the master and worker nodes with the status <code class="language-plaintext highlighter-rouge">Ready</code>.</p>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/js3ik5rdbentis2q3rvv.png" alt="Image description" /></p>

<h2 id="step-3-creating-a-pod">Step 3: Creating a Pod</h2>

<p>Create a simple pod to ensure everything is working correctly.</p>

<ol>
  <li>
    <p><strong>Create a YAML file named <code class="language-plaintext highlighter-rouge">hello-world-pod.yaml</code>:</strong></p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
 <span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
 <span class="na">metadata</span><span class="pi">:</span>
 <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
 <span class="na">spec</span><span class="pi">:</span>
 <span class="na">containers</span><span class="pi">:</span>
     <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello</span>
     <span class="na">image</span><span class="pi">:</span> <span class="s">busybox</span>  <span class="c1"># Replace with your desired Docker image</span>

</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Apply the YAML file to create the pod:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl apply <span class="nt">-f</span> hello-world-pod.yaml
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Verify the Pod is Running:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get pods
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Describe the Pod:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl describe pod hello-world
</code></pre></div>    </div>
  </li>
</ol>

<h2 id="step-4-deleting-the-pod">Step 4: Deleting the Pod</h2>

<p>To delete the pod:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod hello-world
</code></pre></div></div>

<h2 id="step-5-deploying-the-application">Step 5: Deploying the Application</h2>

<h3 id="creating-a-namespace">Creating a namespace</h3>

<p>A namespace in Kubernetes is used to organize and manage resources within a cluster, providing isolation and a way to divide cluster resources among multiple users or teams.</p>

<p>The commands used in this process are:</p>

<ol>
  <li><strong>View Existing Namespaces</strong>:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespaces
</code></pre></div>    </div>
    <p>or</p>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get ns
</code></pre></div>    </div>
  </li>
  <li><strong>Create a New Namespace</strong>:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create namespace demo
</code></pre></div>    </div>
  </li>
  <li><strong>Verify the Creation of the Namespace</strong>:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespaces
</code></pre></div>    </div>
  </li>
</ol>

<p>This process ensures you have successfully created the <code class="language-plaintext highlighter-rouge">demo</code> namespace and confirmed its presence in your Kubernetes cluster.</p>

<h3 id="deployment-yaml-file">Deployment YAML File</h3>

<p>A deployment in Kubernetes is a controller that manages the creation, scaling, and updating of a set of replicated Pods, ensuring the desired state of an application is maintained.</p>

<p>Create a file named <code class="language-plaintext highlighter-rouge">hello-world-deployment.yaml</code> with the following content:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">5</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">hello-world</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-world</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">bashofmann/rancher-demo:1.0.0</span>
          <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">Always</span>
          <span class="na">resources</span><span class="pi">:</span>
            <span class="na">requests</span><span class="pi">:</span>
              <span class="na">memory</span><span class="pi">:</span> <span class="s2">"</span><span class="s">12Mi"</span>
              <span class="na">cpu</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2m"</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">8080</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
              <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
          <span class="na">env</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">COW_COLOR</span>
              <span class="na">value</span><span class="pi">:</span> <span class="s">purple</span>
          <span class="na">readinessProbe</span><span class="pi">:</span>
            <span class="na">httpGet</span><span class="pi">:</span>
              <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
              <span class="na">port</span><span class="pi">:</span> <span class="s">web</span>
          <span class="na">livenessProbe</span><span class="pi">:</span>
            <span class="na">httpGet</span><span class="pi">:</span>
              <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
              <span class="na">port</span><span class="pi">:</span> <span class="s">web</span>
</code></pre></div></div>

<h3 id="explanation-of-the-yaml-file">Explanation of the YAML File</h3>

<ul>
  <li><strong>apiVersion</strong>: Specifies the version of the Kubernetes API to use.</li>
  <li><strong>kind</strong>: Specifies the type of resource being created (Deployment).</li>
  <li><strong>metadata</strong>: Contains data that helps uniquely identify the object, including a <code class="language-plaintext highlighter-rouge">name</code> and <code class="language-plaintext highlighter-rouge">namespace</code>.</li>
  <li><strong>spec</strong>: Defines the desired state of the Deployment.
    <ul>
      <li><strong>replicas</strong>: Number of pod replicas to run.</li>
      <li><strong>selector</strong>: Defines how the Deployment finds which Pods to manage.</li>
      <li><strong>template</strong>: Defines the Pods that will be created.
        <ul>
          <li><strong>metadata</strong>: Labels for the Pods.</li>
          <li><strong>spec</strong>: Specification of the containers within the Pods.
            <ul>
              <li><strong>containers</strong>: List of containers in the Pod.
                <ul>
                  <li><strong>name</strong>: Name of the container.</li>
                  <li><strong>image</strong>: Docker image to use for the container.</li>
                  <li><strong>imagePullPolicy</strong>: Policy to use for image pulls.</li>
                  <li><strong>ports</strong>: Ports to expose from the container.</li>
                  <li><strong>env</strong>: Environment variables for the container.</li>
                  <li><strong>readinessProbe</strong>: Defines a probe to check if the container is ready.</li>
                  <li><strong>livenessProbe</strong>: Defines a probe to check if the container is alive.</li>
                </ul>
              </li>
            </ul>
          </li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<hr />
<p>This is a Go demo application used for demonstrating Kubernetes and load balancing.</p>

<p>It will create a colored icon for each replica within a ReplicaSet and indicate which one most recently served a response. Replicas which haven’t been heard from will begin to fade out, until after a configurable number of seconds they will disappear.</p>

<p>For more information on using the container image, please click <a href="https://github.com/bashofmann/rancher-demo">here</a>.</p>

<h3 id="apply-the-deployment">Apply the Deployment</h3>

<ol>
  <li>
    <p><strong>Apply the YAML file to create the Deployment:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl apply <span class="nt">-f</span> hello-world-deployment.yaml
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Verify the Deployment and Pods:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get deployments <span class="nt">-n</span> demo
 kubectl get pods <span class="nt">-n</span> demo
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Check the ReplicaSets:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get replicaset <span class="nt">-n</span> demo
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>For more information on replicas:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get pods <span class="nt">-n</span> demo <span class="nt">-o</span> wide
</code></pre></div>    </div>
    <p><code class="language-plaintext highlighter-rouge">-o wide</code>: Provides additional details about each pod, such as the node it’s running on, the IP address, the container images, and more.</p>
  </li>
  <li>
    <p><strong>See the pods metrics</strong></p>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top pods <span class="nt">-n</span> demo
</code></pre></div>    </div>
    <p>For nodes</p>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top nodes
</code></pre></div>    </div>
  </li>
</ol>

<h2 id="step-6-exposing-the-application">Step 6: Exposing the Application</h2>

<p>Create a service to expose the Deployment.</p>

<p>A service in Kubernetes provides a stable network endpoint to access a set of pods, ensuring consistent access despite pod restarts or IP changes.</p>

<h4 id="types-of-services">Types of Services:</h4>

<ul>
  <li><strong>ClusterIP</strong>: Exposes the service on an internal IP within the cluster, making it accessible only within the cluster.</li>
  <li><strong>NodePort</strong>: Exposes the service on each node’s IP at a static port, making it accessible from outside the cluster using <code class="language-plaintext highlighter-rouge">&lt;NodeIP&gt;:&lt;NodePort&gt;</code>.</li>
  <li><strong>LoadBalancer</strong>: Exposes the service externally using a cloud provider’s load balancer.</li>
  <li><strong>ExternalName</strong>: Maps the service to the contents of the <code class="language-plaintext highlighter-rouge">externalName</code> field (e.g., <code class="language-plaintext highlighter-rouge">foo.bar.example.com</code>), creating a DNS alias.</li>
</ul>

<ol>
  <li>
    <p><strong>Create a ClusterIP Service:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl expose deployment hello-world <span class="nt">--type</span><span class="o">=</span>ClusterIP <span class="nt">--name</span><span class="o">=</span>hello-world-service <span class="nt">--port</span><span class="o">=</span>8080 <span class="nt">--target-port</span><span class="o">=</span>8080 <span class="nt">-n</span> demo
</code></pre></div>    </div>

    <ul>
      <li><strong>expose deployment hello-world</strong>: Creates a service to expose the deployment named <code class="language-plaintext highlighter-rouge">hello-world</code>.</li>
      <li><strong><code class="language-plaintext highlighter-rouge">--type=ClusterIP</code></strong>: Specifies the type of service to create. <code class="language-plaintext highlighter-rouge">ClusterIP</code> means the service will be accessible only within the cluster.</li>
      <li><strong><code class="language-plaintext highlighter-rouge">--name=hello-world-service</code></strong>: Assigns the name <code class="language-plaintext highlighter-rouge">hello-world-service</code> to the newly created service.</li>
      <li><strong><code class="language-plaintext highlighter-rouge">--port=8080</code></strong>: Defines the port on which the service will be exposed.</li>
      <li><strong><code class="language-plaintext highlighter-rouge">--target-port=8080</code></strong>: Specifies the port on the pod that the service should forward traffic to.</li>
      <li><strong><code class="language-plaintext highlighter-rouge">-n demo</code></strong>: Limits the scope of the command to the <code class="language-plaintext highlighter-rouge">demo</code> namespace.</li>
    </ul>
  </li>
  <li>
    <p><strong>Verify the Service:</strong></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get svc <span class="nt">-n</span> demo
</code></pre></div>    </div>
  </li>
</ol>

<h2 id="step-7-accessing-the-application">Step 7: Accessing the Application</h2>

<p>To access the application from your local machine, use <code class="language-plaintext highlighter-rouge">kubectl port-forward</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward svc/hello-world-service 8080:8080 <span class="nt">-n</span> demo <span class="nt">--address</span> node_address
</code></pre></div></div>

<p>Open your browser and go to <code class="language-plaintext highlighter-rouge">http://node_address:8080</code> to see the application in action.</p>

<ol>
  <li><strong>Create a service of type LoadBalancer</strong></li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl expose deployment hello-world <span class="nt">--type</span><span class="o">=</span>LoadBalancer <span class="nt">--name</span><span class="o">=</span>hello-world-service-loadbalancing <span class="nt">--port</span><span class="o">=</span>8080 <span class="nt">--target-port</span><span class="o">=</span>8080 <span class="nt">-n</span> demo
</code></pre></div></div>

<p>Open your browser and go to <code class="language-plaintext highlighter-rouge">http://any_node_address:PORT</code> to see the application in action. 
<code class="language-plaintext highlighter-rouge">PORT</code> here is generated by default, see the image below.</p>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/2r1n2277m60ukt74wohs.png" alt="Image description" /></p>

<p>Application on the browser</p>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/0mah2fyxsrjsat6wdah6.png" alt="Image description" /></p>

<h2 id="step-8-inspecting-resources">Step 8: Inspecting Resources</h2>

<h3 id="describing-a-pod">Describing a Pod</h3>

<p>To get detailed information about a specific pod:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod &lt;pod-name&gt; <span class="nt">-n</span> demo
</code></pre></div></div>

<h3 id="describing-a-deployment">Describing a Deployment</h3>

<p>To get detailed information about the deployment:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe deployment hello-world <span class="nt">-n</span> demo
</code></pre></div></div>

<h3 id="viewing-pod-logs">Viewing Pod Logs</h3>

<p>To view the logs of a pod:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs <span class="nt">-f</span> &lt;pod-name&gt; <span class="nt">-n</span> demo
</code></pre></div></div>

<h2 id="step-9-cleaning-up">Step 9: Cleaning Up</h2>

<p>To delete all the resources created:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete namespace demo
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>

<p>You’ve successfully set up a K3s cluster, deployed a simple application, and used various <code class="language-plaintext highlighter-rouge">kubectl</code> commands to manage and inspect Kubernetes resources. This foundation will help you explore Kubernetes further and leverage its capabilities.</p>

<h1 id="bonus">Bonus</h1>
<h2 id="scaling-and-deploying-a-horizontal-pod-autoscaler-in-kubernetes">Scaling and Deploying a Horizontal Pod Autoscaler in Kubernetes</h2>

<h3 id="step-1-scaling-updown-manually">Step 1: Scaling Up/Down Manually</h3>

<p>You can manually scale the number of replicas in a deployment using the <code class="language-plaintext highlighter-rouge">kubectl scale</code> command.</p>

<h4 id="scale-up">Scale Up</h4>

<p>To scale the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment to 10 replicas:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment hello-world <span class="nt">--replicas</span><span class="o">=</span>10 <span class="nt">-n</span> demo
</code></pre></div></div>

<h4 id="scale-down">Scale Down</h4>

<p>To scale the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment back down to 3 replicas:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment hello-world <span class="nt">--replicas</span><span class="o">=</span>3 <span class="nt">-n</span> demo
</code></pre></div></div>

<h3 id="step-2-deploying-a-horizontal-pod-autoscaler-hpa">Step 2: Deploying a Horizontal Pod Autoscaler (HPA)</h3>

<p>The Horizontal Pod Autoscaler automatically adjusts the number of pod replicas in a deployment based on observed CPU utilization (or other select metrics).</p>

<h4 id="prerequisites-1">Prerequisites</h4>

<p>Ensure that the Metrics Server is deployed in your cluster. The Metrics Server collects resource metrics from the Kubernetes nodes and pods and is required for HPA to function.</p>

<p>To install the Metrics Server:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
</code></pre></div></div>

<h4 id="deploy-hpa-for-the-hello-world-deployment">Deploy HPA for the <code class="language-plaintext highlighter-rouge">hello-world</code> Deployment</h4>

<ol>
  <li>
    <p><strong>Create the HPA</strong></p>

    <p>Create the file <code class="language-plaintext highlighter-rouge">hello-world-hpa.yaml</code></p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">autoscaling/v2</span>
 <span class="na">kind</span><span class="pi">:</span> <span class="s">HorizontalPodAutoscaler</span>
 <span class="na">metadata</span><span class="pi">:</span>
   <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world-autoscaler</span>
   <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
 <span class="na">spec</span><span class="pi">:</span>
   <span class="na">scaleTargetRef</span><span class="pi">:</span>
     <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
     <span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
     <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
   <span class="na">minReplicas</span><span class="pi">:</span> <span class="m">5</span>
   <span class="na">maxReplicas</span><span class="pi">:</span> <span class="m">20</span>
   <span class="na">metrics</span><span class="pi">:</span>
   <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Resource</span>
     <span class="na">resource</span><span class="pi">:</span>
       <span class="na">name</span><span class="pi">:</span> <span class="s">cpu</span>
       <span class="na">target</span><span class="pi">:</span>
         <span class="na">type</span><span class="pi">:</span> <span class="s">Utilization</span>
         <span class="na">averageUtilization</span><span class="pi">:</span> <span class="m">70</span>
   <span class="na">behavior</span><span class="pi">:</span>
     <span class="na">scaleUp</span><span class="pi">:</span>
       <span class="na">stabilizationWindowSeconds</span><span class="pi">:</span> <span class="m">10</span>
       <span class="na">policies</span><span class="pi">:</span>
       <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Pods</span>
         <span class="na">value</span><span class="pi">:</span> <span class="m">1</span>
         <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>
     <span class="na">scaleDown</span><span class="pi">:</span>
       <span class="na">stabilizationWindowSeconds</span><span class="pi">:</span> <span class="m">10</span>
       <span class="na">policies</span><span class="pi">:</span>
       <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Pods</span>
         <span class="na">value</span><span class="pi">:</span> <span class="m">1</span>
         <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>


</code></pre></div>    </div>
    <p>Apply</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> hello-world-hpa.yaml
</code></pre></div>    </div>

    <p>This command sets up an HPA for the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment that:</p>
    <ul>
      <li>Targets 70% CPU utilization.</li>
      <li>Scales the number of pod replicas between 1 and 10 based on the observed CPU utilization.</li>
    </ul>
  </li>
  <li>
    <p><strong>Verify the HPA</strong></p>

    <p>To check the status of the HPA:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get hpa <span class="nt">-n</span> demo
</code></pre></div>    </div>

    <p>The output should show something like this:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME          REFERENCE                TARGETS   MINPODS   MAXPODS   REPLICAS   AGE
hello-world   Deployment/hello-world   10%/50%   1         10        3          5m
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="step-3-testing-the-hpa">Step 3: Testing the HPA</h3>

<p>To test the HPA, you can simulate a load on the <code class="language-plaintext highlighter-rouge">hello-world</code> pods to observe the scaling behavior. Here’s how you can do it:</p>

<ol>
  <li>
    <p><strong>Run a Load Generator Pod</strong></p>

    <p>Create a pod that continuously sends requests to the <code class="language-plaintext highlighter-rouge">hello-world</code> service. Create a file named <code class="language-plaintext highlighter-rouge">load-generator.yaml</code> with the following content:</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">load-generator</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">busybox</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">busybox</span>
    <span class="na">args</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">/bin/sh</span>
    <span class="pi">-</span> <span class="s">-c</span>
    <span class="pi">-</span> <span class="pi">|</span>
      <span class="s">while true; do wget -q -O- http://NODE_IP_ADDRESS:8080; done</span>
</code></pre></div>    </div>

    <p>Apply this YAML file:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> load-generator.yaml
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Monitor the HPA</strong></p>

    <p>Keep an eye on the HPA status to see how it adjusts the number of replicas:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>watch <span class="nt">-n3</span> kubectl get hpa,pod <span class="nt">-n</span> demo <span class="nt">-o</span> wide
</code></pre></div>    </div>

    <p>You should see the <code class="language-plaintext highlighter-rouge">TARGETS</code> column indicating higher CPU utilization, and the <code class="language-plaintext highlighter-rouge">REPLICAS</code> column increasing as the HPA scales up the deployment.</p>
  </li>
  <li>
    <p><strong>Stop the Load Generator</strong></p>

    <p>Once you’ve observed the scaling behavior, delete the load generator pod:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod load-generator <span class="nt">-n</span> demo
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="summary">Summary</h3>

<ul>
  <li><strong>Manual Scaling</strong>: Use <code class="language-plaintext highlighter-rouge">kubectl scale</code> to manually adjust the number of replicas.</li>
  <li><strong>Horizontal Pod Autoscaler (HPA)</strong>: Automatically adjusts the number of replicas based on CPU utilization (or other metrics).</li>
  <li><strong>Metrics Server</strong>: Required for HPA to function, it collects resource metrics.</li>
  <li><strong>Testing HPA</strong>: Use a load generator to simulate traffic and observe the HPA scaling behavior.</li>
</ul>

<p>This guide helps you understand and implement both manual and automatic scaling in Kubernetes, ensuring your applications can handle varying loads efficiently.</p>

<p><strong>Congratulations !!!</strong></p>

<blockquote>
  <p>Why did the Kubernetes developer go broke?
Because he lost all his containers! 🤣</p>
</blockquote>

<hr />]]></content><author><name></name></author><category term="tutorial" /><category term="Kubernetes" /><category term="k3s" /><category term="beginners" /><summary type="html"><![CDATA[A comprehensive guide to setting up a lightweight Kubernetes cluster using K3s, deploying a "Hello-World" application, and exploring Kubernetes concepts like namespaces, deployments, services, and scaling with Horizontal Pod Autoscalers.]]></summary></entry></feed>