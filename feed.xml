<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://menraromial.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://menraromial.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-20T13:38:21+00:00</updated><id>https://menraromial.github.io/feed.xml</id><title type="html">blank</title><subtitle>A personal website and blog by MENRA W. Romial, showcasing expertise in cloud computing, edge technologies, IoT, and innovation. Explore research insights, technical articles, and projects aimed at advancing technology and innovation. </subtitle><entry><title type="html">Master the CI/CD Pipeline Locally</title><link href="https://menraromial.github.io/blog/2025/master-ci-ci-pipeline-locally/" rel="alternate" type="text/html" title="Master the CI/CD Pipeline Locally"/><published>2025-08-20T10:30:00+00:00</published><updated>2025-08-20T10:30:00+00:00</updated><id>https://menraromial.github.io/blog/2025/master-ci-ci-pipeline-locally</id><content type="html" xml:base="https://menraromial.github.io/blog/2025/master-ci-ci-pipeline-locally/"><![CDATA[<p>Welcome to this comprehensive course on DevOps culture and tooling. The goal is to guide you, step-by-step, from theory to practice, empowering you to build, secure, and automate a full Continuous Integration and Continuous Deployment (CI/CD) pipeline.</p> <p><strong>Course Philosophy:</strong> Everything we learn will be put into practice in a <strong>100% local lab environment</strong>, running on your own machine using Docker. You will not need any cloud accounts or paid services. Every concept is illustrated with hands-on workshops and concrete examples.</p> <h2 id="course-navigation"><strong>Course Navigation</strong></h2> <ul> <li><a href="#module-00-introduction-to-devops--lab-setup">Module 00: Introduction to DevOps &amp; Lab Setup</a></li> <li><a href="#module-01-source-code-management-with-gitea">Module 01: Source Code Management with Gitea</a></li> <li><a href="#module-02-containerization-with-docker">Module 02: Containerization with Docker</a></li> <li><a href="#module-03-continuous-integration-ci-with-jenkins">Module 03: Continuous Integration (CI) with Jenkins</a></li> <li><a href="#module-04-orchestration-with-kubernetes-via-kind">Module 04: Orchestration with Kubernetes (via Kind)</a></li> <li><a href="#module-05-continuous-deployment-cd-to-kubernetes">Module 05: Continuous Deployment (CD) to Kubernetes</a></li> <li><a href="#module-06-reverse-proxy--load-balancing-nginx-ingress">Module 06: Reverse Proxy &amp; Load Balancing (Nginx Ingress)</a></li> <li><a href="#module-07-infrastructure-as-code-iac-with-terraform">Module 07: Infrastructure as Code (IaC) with Terraform</a></li> <li><a href="#module-08-configuration-management-with-ansible">Module 08: Configuration Management with Ansible</a></li> <li><a href="#module-09-integrating-security-devsecops">Module 09: Integrating Security (DevSecOps)</a></li> <li><a href="#module-10-monitoring--observability">Module 10: Monitoring &amp; Observability</a></li> <li><a href="#module-11-final-project---the-complete-a-to-z-pipeline">Module 11: Final Project - The Complete A-to-Z Pipeline</a></li> <li><a href="#module-12-conclusion--next-steps">Module 12: Conclusion &amp; Next Steps</a></li> </ul> <h2 id="module-00-introduction-to-devops--lab-setup"><strong>Module 00: Introduction to DevOps &amp; Lab Setup</strong></h2> <h3 id="what-is-devops">What is DevOps?</h3> <p>DevOps is not a tool, but a <strong>culture</strong> and a <strong>philosophy</strong>. It’s a portmanteau of “Development” (Dev) and “Operations” (Ops). The goal is to break down the silos between development teams, who want to deliver new features quickly, and operations teams, who want to ensure production stability.</p> <p>The pillars of DevOps are based on the <strong>C.A.L.M.S</strong> model:</p> <ul> <li><strong>C</strong>ulture: Fostering collaboration, sharing, and trust.</li> <li><strong>A</strong>utomation: Automating everything that is repetitive (tests, builds, deployments).</li> <li><strong>L</strong>ean: Optimizing processes, eliminating waste, and delivering value quickly.</li> <li><strong>M</strong>easurement: Collecting metrics to understand and improve the system.</li> <li><strong>S</strong>haring: Sharing knowledge, tools, and responsibilities.</li> </ul> <h3 id="our-local-lab-architecture">Our Local Lab Architecture</h3> <p>We will use <code class="language-plaintext highlighter-rouge">docker-compose</code> to orchestrate our lab services. This will allow us to easily start and stop all the necessary tools, which will communicate over a shared Docker network.</p> <pre><code class="language-mermaid">graph TD
    subgraph "Your Host Machine"
        subgraph "Docker Network 'devops-net'"
            Gitea[Gitea&lt;br&gt;git.lab.local:3000]
            Jenkins[Jenkins&lt;br&gt;jenkins.lab.local:8080]
            Registry[Docker Registry&lt;br&gt;registry.lab.local:5000]
        end
        subgraph "Kubernetes Cluster (Kind)"
            K8sControl[Control Plane]
            K8sWorker[Worker Node]
            App[Deployed Application]
        end
        Jenkins -- Pull/Clone --&gt; Gitea
        Jenkins -- Build &amp; Push --&gt; Registry
        Jenkins -- Deploy --&gt; K8sControl
        K8sWorker -- Pull Image --&gt; Registry
        K8sControl -- Manages --&gt; K8sWorker
        K8sWorker -- Runs --&gt; App
    end
    User[User] -- Access via Browser --&gt; Services
</code></pre> <h3 id="prerequisites">Prerequisites</h3> <ul> <li><strong>Docker</strong> and <strong>Docker Compose</strong> installed.</li> <li>A text editor (e.g., VS Code).</li> <li>A terminal.</li> <li>Basic knowledge of the Linux command line.</li> <li><strong>Kind</strong> and <strong>kubectl</strong> installed for the Kubernetes module.</li> </ul> <h3 id="workshop-launching-the-lab">Workshop: Launching the Lab</h3> <ol> <li>Create a folder for our project, e.g., <code class="language-plaintext highlighter-rouge">devops-course-lab</code>.</li> <li> <p>Inside, create a <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> file:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3.8'</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">gitea</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">gitea/gitea:latest</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">gitea</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">USER_UID=1000</span>
      <span class="pi">-</span> <span class="s">USER_GID=1000</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./gitea:/data</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3000:3000"</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">2222:22"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

  <span class="na">jenkins</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">jenkins/jenkins:lts-jdk11</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">jenkins</span>
    <span class="na">privileged</span><span class="pi">:</span> <span class="kc">true</span> <span class="c1"># Required for Docker-in-Docker</span>
    <span class="na">user</span><span class="pi">:</span> <span class="s">root</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">50000:50000"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./jenkins_home:/var/jenkins_home</span>
      <span class="pi">-</span> <span class="s">/var/run/docker.sock:/var/run/docker.sock</span> <span class="c1"># Mount the Docker socket</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

  <span class="na">registry</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">registry:2</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">registry</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">5000:5000"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./registry_data:/var/lib/registry</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">devops-net</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">devops-net</span>
    <span class="na">driver</span><span class="pi">:</span> <span class="s">bridge</span>
</code></pre></div> </div> </li> <li>Launch the lab: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker-compose up <span class="nt">-d</span>
</code></pre></div> </div> </li> <li><strong>Docker Configuration for Local Registry (VERY IMPORTANT)</strong>: Our registry is insecure (HTTP). We need to tell Docker to trust it. Edit (or create) the file <code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code> (on Linux/Mac) and add: <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"insecure-registries"</span><span class="w"> </span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"localhost:5000"</span><span class="p">,</span><span class="w"> </span><span class="s2">"registry:5000"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div> </div> <blockquote> <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">registry:5000</code> is necessary so that containers <em>inside</em> the Docker network (like Jenkins or Kubernetes/Kind) can access the registry by its service name.</p> </blockquote> <p>Then restart the Docker service: <code class="language-plaintext highlighter-rouge">sudo systemctl restart docker</code>.</p> </li> </ol> <h3 id="sources--further-reading">Sources &amp; Further Reading</h3> <ul> <li><strong>Book:</strong> <em>The Phoenix Project</em> by Gene Kim, Kevin Behr, and George Spafford.</li> <li><strong>Documentation:</strong> <a href="https://docs.docker.com/compose/">Docker Compose Overview</a></li> </ul> <hr/> <h2 id="module-01-source-code-management-with-gitea"><strong>Module 01: Source Code Management with Gitea</strong></h2> <p>Every DevOps project starts with code. A version control system like Git is essential. Gitea is a lightweight alternative to GitLab or GitHub, perfect for our lab.</p> <h3 id="key-concepts">Key Concepts</h3> <ul> <li><strong>Git:</strong> A distributed version control system.</li> <li><strong>Repository:</strong> A folder containing your project and its Git history.</li> <li><strong>Commit / Branch / Merge:</strong> The basic Git operations for saving, isolating, and combining work.</li> <li><strong>Webhook:</strong> A mechanism that allows Gitea to notify another service (Jenkins) when an event occurs (e.g., a <code class="language-plaintext highlighter-rouge">push</code>).</li> </ul> <h3 id="workshop-configure-gitea-and-create-our-project">Workshop: Configure Gitea and Create Our Project</h3> <ol> <li><strong>Access and Configuration:</strong> <ul> <li>Open your browser to <code class="language-plaintext highlighter-rouge">http://localhost:3000</code>.</li> <li>Follow the installation wizard. <ul> <li>Database Type: <code class="language-plaintext highlighter-rouge">SQLite3</code>.</li> <li><strong>Gitea Base URL:</strong> <code class="language-plaintext highlighter-rouge">http://gitea:3000</code> (important for internal Docker communication).</li> <li>Create your admin account.</li> </ul> </li> </ul> </li> <li><strong>Create a New Repository:</strong> <ul> <li>Log in, click the <code class="language-plaintext highlighter-rouge">+</code> icon -&gt; “New Repository”.</li> <li>Repository Name: <code class="language-plaintext highlighter-rouge">hello-devops</code>.</li> <li>Click “Create Repository”.</li> </ul> </li> <li><strong>Create Our Base Application:</strong> <ul> <li>In a new folder on your machine, create a file named <code class="language-plaintext highlighter-rouge">app.py</code>: <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">flask</span> <span class="kn">import</span> <span class="n">Flask</span>
<span class="kn">import</span> <span class="n">os</span>

<span class="n">app</span> <span class="o">=</span> <span class="nc">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="c1"># An environment variable to demonstrate configuration
</span>    <span class="n">version</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">APP_VERSION</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">v1.0</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Hello, DevOps World! This is version: </span><span class="si">{</span><span class="n">version</span><span class="si">}</span><span class="sh">"</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">'</span><span class="s">__main__</span><span class="sh">'</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="sh">'</span><span class="s">0.0.0.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">5000</span><span class="p">)</span>
</code></pre></div> </div> </li> </ul> </li> <li><strong>Push the Code to Gitea:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Replace &lt;your_user&gt; with your Gitea username</span>
git init
git add app.py
git commit <span class="nt">-m</span> <span class="s2">"Initial commit"</span>
git branch <span class="nt">-M</span> main
git remote add origin http://localhost:3000/&lt;your_user&gt;/hello-devops.git
git push <span class="nt">-u</span> origin main
</code></pre></div> </div> </li> </ol> <h3 id="sources--further-reading-1">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://docs.gitea.io/">Gitea Documentation</a></li> <li><strong>Tutorial:</strong> <a href="https://git-scm.com/book/en/v2">Pro Git Book</a></li> </ul> <hr/> <h2 id="module-02-containerization-with-docker"><strong>Module 02: Containerization with Docker</strong></h2> <p>Docker allows us to “package” an application and its dependencies into a <strong>container</strong>. This ensures the application will run the same way everywhere.</p> <h3 id="key-concepts-1">Key Concepts</h3> <ul> <li><strong>Docker Image:</strong> A read-only template (the “recipe”).</li> <li><strong>Docker Container:</strong> A runnable instance of an image (the “prepared dish”).</li> <li><strong>Dockerfile:</strong> A text file containing instructions to build an image.</li> <li><strong>Docker Registry:</strong> An image storage service (like our local registry).</li> </ul> <pre><code class="language-mermaid">graph TD
    A[Dockerfile] -- docker build --&gt; B(Docker Image);
    B -- docker run --&gt; C{Container};
    B -- docker push --&gt; D[Docker Registry];
    D -- docker pull --&gt; B;
</code></pre> <h3 id="workshop-dockerizing-our-application">Workshop: Dockerizing Our Application</h3> <ol> <li> <p><strong>Create the <code class="language-plaintext highlighter-rouge">Dockerfile</code>:</strong> At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create this file:</p> <div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use a lightweight base image</span>
<span class="k">FROM</span><span class="s"> python:3.9-slim</span>

<span class="c"># Set the working directory</span>
<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="c"># Install dependencies</span>
<span class="k">COPY</span><span class="s"> requirements.txt .</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="c"># Copy the application code</span>
<span class="k">COPY</span><span class="s"> app.py .</span>

<span class="c"># Expose the application port</span>
<span class="k">EXPOSE</span><span class="s"> 5000</span>

<span class="c"># Command to start the application</span>
<span class="k">CMD</span><span class="s"> ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]</span>
</code></pre></div> </div> </li> <li><strong>Create <code class="language-plaintext highlighter-rouge">requirements.txt</code>:</strong> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flask==2.1.2
gunicorn==20.1.0
</code></pre></div> </div> <blockquote> <p><strong>Note:</strong> We use <code class="language-plaintext highlighter-rouge">gunicorn</code>, a robust WSGI server, which is more suitable for “production” than Flask’s built-in development server.</p> </blockquote> </li> <li><strong>Build and Test the Image:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Build the image</span>
docker build <span class="nt">-t</span> localhost:5000/hello-devops:1.0 <span class="nb">.</span>

<span class="c"># Run a container to test it</span>
docker run <span class="nt">-d</span> <span class="nt">--name</span> test-app <span class="nt">-p</span> 8001:5000 <span class="nt">-e</span> <span class="nv">APP_VERSION</span><span class="o">=</span>v1.0-test localhost:5000/hello-devops:1.0

<span class="c"># Check your browser at http://localhost:8001</span>
<span class="c"># Stop and remove the test container</span>
docker stop test-app <span class="o">&amp;&amp;</span> docker <span class="nb">rm </span>test-app
</code></pre></div> </div> </li> <li><strong>Push the Image to Our Local Registry:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push localhost:5000/hello-devops:1.0
</code></pre></div> </div> </li> <li><strong>Push the New Files to Gitea:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add Dockerfile requirements.txt
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Dockerfile for the application"</span>
git push origin main
</code></pre></div> </div> </li> </ol> <h3 id="sources--further-reading-2">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://docs.docker.com/get-started/">Docker Get Started</a></li> <li><strong>Best Practices:</strong> <a href="https://docs.docker.com/develop/develop-images/dockerfile_best-practices/">Best practices for writing Dockerfiles</a></li> </ul> <hr/> <h2 id="module-03-continuous-integration-ci-with-jenkins"><strong>Module 03: Continuous Integration (CI) with Jenkins</strong></h2> <p>Continuous Integration (CI) automates the building and testing of code with every change. Jenkins is the perfect tool to orchestrate this process.</p> <h3 id="key-concepts-2">Key Concepts</h3> <ul> <li><strong>Pipeline:</strong> A set of steps (build, test, push) executed by Jenkins.</li> <li><strong>Jenkinsfile:</strong> The definition of the pipeline as code, versioned with the application.</li> <li><strong>Agent:</strong> A machine (or container) where Jenkins executes tasks.</li> </ul> <h3 id="workshop-create-a-ci-pipeline-with-jenkins">Workshop: Create a CI Pipeline with Jenkins</h3> <ol> <li><strong>Initial Jenkins Setup:</strong> <ul> <li>Access Jenkins: <code class="language-plaintext highlighter-rouge">http://localhost:8080</code>.</li> <li>Get the initial admin password: <code class="language-plaintext highlighter-rouge">docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword</code>.</li> <li>Install suggested plugins and create your user.</li> <li><strong>Install the <code class="language-plaintext highlighter-rouge">Docker Pipeline</code> and <code class="language-plaintext highlighter-rouge">Gitea</code> plugins</strong> via “Manage Jenkins” &gt; “Manage Plugins”.</li> </ul> </li> <li> <p><strong>Create the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</strong> At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>.</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>

    <span class="n">environment</span> <span class="o">{</span>
        <span class="n">REGISTRY_URL</span> <span class="o">=</span> <span class="s1">'registry:5000'</span> <span class="c1">// Jenkins communicates via Docker service name</span>
        <span class="n">IMAGE_NAME</span> <span class="o">=</span> <span class="s2">"hello-devops"</span>
        <span class="n">IMAGE_TAG</span> <span class="o">=</span> <span class="s2">"build-${env.BUILD_NUMBER}"</span> <span class="c1">// Use build number for a unique tag</span>
    <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">checkout</span> <span class="n">scm</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build Docker Image'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="n">echo</span> <span class="s2">"Building image: ${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span>
                    <span class="n">docker</span><span class="o">.</span><span class="na">build</span><span class="o">(</span><span class="s2">"${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span><span class="o">,</span> <span class="s2">"."</span><span class="o">)</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Push to Local Registry'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="n">echo</span> <span class="s2">"Pushing image to local registry"</span>
                    <span class="c1">// No credentials needed since our registry is configured as "insecure"</span>
                    <span class="n">docker</span><span class="o">.</span><span class="na">push</span><span class="o">(</span><span class="s2">"${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span><span class="o">)</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="n">post</span> <span class="o">{</span>
        <span class="n">always</span> <span class="o">{</span>
            <span class="c1">// Clean up the image on the Jenkins agent to save space</span>
            <span class="n">echo</span> <span class="s2">"Cleaning up local Docker image..."</span>
            <span class="n">sh</span> <span class="s2">"docker rmi ${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG} || true"</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div> </div> </li> <li><strong>Push the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to Gitea:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add Jenkinsfile
git commit <span class="nt">-m</span> <span class="s2">"ci: Add Jenkinsfile for CI pipeline"</span>
git push origin main
</code></pre></div> </div> </li> <li><strong>Create the Pipeline in Jenkins:</strong> <ul> <li>“New Item” &gt; Name: <code class="language-plaintext highlighter-rouge">hello-devops-pipeline</code> &gt; Type: <code class="language-plaintext highlighter-rouge">Pipeline</code> &gt; OK.</li> <li>In the “Pipeline” section, choose “Pipeline script from SCM”.</li> <li>SCM: <code class="language-plaintext highlighter-rouge">Git</code>.</li> <li>Repository URL: <code class="language-plaintext highlighter-rouge">http://gitea:3000/&lt;your_user&gt;/hello-devops.git</code>.</li> <li>Script Path: <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>.</li> <li>Save.</li> </ul> </li> <li><strong>Run and Automate:</strong> <ul> <li>Click “Build Now” to test it.</li> <li><strong>Configure the Webhook in Gitea:</strong> <ul> <li>Repository Settings &gt; Webhooks &gt; Add Webhook &gt; Gitea.</li> <li>Target URL: <code class="language-plaintext highlighter-rouge">http://jenkins:8080/gitea-webhook/post</code>.</li> <li>Add the webhook.</li> </ul> </li> <li>Now, every <code class="language-plaintext highlighter-rouge">git push</code> will automatically trigger a build!</li> </ul> </li> </ol> <h3 id="sources--further-reading-3">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://www.jenkins.io/doc/book/pipeline/">Jenkins User Handbook - Pipeline</a></li> </ul> <hr/> <h2 id="module-04-orchestration-with-kubernetes-via-kind"><strong>Module 04: Orchestration with Kubernetes (via Kind)</strong></h2> <p>Kubernetes (K8s) is a container orchestration system. It automates the deployment, scaling, and management of containerized applications. <strong>Kind (Kubernetes in Docker)</strong> lets us create a local K8s cluster using Docker containers as “nodes”.</p> <h3 id="key-concepts-3">Key Concepts</h3> <ul> <li><strong>Cluster:</strong> A set of machines (nodes) that run containerized applications.</li> <li><strong>Node:</strong> A worker machine (VM or physical) in a cluster.</li> <li><strong>Pod:</strong> The smallest deployable unit in K8s. A pod holds one or more containers.</li> <li><strong>Deployment:</strong> An object that manages a set of replicated pods, ensuring a certain number are always running.</li> <li><strong>Service:</strong> An abstraction that exposes a set of pods as a network service with a stable IP address and port.</li> <li><strong>kubectl:</strong> The command-line tool for interacting with a K8s cluster.</li> </ul> <h3 id="workshop-create-a-local-cluster-and-deploy-manually">Workshop: Create a Local Cluster and Deploy Manually</h3> <ol> <li><strong>Prerequisites:</strong> Ensure you have installed <code class="language-plaintext highlighter-rouge">kind</code> and <code class="language-plaintext highlighter-rouge">kubectl</code>. <ul> <li><a href="https://kind.sigs.k8s.io/docs/user/quick-start/#installation">Install Kind</a></li> <li><a href="https://kubernetes.io/docs/tasks/tools/install-kubectl/">Install kubectl</a></li> </ul> </li> <li> <p><strong>Create a Kind Configuration File:</strong> Create a file named <code class="language-plaintext highlighter-rouge">kind-config.yaml</code>. This configuration is crucial for allowing the Kind cluster to communicate with our local Docker registry.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">kind</span><span class="pi">:</span> <span class="s">Cluster</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">kind.x-k8s.io/v1alpha4</span>
<span class="c1"># Allows the Kind cluster to connect to our lab's network</span>
<span class="na">containerdConfigPatches</span><span class="pi">:</span>
<span class="pi">-</span> <span class="pi">|-</span>
  <span class="s">[plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry:5000"]</span>
    <span class="s">endpoint = ["http://registry:5000"]</span>
<span class="na">nodes</span><span class="pi">:</span>
<span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">control-plane</span>
<span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">worker</span>
</code></pre></div> </div> </li> <li><strong>Launch the Kind Cluster:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create the cluster</span>
kind create cluster <span class="nt">--config</span> kind-config.yaml
    
<span class="c"># Connect the cluster to our lab's network</span>
docker network connect devops-net kind-control-plane
docker network connect devops-net kind-worker

<span class="c"># Verify that the nodes are ready</span>
kubectl get nodes
</code></pre></div> </div> </li> <li> <p><strong>Create Kubernetes Deployment Manifests:</strong> In your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a <code class="language-plaintext highlighter-rouge">k8s</code> folder. Inside, create <code class="language-plaintext highlighter-rouge">deployment.yaml</code>.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/deployment.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span> <span class="c1"># We want 2 instances of our app</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-container</span>
        <span class="c1"># IMPORTANT: We use the internal registry URL</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s">registry:5000/hello-devops:1.0</span> 
        <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">5000</span>
        <span class="na">env</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">APP_VERSION</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">v1.0-k8s"</span>
</code></pre></div> </div> </li> <li> <p><strong>Create a Service to Expose the Deployment:</strong> Create <code class="language-plaintext highlighter-rouge">service.yaml</code> in the <code class="language-plaintext highlighter-rouge">k8s</code> folder.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/service.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">NodePort</span> <span class="c1"># Exposes the service on a port of each node</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span> <span class="c1"># Targets pods with this label</span>
  <span class="na">ports</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">80</span> <span class="c1"># Port of the service inside the cluster</span>
    <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span> <span class="c1"># Port of the containers</span>
    <span class="na">nodePort</span><span class="pi">:</span> <span class="m">30007</span> <span class="c1"># External port to access the service</span>
</code></pre></div> </div> </li> <li><strong>Deploy to the Cluster:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Apply the manifests</span>
kubectl apply <span class="nt">-f</span> k8s/deployment.yaml
kubectl apply <span class="nt">-f</span> k8s/service.yaml

<span class="c"># Check the deployment</span>
kubectl get deployments
kubectl get pods
kubectl get services

<span class="c"># Access the application</span>
<span class="c"># The service is exposed on port 30007 of each node.</span>
<span class="c"># The node's IP is localhost since Kind runs on our machine.</span>
curl http://localhost:30007 
</code></pre></div> </div> <p>You should see <code class="language-plaintext highlighter-rouge">Hello, DevOps World! This is version: v1.0-k8s</code>.</p> </li> <li><strong>Push the Manifests to Gitea:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add k8s/
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Kubernetes manifests"</span>
git push origin main
</code></pre></div> </div> </li> </ol> <h3 id="sources--further-reading-4">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://kubernetes.io/docs/home/">Kubernetes Documentation</a></li> <li><strong>Documentation:</strong> <a href="https://kind.sigs.k8s.io/docs/user/quick-start/">Kind Quick Start</a></li> </ul> <hr/> <h2 id="module-05-continuous-deployment-cd-to-kubernetes"><strong>Module 05: Continuous Deployment (CD) to Kubernetes</strong></h2> <p>Now that our CI pipeline builds and pushes the image, the next step is to automatically deploy this new image to our Kubernetes cluster. This is Continuous Deployment (CD).</p> <h3 id="key-concepts-4">Key Concepts</h3> <ul> <li><strong>Continuous Deployment:</strong> The practice of automating the deployment of new application versions to production (or in our case, our local cluster).</li> <li><strong>Kubernetes Credentials:</strong> Jenkins needs permission to talk to our K8s cluster. We will use the <code class="language-plaintext highlighter-rouge">kubeconfig</code> file.</li> </ul> <h3 id="workshop-extend-the-jenkins-pipeline-for-deployment">Workshop: Extend the Jenkins Pipeline for Deployment</h3> <ol> <li><strong>Prepare Credentials for Jenkins:</strong> <ul> <li>The <code class="language-plaintext highlighter-rouge">kubeconfig</code> file contains the connection information for your cluster. It’s usually located at <code class="language-plaintext highlighter-rouge">~/.kube/config</code>.</li> <li><strong>In Jenkins:</strong> Go to “Manage Jenkins” &gt; “Manage Credentials”.</li> <li>“Add Credentials” &gt; Kind: <code class="language-plaintext highlighter-rouge">Secret file</code>.</li> <li>Upload your <code class="language-plaintext highlighter-rouge">~/.kube/config</code> file.</li> <li>ID: <code class="language-plaintext highlighter-rouge">kubeconfig-kind</code>.</li> <li>Save.</li> </ul> </li> <li> <p><strong>Install <code class="language-plaintext highlighter-rouge">kubectl</code> on the Jenkins Agent:</strong> Our base Jenkins container doesn’t have <code class="language-plaintext highlighter-rouge">kubectl</code>. The easiest way is to install it at the start of the pipeline.</p> </li> <li> <p><strong>Update the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</strong> Modify your <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to add a new deployment stage.</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>

    <span class="n">environment</span> <span class="o">{</span>
        <span class="n">REGISTRY_URL</span> <span class="o">=</span> <span class="s1">'registry:5000'</span>
        <span class="n">IMAGE_NAME</span> <span class="o">=</span> <span class="s2">"hello-devops"</span>
        <span class="n">IMAGE_TAG</span> <span class="o">=</span> <span class="s2">"build-${env.BUILD_NUMBER}"</span>
        <span class="n">KUBECONFIG_CREDENTIAL_ID</span> <span class="o">=</span> <span class="s1">'kubeconfig-kind'</span>
        <span class="n">K8S_DEPLOYMENT_NAME</span> <span class="o">=</span> <span class="s1">'hello-devops-deployment'</span>
    <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build &amp; Push Docker Image'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="kt">def</span> <span class="n">customImage</span> <span class="o">=</span> <span class="n">docker</span><span class="o">.</span><span class="na">build</span><span class="o">(</span><span class="s2">"${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}"</span><span class="o">,</span> <span class="s2">"."</span><span class="o">)</span>
                    <span class="n">customImage</span><span class="o">.</span><span class="na">push</span><span class="o">()</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Install Kubectl'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">sh</span> <span class="s1">'''
                if ! command -v kubectl &amp;&gt; /dev/null
                then
                    echo "Kubectl not found. Installing..."
                    curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
                    install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
                fi
                kubectl version --client
                '''</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="c1">// Use the kubeconfig credentials</span>
                <span class="n">withCredentials</span><span class="o">([</span><span class="n">file</span><span class="o">(</span><span class="nl">credentialsId:</span> <span class="n">KUBECONFIG_CREDENTIAL_ID</span><span class="o">,</span> <span class="nl">variable:</span> <span class="s1">'KUBECONFIG_FILE'</span><span class="o">)])</span> <span class="o">{</span>
                    <span class="n">script</span> <span class="o">{</span>
                        <span class="c1">// Set the environment variable so kubectl uses this file</span>
                        <span class="n">sh</span> <span class="s2">"export KUBECONFIG=${KUBECONFIG_FILE}"</span>

                        <span class="c1">// Update the deployment's image with the new version</span>
                        <span class="c1">// Add --record to keep track of the command</span>
                        <span class="n">sh</span> <span class="s2">"kubectl set image deployment/${K8S_DEPLOYMENT_NAME} hello-devops-container=${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG} --record"</span>

                        <span class="c1">// Wait for the deployment to complete</span>
                        <span class="n">sh</span> <span class="s2">"kubectl rollout status deployment/${K8S_DEPLOYMENT_NAME}"</span>

                        <span class="n">echo</span> <span class="s2">"Deployment successful!"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
    <span class="n">post</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div> </div> <blockquote> <p><strong>Note:</strong> The <code class="language-plaintext highlighter-rouge">kubectl set image</code> command is a simple, imperative way to update an image. In more advanced scenarios, tools like Helm or Kustomize are used for a more declarative approach.</p> </blockquote> </li> <li><strong>Push the Changes and Watch the Magic:</strong> <ul> <li><code class="language-plaintext highlighter-rouge">git add Jenkinsfile</code></li> <li><code class="language-plaintext highlighter-rouge">git commit -m "ci: Add CD stage to deploy to Kubernetes"</code></li> <li><code class="language-plaintext highlighter-rouge">git push origin main</code></li> </ul> <p>The webhook will trigger Jenkins. Watch the pipeline: it will build, push the image, install kubectl, and then update your K8s deployment with the brand-new image!</p> </li> <li><strong>Verification:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Watch the pods, you'll see the old ones terminate and new ones start</span>
kubectl get pods <span class="nt">-w</span>

<span class="c"># Check the deployment history</span>
kubectl rollout <span class="nb">history </span>deployment/hello-devops-deployment
</code></pre></div> </div> </li> </ol> <hr/> <h2 id="module-06-reverse-proxy--load-balancing-nginx-ingress"><strong>Module 06: Reverse Proxy &amp; Load Balancing (Nginx Ingress)</strong></h2> <p>Currently, we access our service via a <code class="language-plaintext highlighter-rouge">NodePort</code> (e.g., <code class="language-plaintext highlighter-rouge">30007</code>), which isn’t ideal. A <strong>reverse proxy</strong> acts as an entry point to your cluster. It receives requests and forwards them to the appropriate services.</p> <h3 id="key-concepts-5">Key Concepts</h3> <ul> <li><strong>Reverse Proxy:</strong> A server that sits in front of web servers and forwards client requests to the appropriate server. It can handle SSL, compression, caching, etc.</li> <li><strong>Load Balancing:</strong> Distributing incoming traffic across multiple servers (or pods) to improve performance and availability.</li> <li><strong>Ingress Controller (Kubernetes):</strong> A specific type of reverse proxy for Kubernetes that manages external access to cluster services, usually via HTTP/HTTPS.</li> </ul> <h3 id="architecture-with-an-ingress-controller">Architecture with an Ingress Controller</h3> <pre><code class="language-mermaid">graph LR
    User -- http://hello.devops.local --&gt; IngressController(Nginx Ingress Controller&lt;br&gt;on K8s node port 80/443);
    IngressController -- Routing Rule --&gt; Service[hello-devops-service];
    Service -- Forwards to --&gt; Pod1[Pod hello-devops];
    Service -- Forwards to --&gt; Pod2[Pod hello-devops];
</code></pre> <h3 id="workshop-deploy-an-nginx-ingress">Workshop: Deploy an Nginx Ingress</h3> <ol> <li> <p><strong>Install the Nginx Ingress Controller on Kind:</strong> Kind has specific documentation for this.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml

<span class="c"># Wait for the ingress controller pods to be ready</span>
kubectl <span class="nb">wait</span> <span class="nt">--namespace</span> ingress-nginx <span class="se">\</span>
  <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="se">\</span>
  <span class="nt">--selector</span><span class="o">=</span>app.kubernetes.io/component<span class="o">=</span>controller <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>90s
</code></pre></div> </div> </li> <li> <p><strong>Modify our Kubernetes Service:</strong> The <code class="language-plaintext highlighter-rouge">NodePort</code> type is no longer needed. We’ll change it to <code class="language-plaintext highlighter-rouge">ClusterIP</code> (the default type), which is only accessible from within the cluster. Modify <code class="language-plaintext highlighter-rouge">k8s/service.yaml</code>:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/service.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="c1"># type: ClusterIP (can be omitted as it's the default)</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">hello-devops</span>
  <span class="na">ports</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
    <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span>
</code></pre></div> </div> </li> <li> <p><strong>Create an Ingress Resource:</strong> Create <code class="language-plaintext highlighter-rouge">k8s/ingress.yaml</code>. This is the rule that tells Nginx how to route traffic.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/ingress.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-ingress</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">host</span><span class="pi">:</span> <span class="s2">"</span><span class="s">hello.devops.local"</span>
    <span class="na">http</span><span class="pi">:</span>
      <span class="na">paths</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
        <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
        <span class="na">backend</span><span class="pi">:</span>
          <span class="na">service</span><span class="pi">:</span>
            <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-service</span>
            <span class="na">port</span><span class="pi">:</span>
              <span class="na">number</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div> </div> </li> <li><strong>Modify Your Local <code class="language-plaintext highlighter-rouge">hosts</code> File:</strong> To make your browser aware that <code class="language-plaintext highlighter-rouge">hello.devops.local</code> points to your local cluster, add this line to your <code class="language-plaintext highlighter-rouge">/etc/hosts</code> file (on Linux/Mac) or <code class="language-plaintext highlighter-rouge">C:\Windows\System32\drivers\etc\hosts</code> (on Windows). <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>127.0.0.1 hello.devops.local
</code></pre></div> </div> </li> <li><strong>Deploy and Test:</strong> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Apply the service changes and the new ingress</span>
kubectl apply <span class="nt">-f</span> k8s/service.yaml
kubectl apply <span class="nt">-f</span> k8s/ingress.yaml

<span class="c"># Check that the ingress is created</span>
kubectl get ingress
</code></pre></div> </div> <p>Open your browser to <code class="language-plaintext highlighter-rouge">http://hello.devops.local</code>. It works! Nginx received the request and forwarded it to your service.</p> </li> <li><strong>Push to Gitea:</strong> Don’t forget to version your new manifests! <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add k8s/
git commit <span class="nt">-m</span> <span class="s2">"feat: Add Nginx Ingress for clean routing"</span>
git push origin main
</code></pre></div> </div> </li> </ol> <h3 id="sources--further-reading-5">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://kubernetes.io/docs/concepts/services-networking/ingress/">Kubernetes Ingress</a></li> <li><strong>Documentation:</strong> <a href="https://kubernetes.github.io/ingress-nginx/">Nginx Ingress Controller</a></li> </ul> <hr/> <h2 id="module-07-infrastructure-as-code-iac-with-terraform"><strong>Module 07: Infrastructure as Code (IaC) with Terraform</strong></h2> <p>Infrastructure as Code (IaC) is the management and provisioning of infrastructure (servers, networks, etc.) through configuration files, rather than manual configuration. Terraform is the leading tool in this space.</p> <h3 id="key-concepts-6">Key Concepts</h3> <ul> <li><strong>Provider:</strong> A plugin that allows Terraform to interact with a specific API (AWS, Docker, Kubernetes…).</li> <li><strong>Resource:</strong> A block of code that describes an infrastructure component (e.g., a Docker network, a container).</li> <li><strong>State File:</strong> A file (usually <code class="language-plaintext highlighter-rouge">terraform.tfstate</code>) where Terraform records the state of the infrastructure it manages.</li> <li><strong>Plan:</strong> The <code class="language-plaintext highlighter-rouge">terraform plan</code> command shows what Terraform will create, modify, or destroy.</li> <li><strong>Apply:</strong> The <code class="language-plaintext highlighter-rouge">terraform apply</code> command executes the actions from the plan.</li> </ul> <h3 id="workshop-manage-our-docker-network-with-terraform">Workshop: Manage Our Docker Network with Terraform</h3> <p>Instead of letting <code class="language-plaintext highlighter-rouge">docker-compose</code> create our <code class="language-plaintext highlighter-rouge">devops-net</code> network, we’ll manage it with Terraform to illustrate the IaC concept.</p> <ol> <li> <p><strong>Prerequisites:</strong> <a href="https://learn.hashicorp.com/tutorials/terraform/install-cli">Install Terraform</a>.</p> </li> <li> <p><strong>Create Terraform Files:</strong> At the root of your <code class="language-plaintext highlighter-rouge">devops-course-lab</code> project, create a <code class="language-plaintext highlighter-rouge">terraform</code> folder. Inside, create <code class="language-plaintext highlighter-rouge">main.tf</code>.</p> <div class="language-terraform highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># terraform/main.tf</span>

<span class="c1"># Declare required providers</span>
<span class="k">terraform</span> <span class="p">{</span>
  <span class="nx">required_providers</span> <span class="p">{</span>
    <span class="nx">docker</span> <span class="o">=</span> <span class="p">{</span>
      <span class="nx">source</span>  <span class="o">=</span> <span class="s2">"kreuzwerker/docker"</span>
      <span class="nx">version</span> <span class="o">=</span> <span class="s2">"~&gt; 2.16.0"</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="c1"># Configure the Docker provider</span>
<span class="k">provider</span> <span class="s2">"docker"</span> <span class="p">{}</span>

<span class="c1"># Define a resource of type "Docker network"</span>
<span class="k">resource</span> <span class="s2">"docker_network"</span> <span class="s2">"devops_network"</span> <span class="p">{</span>
  <span class="nx">name</span>   <span class="o">=</span> <span class="s2">"devops-net"</span>
  <span class="nx">driver</span> <span class="o">=</span> <span class="s2">"bridge"</span>
<span class="p">}</span>
</code></pre></div> </div> </li> <li> <p><strong>Modify <code class="language-plaintext highlighter-rouge">docker-compose.yml</code>:</strong> We need to tell <code class="language-plaintext highlighter-rouge">docker-compose</code> to use the external network that Terraform will create. Modify your <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> at the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code>.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ... (services)</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">devops-net</span><span class="pi">:</span>
    <span class="c1"># Indicates that this network is external and already created</span>
    <span class="na">external</span><span class="pi">:</span> <span class="kc">true</span>
</code></pre></div> </div> </li> <li><strong>Run Terraform:</strong> <ul> <li>Stop and remove your current containers and networks to start fresh: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># In the devops-course-lab folder</span>
docker-compose down
docker network <span class="nb">rm </span>devops-net
</code></pre></div> </div> </li> <li>Navigate into the <code class="language-plaintext highlighter-rouge">terraform</code> folder and run the commands: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Initialize Terraform and download the Docker provider</span>
terraform init

<span class="c"># Show what Terraform is going to do</span>
terraform plan

<span class="c"># Apply the changes (answer "yes")</span>
terraform apply
</code></pre></div> </div> <p>Terraform has now created the <code class="language-plaintext highlighter-rouge">devops-net</code> network.</p> </li> </ul> </li> <li><strong>Relaunch the Lab:</strong> Return to the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code> and restart <code class="language-plaintext highlighter-rouge">docker-compose</code>. <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker-compose up <span class="nt">-d</span>
</code></pre></div> </div> <p>The containers will start and attach to the Terraform-managed network!</p> </li> </ol> <h3 id="sources--further-reading-6">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://learn.hashicorp.com/tutorials/terraform/infrastructure-as-code">Introduction to Terraform</a></li> <li><strong>Provider:</strong> <a href="https://registry.terraform.io/providers/kreuzwerker/docker/latest/docs">Terraform Docker Provider</a></li> </ul> <hr/> <h2 id="module-08-configuration-management-with-ansible"><strong>Module 08: Configuration Management with Ansible</strong></h2> <p>While Terraform is excellent for <em>provisioning</em> infrastructure (creating servers), Ansible is excellent for <em>configuring</em> it (installing software, managing config files, etc.).</p> <h3 id="key-concepts-7">Key Concepts</h3> <ul> <li><strong>Inventory:</strong> A list of servers (hosts) that Ansible should manage.</li> <li><strong>Playbook:</strong> A YAML file that describes a set of tasks to execute on the hosts.</li> <li><strong>Task:</strong> A single action that Ansible executes (e.g., install a package, copy a file).</li> <li><strong>Module:</strong> A tool that Ansible uses to accomplish a task (e.g., <code class="language-plaintext highlighter-rouge">apt</code>, <code class="language-plaintext highlighter-rouge">copy</code>, <code class="language-plaintext highlighter-rouge">service</code>).</li> <li><strong>Agentless:</strong> Ansible does not require an agent on the target machines. It typically connects via SSH.</li> </ul> <h3 id="workshop-use-ansible-to-configure-a-file-on-our-jenkins-container">Workshop: Use Ansible to Configure a File on our Jenkins Container</h3> <p>We will simulate server configuration by using Ansible to create a file inside the Jenkins container.</p> <ol> <li> <p><strong>Prerequisites:</strong> <a href="https://docs.ansible.com/ansible/latest/installation_guide/intro_installation.html">Install Ansible</a>.</p> </li> <li> <p><strong>Create Ansible Files:</strong> At the root of <code class="language-plaintext highlighter-rouge">devops-course-lab</code>, create an <code class="language-plaintext highlighter-rouge">ansible</code> folder.</p> <ul> <li><strong>Create an inventory <code class="language-plaintext highlighter-rouge">inventory.ini</code>:</strong> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ansible/inventory.ini
</span><span class="nn">[jenkins_server]</span>
<span class="c"># Target the container directly by its name using the docker connection
</span><span class="err">jenkins</span> <span class="py">ansible_connection</span><span class="p">=</span><span class="s">docker</span>
</code></pre></div> </div> </li> <li><strong>Create a playbook <code class="language-plaintext highlighter-rouge">configure-jenkins.yml</code>:</strong> ```yaml <h1 id="ansibleconfigure-jenkinsyml">ansible/configure-jenkins.yml</h1> <ul> <li> <p>name: Configure Jenkins Container hosts: jenkins_server become: yes # To execute tasks as root</p> <p>tasks:</p> <ul> <li> <p>name: Create a welcome message file copy: content: “This file was created by Ansible on .” dest: /var/jenkins_home/ansible_managed.txt owner: jenkins group: jenkins mode: ‘0644’</p> </li> <li> <p>name: Ensure a package is installed (example) apt: name: cowsay state: present update_cache: yes ```</p> </li> </ul> </li> </ul> </li> </ul> </li> <li><strong>Run the Ansible Playbook:</strong> In your terminal, from the <code class="language-plaintext highlighter-rouge">ansible</code> folder: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># The command to run the playbook</span>
ansible-playbook <span class="nt">-i</span> inventory.ini configure-jenkins.yml
</code></pre></div> </div> </li> <li><strong>Verify the Result:</strong> Connect to the Jenkins container and check that the file was created and the package is installed. <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check the file</span>
docker <span class="nb">exec </span>jenkins <span class="nb">cat</span> /var/jenkins_home/ansible_managed.txt

<span class="c"># Check the installed package</span>
docker <span class="nb">exec </span>jenkins cowsay <span class="s2">"Ansible was here!"</span>
</code></pre></div> </div> <p>You’ll see the welcome message and the ASCII cow!</p> </li> </ol> <h3 id="sources--further-reading-7">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://docs.ansible.com/ansible/latest/index.html">Ansible Documentation</a></li> <li><strong>Connection Plugin:</strong> <a href="https://docs.ansible.com/ansible/latest/collections/community/docker/docker_connection.html">Ansible Docker Connection</a></li> </ul> <hr/> <h2 id="module-09-integrating-security-devsecops"><strong>Module 09: Integrating Security (DevSecOps)</strong></h2> <p>DevSecOps is an extension of the DevOps culture that aims to integrate security into every stage of the software lifecycle. The idea is to “<strong>Shift Left</strong>,” meaning security is integrated as early as possible in the development process, rather than being a final check before production.</p> <h3 id="key-concepts-8">Key Concepts</h3> <ul> <li><strong>Shift Left:</strong> Detecting and fixing security flaws early in the cycle (during coding, building), where it is cheapest and fastest.</li> <li><strong>SAST (Static Application Security Testing):</strong> Analyzing source code without executing it to find vulnerabilities (e.g., SQL injections, hardcoded passwords).</li> <li><strong>SCA (Software Composition Analysis):</strong> Analyzing the project’s open-source dependencies to find known vulnerabilities (CVEs).</li> <li><strong>Container Image Scanning:</strong> Analyzing the final Docker image to find vulnerabilities in OS packages and libraries.</li> </ul> <h3 id="secure-pipeline-architecture">Secure Pipeline Architecture</h3> <pre><code class="language-mermaid">graph TD
    subgraph "Jenkins Pipeline"
        direction LR
        A[Checkout] --&gt; B[SAST&lt;br&gt;SonarQube];
        B --&gt; C[Build Image];
        C --&gt; D[SCA &amp; Image Scan&lt;br&gt;Trivy];
        D --&gt; E{Push to Registry};
        E --&gt; F[Deploy to K8s];
    end
    Dev[git push] --&gt; A;
    B -- Sends report --&gt; SonarQubeUI[SonarQube UI];
    D -- Shows flaws --&gt; Console[Jenkins Console];
</code></pre> <h3 id="workshop-securing-our-cicd-pipeline">Workshop: Securing our CI/CD Pipeline</h3> <h4 id="1-add-sonarqube-to-the-lab">1. Add SonarQube to the Lab</h4> <p>SonarQube is a leading platform for code quality and security analysis. Add these services to your <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> file and restart with <code class="language-plaintext highlighter-rouge">docker-compose up -d</code>.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#... inside the services section:</span>

  <span class="na">sonarqube</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">sonarqube:lts-community</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">sonarqube</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">9000:9000"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.url=jdbc:postgresql://sonardb:5432/sonar</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.username=sonar</span>
      <span class="pi">-</span> <span class="s">sonar.jdbc.password=sonar</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonarqube_conf:/opt/sonarqube/conf</span>
      <span class="pi">-</span> <span class="s">sonarqube_data:/opt/sonarqube/data</span>
      <span class="pi">-</span> <span class="s">sonarqube_extensions:/opt/sonarqube/extensions</span>
      <span class="pi">-</span> <span class="s">sonarqube_logs:/opt/sonarqube/logs</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">sonardb</span>

  <span class="na">sonardb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">postgres:12</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">sonardb</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">POSTGRES_USER=sonar</span>
      <span class="pi">-</span> <span class="s">POSTGRES_PASSWORD=sonar</span>
      <span class="pi">-</span> <span class="s">POSTGRES_DB=sonar</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">postgresql:/var/lib/postgresql</span>
      <span class="pi">-</span> <span class="s">postgresql_data:/var/lib/postgresql/data</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">devops-net</span>

<span class="c1">#... at the end of the file, add the volumes:</span>
<span class="na">volumes</span><span class="pi">:</span>
  <span class="c1">#... (jenkins_home, etc.)</span>
  <span class="na">sonarqube_conf</span><span class="pi">:</span>
  <span class="na">sonarqube_data</span><span class="pi">:</span>
  <span class="na">sonarqube_extensions</span><span class="pi">:</span>
  <span class="na">sonarqube_logs</span><span class="pi">:</span>
  <span class="na">postgresql</span><span class="pi">:</span>
  <span class="na">postgresql_data</span><span class="pi">:</span>
</code></pre></div></div> <h4 id="2-configure-sonarqube-and-jenkins">2. Configure SonarQube and Jenkins</h4> <ol> <li><strong>SonarQube:</strong> <ul> <li>Access <code class="language-plaintext highlighter-rouge">http://localhost:9000</code> (it might take a few minutes to start).</li> <li>Log in with <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code>. Change the password.</li> <li>Go to “Administration” &gt; “Projects” &gt; “Management” and create a project manually. Name it <code class="language-plaintext highlighter-rouge">hello-devops</code>.</li> <li>Go to your profile (top right) &gt; “My Account” &gt; “Security”. Generate a token, name it <code class="language-plaintext highlighter-rouge">jenkins-token</code>, and copy it.</li> </ul> </li> <li><strong>Jenkins:</strong> <ul> <li>Install the <strong>SonarQube Scanner</strong> plugin.</li> <li>Go to “Manage Jenkins” &gt; “Configure System”. In the “SonarQube servers” section, add a server: <ul> <li>Name: <code class="language-plaintext highlighter-rouge">sonarqube</code></li> <li>Server URL: <code class="language-plaintext highlighter-rouge">http://sonarqube:9000</code></li> <li>Server authentication token: “Add” &gt; “Jenkins” &gt; Kind: <code class="language-plaintext highlighter-rouge">Secret text</code>, paste your token, ID: <code class="language-plaintext highlighter-rouge">sonarqube-token</code>.</li> </ul> </li> <li>Go to “Manage Jenkins” &gt; “Global Tool Configuration”. Under “SonarQube Scanner”, “Add SonarQube Scanner”, and choose automatic installation.</li> </ul> </li> </ol> <h4 id="3-update-the-application-and-pipeline">3. Update the Application and Pipeline</h4> <ol> <li><strong>Add a SonarQube configuration file:</strong> At the root of your <code class="language-plaintext highlighter-rouge">hello-devops</code> project, create a file named <code class="language-plaintext highlighter-rouge">sonar-project.properties</code>: <div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">sonar.projectKey</span><span class="p">=</span><span class="s">hello-devops</span>
<span class="py">sonar.projectName</span><span class="p">=</span><span class="s">hello-devops</span>
<span class="py">sonar.projectVersion</span><span class="p">=</span><span class="s">1.0</span>
<span class="py">sonar.sources</span><span class="p">=</span><span class="s">.</span>
<span class="py">sonar.python.version</span><span class="p">=</span><span class="s">3.9</span>
<span class="py">sonar.sourceEncoding</span><span class="p">=</span><span class="s">UTF-8</span>
</code></pre></div> </div> </li> <li> <p><strong>Modify the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> to add security stages:</strong> We will add a stage for SonarQube (SAST) and one for Trivy (Image Scanning).</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>
    <span class="n">tools</span> <span class="o">{</span>
        <span class="c1">// Ensure the SonarQube Scanner tool is available</span>
        <span class="c1">// Name must match the one in Global Tool Configuration</span>
        <span class="n">sonarqube</span> <span class="s1">'SonarQubeScanner'</span> 
    <span class="o">}</span>
    <span class="n">environment</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="c1">// NEW STAGE: SAST</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Static Code Analysis'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="c1">// Use the SonarQube configuration defined in Jenkins</span>
                    <span class="n">withSonarQubeEnv</span><span class="o">(</span><span class="s1">'sonarqube'</span><span class="o">)</span> <span class="o">{</span>
                        <span class="c1">// 'scannerHome' is injected by the plugin</span>
                        <span class="n">sh</span> <span class="s2">"${scannerHome}/bin/sonar-scanner"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="c1">// NEW STAGE: Quality Gate</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'SonarQube Quality Gate'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="c1">// Wait for the analysis result and fail the pipeline if the Quality Gate is not passed</span>
                <span class="n">timeout</span><span class="o">(</span><span class="nl">time:</span> <span class="mi">1</span><span class="o">,</span> <span class="nl">unit:</span> <span class="s1">'HOURS'</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">waitForQualityGate</span> <span class="nl">abortPipeline:</span> <span class="kc">true</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
            
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build &amp; Push Docker Image'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>

        <span class="c1">// NEW STAGE: IMAGE SCANNING</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Vulnerability Scan (Image)'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="c1">// Use the official Trivy image to scan our image</span>
                    <span class="c1">// Fail the pipeline if a CRITICAL vulnerability is found</span>
                    <span class="n">sh</span> <span class="s2">"""
                    docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
                    aquasec/trivy:latest image --exit-code 1 --severity CRITICAL ${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}
                    """</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
            
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
    <span class="o">}</span>
    <span class="n">post</span> <span class="o">{</span> <span class="cm">/* ... */</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div> </div> </li> </ol> <h4 id="4-push-changes-and-analyze">4. Push Changes and Analyze</h4> <ol> <li><code class="language-plaintext highlighter-rouge">git add sonar-project.properties Jenkinsfile</code></li> <li><code class="language-plaintext highlighter-rouge">git commit -m "feat(ci): Integrate SonarQube and Trivy scans"</code></li> <li><code class="language-plaintext highlighter-rouge">git push origin main</code></li> </ol> <p>Observe the new pipeline. You can now go to <code class="language-plaintext highlighter-rouge">http://localhost:9000</code> to see the full code analysis report, and check the Jenkins console for Trivy’s vulnerability report.</p> <h3 id="sources--further-reading-8">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://docs.sonarqube.org/latest/analysis/scan/sonarscanner-for-jenkins/">SonarQube Scanner for Jenkins</a></li> <li><strong>Documentation:</strong> <a href="https://aquasecurity.github.io/trivy/v0.25.3/integrations/ci/jenkins/">Trivy - CI Integration</a></li> </ul> <hr/> <h2 id="module-10-monitoring--observability"><strong>Module 10: Monitoring &amp; Observability</strong></h2> <p>Deploying is good, but knowing what’s happening is better. Monitoring and observability allow us to watch the health and performance of our application and infrastructure.</p> <h3 id="key-concepts-9">Key Concepts</h3> <ul> <li><strong>Monitoring:</strong> Collecting, processing, and displaying quantitative data about a system. It answers the question: “Is the system working?”.</li> <li><strong>Observability:</strong> The ability to understand a system’s internal state from its external outputs. It answers the question: “Why isn’t the system working?”.</li> <li><strong>The 3 Pillars of Observability:</strong> <ol> <li><strong>Metrics:</strong> Numerical measurements aggregated over time intervals (e.g., CPU usage, requests per second). Tool: <strong>Prometheus</strong>.</li> <li><strong>Logs:</strong> Records of discrete, timestamped events (e.g., application error, user request). Tool: <strong>Loki</strong>.</li> <li><strong>Traces:</strong> A representation of a request’s journey through the different services of a microservices architecture.</li> </ol> </li> </ul> <h3 id="our-monitoring-stack-architecture-the-prom-stack">Our Monitoring Stack Architecture (The “Prom Stack”)</h3> <pre><code class="language-mermaid">graph TD
    subgraph "Kubernetes Cluster"
        Prometheus[Prometheus] -- Scrapes --&gt; AppMetrics(App Pod&lt;br&gt;/metrics);
        Prometheus -- Scrapes --&gt; KubeMetrics[Kube-State-Metrics];
        Promtail[Promtail Agent] -- Collects Logs --&gt; AppLogs(App Pod Logs);
        Promtail -- Sends Logs --&gt; Loki[Loki];
    end
    Grafana[Grafana] -- Queries --&gt; Prometheus;
    Grafana -- Queries --&gt; Loki;
    User[User] -- Visualizes --&gt; Grafana;
</code></pre> <h3 id="workshop-set-up-prometheus-loki-and-grafana">Workshop: Set up Prometheus, Loki, and Grafana</h3> <h4 id="1-instrument-the-application-to-expose-metrics">1. Instrument the Application to Expose Metrics</h4> <p>Our application needs to expose a <code class="language-plaintext highlighter-rouge">/metrics</code> endpoint that Prometheus can read.</p> <ol> <li><strong>Update <code class="language-plaintext highlighter-rouge">requirements.txt</code>:</strong> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flask==2.1.2
gunicorn==20.1.0
prometheus-client==0.14.1
</code></pre></div> </div> </li> <li><strong>Modify <code class="language-plaintext highlighter-rouge">app.py</code>:</strong> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">flask</span> <span class="kn">import</span> <span class="n">Flask</span><span class="p">,</span> <span class="n">Response</span>
<span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">prometheus_client</span> <span class="kn">import</span> <span class="n">Counter</span><span class="p">,</span> <span class="n">generate_latest</span><span class="p">,</span> <span class="n">REGISTRY</span>

<span class="n">app</span> <span class="o">=</span> <span class="nc">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>

<span class="c1"># Create a counter for HTTP requests
</span><span class="n">http_requests_total</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">(</span><span class="sh">'</span><span class="s">http_requests_total</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">Total number of HTTP requests</span><span class="sh">'</span><span class="p">)</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="n">http_requests_total</span><span class="p">.</span><span class="nf">inc</span><span class="p">()</span> <span class="c1"># Increment the counter on each request
</span>    <span class="n">version</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">APP_VERSION</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">v2.0</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">&lt;h1&gt;Hello from the new automated pipeline!&lt;/h1&gt;&lt;p&gt;Version: </span><span class="si">{</span><span class="n">version</span><span class="si">}</span><span class="s">&lt;/p&gt;</span><span class="sh">"</span>

<span class="nd">@app.route</span><span class="p">(</span><span class="sh">'</span><span class="s">/metrics</span><span class="sh">'</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">metrics</span><span class="p">():</span>
    <span class="c1"># Expose metrics in the Prometheus format
</span>    <span class="k">return</span> <span class="nc">Response</span><span class="p">(</span><span class="nf">generate_latest</span><span class="p">(</span><span class="n">REGISTRY</span><span class="p">),</span> <span class="n">mimetype</span><span class="o">=</span><span class="sh">'</span><span class="s">text/plain</span><span class="sh">'</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">'</span><span class="s">__main__</span><span class="sh">'</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="sh">'</span><span class="s">0.0.0.0</span><span class="sh">'</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">5000</span><span class="p">)</span>
</code></pre></div> </div> </li> </ol> <h4 id="2-deploy-the-monitoring-stack-in-kubernetes">2. Deploy the Monitoring Stack in Kubernetes</h4> <p>Manually installing each component is complex. We’ll use community Helm charts, which greatly simplify this process. You’ll need to <a href="https://helm.sh/docs/intro/install/">install Helm</a> first.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Add the Prometheus Community Helm repo</span>
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

<span class="c"># Install kube-prometheus-stack (includes Prometheus, Grafana, Alertmanager...)</span>
<span class="c"># into a dedicated 'monitoring' namespace</span>
kubectl create namespace monitoring
helm <span class="nb">install </span>prometheus prometheus-community/kube-prometheus-stack <span class="nt">--namespace</span> monitoring

<span class="c"># Install Loki and Promtail</span>
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update
helm <span class="nb">install </span>loki grafana/loki-stack <span class="nt">--namespace</span> monitoring
</code></pre></div></div> <h4 id="3-configure-prometheus-to-scrape-our-application">3. Configure Prometheus to Scrape Our Application</h4> <p>The <code class="language-plaintext highlighter-rouge">kube-prometheus-stack</code> uses <code class="language-plaintext highlighter-rouge">ServiceMonitor</code> resources to automatically discover services to scrape.</p> <ol> <li><strong>Update your <code class="language-plaintext highlighter-rouge">k8s/service.yaml</code></strong> to add a name to the port, which is required by the <code class="language-plaintext highlighter-rouge">ServiceMonitor</code>. ```yaml <h1 id="k8sserviceyaml">k8s/service.yaml</h1> <p>apiVersion: v1 kind: Service metadata: name: hello-devops-service labels: app: hello-devops # Important label for discovery spec: selector: app: hello-devops ports:</p> <ul> <li>name: http # Name the port protocol: TCP port: 80 targetPort: 5000 ```</li> </ul> </li> <li><strong>Create a <code class="language-plaintext highlighter-rouge">k8s/servicemonitor.yaml</code> manifest:</strong> ```yaml apiVersion: monitoring.coreos.com/v1 kind: ServiceMonitor metadata: name: hello-devops-monitor namespace: default # Make sure it’s in the same namespace as your service labels: release: prometheus # This label is crucial for Prometheus to find it spec: selector: matchLabels: app: hello-devops # Target services with this label endpoints: <ul> <li>port: http # Scrape the port named ‘http’ path: /metrics # On this path interval: 15s # Every 15 seconds ```</li> </ul> </li> </ol> <h4 id="4-access-grafana-and-visualize">4. Access Grafana and Visualize</h4> <ol> <li><code class="language-plaintext highlighter-rouge">git add .</code> and <code class="language-plaintext highlighter-rouge">git commit/push</code> to deploy your instrumented application and new manifests.</li> <li>Expose Grafana on your local machine: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">--namespace</span> monitoring port-forward svc/prometheus-grafana 8082:80
</code></pre></div> </div> </li> <li>Open <code class="language-plaintext highlighter-rouge">http://localhost:8082</code>. <ul> <li>Login: <code class="language-plaintext highlighter-rouge">admin</code>.</li> <li>Password: <code class="language-plaintext highlighter-rouge">prom-operator</code> (default, should be changed). You can retrieve it with <code class="language-plaintext highlighter-rouge">kubectl get secret --namespace monitoring prometheus-grafana -o jsonpath="{.data.admin-password}" | base64 --decode ; echo</code>.</li> </ul> </li> <li><strong>Explore:</strong> <ul> <li>Go to “Dashboards”. There are already pre-configured dashboards for Kubernetes!</li> <li>Go to “Explore”. Choose the “Prometheus” data source. Type <code class="language-plaintext highlighter-rouge">http_requests_total</code> and run. You’ll see your application’s counter!</li> <li>Change the data source to “Loki”. In the “Log browser”, select <code class="language-plaintext highlighter-rouge">app="hello-devops"</code>. You’ll see your application’s logs directly in Grafana.</li> </ul> </li> </ol> <h3 id="sources--further-reading-9">Sources &amp; Further Reading</h3> <ul> <li><strong>Documentation:</strong> <a href="https://github.com/prometheus/client_python">prometheus-client for Python</a></li> <li><strong>Stack:</strong> <a href="https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack">kube-prometheus-stack</a></li> <li><strong>Stack:</strong> <a href="https://github.com/grafana/helm-charts/tree/main/charts/loki-stack">Loki Helm Chart</a></li> </ul> <hr/> <h2 id="module-11-final-project---the-complete-a-to-z-pipeline"><strong>Module 11: Final Project - The Complete A-to-Z Pipeline</strong></h2> <p>Let’s recap everything we’ve learned by creating a complete workflow. The goal is to simulate a developer making a change and seeing it automatically propagate all the way to our Kubernetes environment, passing security checks along the way.</p> <h3 id="the-scenario">The Scenario</h3> <p>A developer needs to change the application’s message and update the version number. After they push their code, the CI/CD/CS (Continuous Security) chain must automatically:</p> <ol> <li>Verify the code’s quality and security with SonarQube.</li> <li>Build a new Docker image with a unique tag.</li> <li>Scan the image for vulnerabilities with Trivy.</li> <li>If all checks pass, push the image to our local registry.</li> <li>Deploy the application to Kubernetes using a declarative approach.</li> <li>All of this must happen without any manual intervention after the <code class="language-plaintext highlighter-rouge">git push</code>.</li> </ol> <h3 id="final-workflow-architecture">Final Workflow Architecture</h3> <pre><code class="language-mermaid">graph TD
    A[Developer&lt;br&gt;git push] --&gt; B{Gitea};
    B -- Webhook --&gt; C[Jenkins];
    C -- git clone --&gt; B;
    C -- SAST Scan --&gt; SonarQube;
    C -- docker build --&gt; D(Image v2.0);
    C -- Image Scan --&gt; Trivy;
    D -- docker push --&gt; E[Local Registry];
    C -- kubectl apply --&gt; F{Kubernetes Cluster};
    F -- docker pull --&gt; E;
    F -- Deploys --&gt; G[New Pods&lt;br&gt;with image v2.0];
    H[User] -- http://hello.devops.local --&gt; I[Nginx Ingress];
    I --&gt; G;
</code></pre> <h3 id="workshop-the-final-push">Workshop: The Final Push</h3> <ol> <li> <p><strong>Improve the <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> for Declarative Deployment:</strong> Our <code class="language-plaintext highlighter-rouge">kubectl set image</code> command is good, but it doesn’t apply other changes from our manifests. A better practice is to apply the manifest files directly.</p> <p>Modify the <code class="language-plaintext highlighter-rouge">Deploy to Kubernetes</code> stage in your <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ...</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Deploy to Kubernetes'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">withCredentials</span><span class="o">([</span><span class="n">file</span><span class="o">(</span><span class="nl">credentialsId:</span> <span class="n">KUBECONFIG_CREDENTIAL_ID</span><span class="o">,</span> <span class="nl">variable:</span> <span class="s1">'KUBECONFIG_FILE'</span><span class="o">)])</span> <span class="o">{</span>
                    <span class="n">script</span> <span class="o">{</span>
                        <span class="n">sh</span> <span class="s2">"export KUBECONFIG=${KUBECONFIG_FILE}"</span>

                        <span class="c1">// 1. Replace the image tag placeholder in the manifest</span>
                        <span class="c1">// We use sed to replace a placeholder we're about to add</span>
                        <span class="n">sh</span> <span class="s2">"sed -i 's|IMAGE_TAG_PLACEHOLDER|${IMAGE_TAG}|g' k8s/deployment.yaml"</span>
                            
                        <span class="c1">// 2. Apply all manifests from the k8s folder</span>
                        <span class="n">sh</span> <span class="s2">"kubectl apply -f k8s/"</span>

                        <span class="c1">// 3. Wait for the deployment</span>
                        <span class="n">sh</span> <span class="s2">"kubectl rollout status deployment/${K8S_DEPLOYMENT_NAME}"</span>
                        <span class="n">echo</span> <span class="s2">"Deployment successful!"</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
<span class="c1">// ...</span>
</code></pre></div> </div> </li> <li> <p><strong>Update <code class="language-plaintext highlighter-rouge">deployment.yaml</code> with a Placeholder:</strong> Change the image line in <code class="language-plaintext highlighter-rouge">k8s/deployment.yaml</code> so <code class="language-plaintext highlighter-rouge">sed</code> can replace it.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># k8s/deployment.yaml (final modification)</span>
<span class="c1"># ...</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-devops-container</span>
        <span class="c1"># Here's our placeholder!</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s">registry:5000/hello-devops:IMAGE_TAG_PLACEHOLDER</span>
<span class="c1"># ...</span>
</code></pre></div> </div> </li> <li><strong>Commit and Push:</strong> This is the moment of truth. Gather all your changes and push them. <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"refactor(ci): Use declarative deploy and update app to v2.0"</span>
git push origin main
</code></pre></div> </div> </li> <li><strong>Observe and Verify:</strong> <ul> <li>Go to Jenkins and watch the pipeline run through all the stages: SAST, build, image scan, deploy.</li> <li>Once finished, open <code class="language-plaintext highlighter-rouge">http://hello.devops.local</code> in your browser.</li> <li>You should see your new application message!</li> <li>You can also verify the image used by the pods: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.items[0].spec.containers[0].image}'</span>
</code></pre></div> </div> <p>This will display something like <code class="language-plaintext highlighter-rouge">registry:5000/hello-devops:build-X</code>.</p> </li> </ul> </li> </ol> <p><strong>Congratulations! You have set up and executed a complete, secure, and observable CI/CD pipeline, from code to production, entirely on your local machine.</strong></p> <hr/> <h2 id="module-12-conclusion--next-steps"><strong>Module 12: Conclusion &amp; Next Steps</strong></h2> <p>You have come a long way, from basic code management to a fully automated continuous deployment pipeline. You have gained practical skills in the most fundamental tools of the DevOps ecosystem.</p> <h3 id="what-youve-accomplished">What You’ve Accomplished:</h3> <ul> <li><strong>Set up a local DevOps lab</strong> with Docker Compose.</li> <li><strong>Versioned an application</strong> with Gitea.</li> <li><strong>Containerized the application</strong> with Docker.</li> <li><strong>Created a Continuous Integration (CI) pipeline</strong> with Jenkins that builds and stores artifacts.</li> <li><strong>Deployed a local Kubernetes cluster</strong> with Kind.</li> <li><strong>Implemented Continuous Deployment (CD)</strong> to automatically push changes to Kubernetes.</li> <li><strong>Exposed your application professionally</strong> with an Nginx Ingress.</li> <li><strong>Understood the principles of Infrastructure as Code</strong> with Terraform.</li> <li><strong>Discovered configuration management</strong> with Ansible.</li> <li><strong>Integrated security into the pipeline</strong> (<strong>DevSecOps</strong>) with SonarQube and Trivy.</li> <li><strong>Set up a professional-grade monitoring and logging stack</strong> with Prometheus, Grafana, and Loki.</li> </ul>]]></content><author><name></name></author><category term="course"/><category term="Kubernetes"/><category term="docker"/><category term="ansible"/><category term="terraform"/><category term="jenkin"/><category term="gitea"/><summary type="html"><![CDATA[Welcome to this comprehensive course on DevOps culture and tooling. The goal is to guide you, step-by-step, from theory to practice, empowering you to build, secure, and automate a full Continuous Integration and Continuous Deployment (CI/CD) pipeline.]]></summary></entry><entry><title type="html">Setting Up a Kubernetes Cluster with Kubeadm and CRI-O</title><link href="https://menraromial.github.io/blog/2025/kubernetes-with-kubeadm-and-cri.io/" rel="alternate" type="text/html" title="Setting Up a Kubernetes Cluster with Kubeadm and CRI-O"/><published>2025-03-25T16:40:16+00:00</published><updated>2025-03-25T16:40:16+00:00</updated><id>https://menraromial.github.io/blog/2025/kubernetes-with-kubeadm-and-cri.io</id><content type="html" xml:base="https://menraromial.github.io/blog/2025/kubernetes-with-kubeadm-and-cri.io/"><![CDATA[<h2 id="prerequisites">Prerequisites</h2> <p>Before setting up a Kubernetes cluster using Kubeadm and CRI-O, ensure that you meet the following requirements:</p> <h3 id="hardware-requirements">Hardware Requirements</h3> <ul> <li>At least <strong>2 GB of RAM</strong> per node (4 GB recommended for the master node)</li> <li>At least <strong>2 CPUs</strong> per node</li> <li><strong>20 GB of free disk space</strong></li> <li>A stable <strong>internet connection</strong></li> <li>All nodes should be able to communicate with each other</li> </ul> <h3 id="software-requirements">Software Requirements</h3> <ul> <li>A supported <strong>Linux distribution</strong> (Ubuntu 20.04+ or Debian 10+ recommended)</li> <li>User access with <strong>sudo privileges</strong></li> <li>Basic understanding of <strong>Linux command-line interface (CLI)</strong></li> <li>Knowledge of <strong>networking concepts</strong></li> </ul> <h2 id="step-1-configure-kernel-parameters">Step 1: Configure Kernel Parameters</h2> <p>Kubernetes requires some kernel parameters to be set to allow IPtables to see bridged traffic.</p> <h3 id="load-required-kernel-modules">Load Required Kernel Modules</h3> <p>Run the following commands on all nodes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
</span><span class="no">EOF

</span><span class="nb">sudo </span>modprobe overlay
<span class="nb">sudo </span>modprobe br_netfilter
</code></pre></div></div> <h3 id="set-kernel-parameters-for-kubernetes">Set Kernel Parameters for Kubernetes</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
</span><span class="no">EOF
</span></code></pre></div></div> <p>Apply the changes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sysctl <span class="nt">--system</span>
</code></pre></div></div> <h2 id="step-2-disable-swap">Step 2: Disable Swap</h2> <p>Kubernetes does not work well with swap enabled. Disable it using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>swapoff <span class="nt">-a</span>
</code></pre></div></div> <p>To make this change persistent, remove any swap entries from <code class="language-plaintext highlighter-rouge">/etc/fstab</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/fstab
</code></pre></div></div> <h2 id="step-3-install-cri-o-container-runtime-interface---open">Step 3: Install CRI-O (Container Runtime Interface - Open)</h2> <h3 id="add-cri-o-repository-and-install">Add CRI-O Repository and Install</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> software-properties-common gpg curl apt-transport-https ca-certificates

curl <span class="nt">-fsSL</span> https://pkgs.k8s.io/addons:/cri-o:/prerelease:/main/deb/Release.key | <span class="se">\</span>
    gpg <span class="nt">--dearmor</span> | <span class="nb">sudo tee</span> /etc/apt/keyrings/cri-o-apt-keyring.gpg <span class="o">&gt;</span>/dev/null
<span class="nb">echo</span> <span class="s2">"deb [signed-by=/etc/apt/keyrings/cri-o-apt-keyring.gpg] https://pkgs.k8s.io/addons:/cri-o:/prerelease:/main/deb/ /"</span> | <span class="se">\</span>
    <span class="nb">sudo tee</span> /etc/apt/sources.list.d/cri-o.list

<span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> cri-o
</code></pre></div></div> <h3 id="enable-and-start-cri-o-service">Enable and Start CRI-O Service</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl daemon-reload
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>crio <span class="nt">--now</span>
<span class="nb">sudo </span>systemctl start crio.service
</code></pre></div></div> <h2 id="step-4-install-kubernetes-components">Step 4: Install Kubernetes Components</h2> <h3 id="add-kubernetes-repository">Add Kubernetes Repository</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">KUBERNETES_VERSION</span><span class="o">=</span>1.30
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/apt/keyrings
curl <span class="nt">-fsSL</span> https://pkgs.k8s.io/core:/stable:/v<span class="nv">$KUBERNETES_VERSION</span>/deb/Release.key | <span class="se">\</span>
    gpg <span class="nt">--dearmor</span> | <span class="nb">sudo tee</span> /etc/apt/keyrings/kubernetes-apt-keyring.gpg <span class="o">&gt;</span> /dev/null

<span class="nb">echo</span> <span class="s2">"deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v</span><span class="nv">$KUBERNETES_VERSION</span><span class="s2">/deb/ /"</span> | <span class="se">\</span>
    <span class="nb">sudo tee</span> /etc/apt/sources.list.d/kubernetes.list
</code></pre></div></div> <h3 id="install-kubeadm-kubelet-and-kubectl">Install Kubeadm, Kubelet, and Kubectl</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nv">kubelet</span><span class="o">=</span>1.30.0-1.1 <span class="nv">kubectl</span><span class="o">=</span>1.30.0-1.1 <span class="nv">kubeadm</span><span class="o">=</span>1.30.0-1.1
</code></pre></div></div> <h3 id="configure-kubelet-node-ip">Configure Kubelet Node IP</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> jq
<span class="nv">local_ip</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>ip <span class="nt">--json</span> addr show eno1 | jq <span class="nt">-r</span> <span class="s1">'.[0].addr_info[] | select(.family == "inet") | .local'</span><span class="si">)</span><span class="s2">"</span>

<span class="nb">sudo tee</span> /etc/default/kubelet <span class="o">&gt;</span> /dev/null <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
KUBELET_EXTRA_ARGS=--node-ip=</span><span class="nv">$local_ip</span><span class="sh">
</span><span class="no">EOF
</span></code></pre></div></div> <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">eno1</code> and <code class="language-plaintext highlighter-rouge">enp85s0</code> are network interface names that may vary depending on the system. You can check your network interfaces using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ifconfig <span class="nt">-a</span>
</code></pre></div></div> <p>or</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip a
</code></pre></div></div> <p>Adjust the interface name accordingly based on the output of these commands.</p> <h2 id="step-5-initialize-kubernetes-on-the-master-node">Step 5: Initialize Kubernetes on the Master Node</h2> <p>Set the master node IP and hostname:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">IPADDR</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>ip <span class="nt">--json</span> addr show enp85s0 | jq <span class="nt">-r</span> <span class="s1">'.[0].addr_info[] | select(.family == "inet") | .local'</span><span class="si">)</span><span class="s2">"</span>
<span class="nv">NODENAME</span><span class="o">=</span><span class="si">$(</span><span class="nb">hostname</span> <span class="nt">-s</span><span class="si">)</span>
<span class="nv">POD_CIDR</span><span class="o">=</span><span class="s2">"192.168.0.0/16"</span>
</code></pre></div></div> <p>Initialize Kubernetes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm init <span class="nt">--apiserver-advertise-address</span><span class="o">=</span><span class="nv">$IPADDR</span> <span class="nt">--apiserver-cert-extra-sans</span><span class="o">=</span><span class="nv">$IPADDR</span> <span class="nt">--pod-network-cidr</span><span class="o">=</span><span class="nv">$POD_CIDR</span> <span class="nt">--node-name</span> <span class="nv">$NODENAME</span> <span class="nt">--ignore-preflight-errors</span> Swap <span class="nt">--cri-socket</span> unix:///var/run/crio/crio.sock 
</code></pre></div></div> <p>Set up kubeconfig for the root user:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> <span class="nv">$HOME</span>/.kube
<span class="nb">cp</span> <span class="nt">-i</span> /etc/kubernetes/admin.conf <span class="nv">$HOME</span>/.kube/config
<span class="nb">chown</span> <span class="si">$(</span><span class="nb">id</span> <span class="nt">-u</span><span class="si">)</span>:<span class="si">$(</span><span class="nb">id</span> <span class="nt">-g</span><span class="si">)</span> <span class="nv">$HOME</span>/.kube/config
</code></pre></div></div> <h2 id="step-6-deploy-network-and-monitoring-components">Step 6: Deploy Network and Monitoring Components</h2> <h3 id="install-calico-network-plugin">Install Calico Network Plugin</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://docs.projectcalico.org/manifests/calico.yaml
</code></pre></div></div> <h3 id="install-metrics-server">Install Metrics Server</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/techiescamp/kubeadm-scripts/main/manifests/metrics-server.yaml
</code></pre></div></div> <h3 id="install-ingress-controller">Install Ingress Controller</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v0.49.0/deploy/static/provider/baremetal/deploy.yaml
</code></pre></div></div> <h2 id="step-7-join-worker-nodes-to-the-cluster">Step 7: Join Worker Nodes to the Cluster</h2> <p>Generate a join command on the master node:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubeadm token create <span class="nt">--print-join-command</span>
</code></pre></div></div> <p>Run the output command on worker nodes to join them to the cluster.</p> <h2 id="step-8-verify-cluster-status">Step 8: Verify Cluster Status</h2> <h3 id="check-nodes">Check Nodes</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div> <h3 id="check-all-pods-in-a-specific-node">Check All Pods in a Specific Node</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--all-namespaces</span> <span class="nt">--field-selector</span> spec.nodeName<span class="o">=</span>&lt;NODE_NAME&gt;
</code></pre></div></div> <h2 id="troubleshooting-tips">Troubleshooting Tips</h2> <ul> <li>Always check system logs: <code class="language-plaintext highlighter-rouge">journalctl -u kubelet</code></li> <li>Verify node status: <code class="language-plaintext highlighter-rouge">kubectl get nodes</code></li> <li>Check pod status: <code class="language-plaintext highlighter-rouge">kubectl get pods --all-namespaces</code></li> </ul> <h2 id="security-considerations">Security Considerations</h2> <ul> <li>Regularly update Kubernetes and CRI-O</li> <li>Implement RBAC (Role-Based Access Control)</li> <li>Use network policies</li> <li>Regularly scan containers for vulnerabilities</li> </ul> <p><strong>Note</strong>: Always refer to the official Kubernetes and CRI-O documentation for the most up-to-date information and best practices.</p>]]></content><author><name></name></author><category term="tutorial"/><category term="Kubernetes"/><category term="kubeadm"/><category term="cri.io"/><category term="beginners"/><summary type="html"><![CDATA[Learn how to set up a production-ready Kubernetes cluster using Kubeadm and CRI-O. This step-by-step guide covers prerequisites, installation, configuration, and best practices for deploying and managing your cluster effectively.]]></summary></entry><entry><title type="html">Setting Up a K3s Cluster with a Hello-World Deployment</title><link href="https://menraromial.github.io/blog/2024/setup-k3s-cluster/" rel="alternate" type="text/html" title="Setting Up a K3s Cluster with a Hello-World Deployment"/><published>2024-07-07T14:30:00+00:00</published><updated>2024-07-07T14:30:00+00:00</updated><id>https://menraromial.github.io/blog/2024/setup-k3s-cluster</id><content type="html" xml:base="https://menraromial.github.io/blog/2024/setup-k3s-cluster/"><![CDATA[<p>This tutorial will guide you through setting up a K3s cluster with one master node and two worker nodes, deploying a simple “Hello-World” application, and utilizing various <code class="language-plaintext highlighter-rouge">kubectl</code> commands to manage and inspect Kubernetes resources.</p> <h2 id="prerequisites">Prerequisites</h2> <ul> <li>Three Linux machines (or VMs) with access to each other.</li> <li>Basic understanding of Linux commands and Kubernetes concepts.</li> </ul> <h2 id="step-1-installing-k3s">Step 1: Installing K3s</h2> <h3 id="install-k3s-on-the-master-node">Install K3s on the Master Node</h3> <ol> <li> <p><strong>SSH into the Master Node:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> ssh user@master-node-ip
</code></pre></div> </div> </li> <li> <p><strong>Install K3s:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> curl <span class="nt">-sfL</span> https://get.k3s.io |  <span class="nv">INSTALL_K3S_VERSION</span><span class="o">=</span>v1.27.12+k3s1 sh <span class="nt">-s</span> - <span class="nt">--disable</span> traefik <span class="nt">--write-kubeconfig-mode</span> 644 <span class="nt">--node-name</span> &lt;NODE_NAME&gt;
</code></pre></div> </div> </li> </ol> <p>The command installs K3s version <code class="language-plaintext highlighter-rouge">v1.27.12+k3s1</code> on a node named <code class="language-plaintext highlighter-rouge">&lt;NODE_NAME&gt;</code>, disables the default Traefik ingress controller, and sets the permissions for the kubeconfig file to <code class="language-plaintext highlighter-rouge">644</code>.</p> <ol> <li> <p><strong>Verify Installation:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get nodes
</code></pre></div> </div> <p>You should see the master node listed.</p> </li> </ol> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/97n9mbhc57emcmzbl4j9.png" alt="Image description"/></p> <h3 id="install-k3s-on-the-worker-nodes">Install K3s on the Worker Nodes</h3> <ol> <li> <p><strong>SSH into each Worker Node:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> ssh user@worker-node-ip
</code></pre></div> </div> </li> <li> <p><strong>Get the token from the master node</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nb">cat</span> /var/lib/rancher/k3s/server/node-token
</code></pre></div> </div> </li> <li> <p><strong>Install K3s on Worker Nodes:</strong></p> <p>On each worker node, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> curl <span class="nt">-sfL</span> https://get.k3s.io | <span class="nv">INSTALL_K3S_VERSION</span><span class="o">=</span>v1.27.12+k3s1  <span class="nv">K3S_NODE_NAME</span><span class="o">=</span>YOUR_NODE_NAME <span class="nv">K3S_URL</span><span class="o">=</span>https://master-node-ip:6443 <span class="nv">K3S_TOKEN</span><span class="o">=</span>YOUR_NODE_TOKEN sh -
</code></pre></div> </div> <p>You can find the <code class="language-plaintext highlighter-rouge">YOUR_NODE_TOKEN</code> value on the master node at <code class="language-plaintext highlighter-rouge">/var/lib/rancher/k3s/server/node-token</code>.</p> </li> </ol> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/0w2dhl0ynwvsr5r19e2j.png" alt="Image description"/></p> <ol> <li> <p><strong>Verify Worker Nodes Join the Cluster:</strong></p> <p>On the master node, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get nodes
</code></pre></div> </div> <p>You should see all nodes (master and workers) listed.</p> </li> </ol> <h2 id="step-2-verifying-node-status">Step 2: Verifying Node Status</h2> <p>To check the status of the nodes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div> <p>You should see the master and worker nodes with the status <code class="language-plaintext highlighter-rouge">Ready</code>.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/js3ik5rdbentis2q3rvv.png" alt="Image description"/></p> <h2 id="step-3-creating-a-pod">Step 3: Creating a Pod</h2> <p>Create a simple pod to ensure everything is working correctly.</p> <ol> <li> <p><strong>Create a YAML file named <code class="language-plaintext highlighter-rouge">hello-world-pod.yaml</code>:</strong></p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
 <span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
 <span class="na">metadata</span><span class="pi">:</span>
 <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
 <span class="na">spec</span><span class="pi">:</span>
 <span class="na">containers</span><span class="pi">:</span>
     <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello</span>
     <span class="na">image</span><span class="pi">:</span> <span class="s">busybox</span>  <span class="c1"># Replace with your desired Docker image</span>

</code></pre></div> </div> </li> <li> <p><strong>Apply the YAML file to create the pod:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl apply <span class="nt">-f</span> hello-world-pod.yaml
</code></pre></div> </div> </li> <li> <p><strong>Verify the Pod is Running:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get pods
</code></pre></div> </div> </li> <li> <p><strong>Describe the Pod:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl describe pod hello-world
</code></pre></div> </div> </li> </ol> <h2 id="step-4-deleting-the-pod">Step 4: Deleting the Pod</h2> <p>To delete the pod:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod hello-world
</code></pre></div></div> <h2 id="step-5-deploying-the-application">Step 5: Deploying the Application</h2> <h3 id="creating-a-namespace">Creating a namespace</h3> <p>A namespace in Kubernetes is used to organize and manage resources within a cluster, providing isolation and a way to divide cluster resources among multiple users or teams.</p> <p>The commands used in this process are:</p> <ol> <li><strong>View Existing Namespaces</strong>: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespaces
</code></pre></div> </div> <p>or</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get ns
</code></pre></div> </div> </li> <li><strong>Create a New Namespace</strong>: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create namespace demo
</code></pre></div> </div> </li> <li><strong>Verify the Creation of the Namespace</strong>: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespaces
</code></pre></div> </div> </li> </ol> <p>This process ensures you have successfully created the <code class="language-plaintext highlighter-rouge">demo</code> namespace and confirmed its presence in your Kubernetes cluster.</p> <h3 id="deployment-yaml-file">Deployment YAML File</h3> <p>A deployment in Kubernetes is a controller that manages the creation, scaling, and updating of a set of replicated Pods, ensuring the desired state of an application is maintained.</p> <p>Create a file named <code class="language-plaintext highlighter-rouge">hello-world-deployment.yaml</code> with the following content:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">5</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">hello-world</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">hello-world</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">bashofmann/rancher-demo:1.0.0</span>
          <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">Always</span>
          <span class="na">resources</span><span class="pi">:</span>
            <span class="na">requests</span><span class="pi">:</span>
              <span class="na">memory</span><span class="pi">:</span> <span class="s2">"</span><span class="s">12Mi"</span>
              <span class="na">cpu</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2m"</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">8080</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
              <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
          <span class="na">env</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">COW_COLOR</span>
              <span class="na">value</span><span class="pi">:</span> <span class="s">purple</span>
          <span class="na">readinessProbe</span><span class="pi">:</span>
            <span class="na">httpGet</span><span class="pi">:</span>
              <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
              <span class="na">port</span><span class="pi">:</span> <span class="s">web</span>
          <span class="na">livenessProbe</span><span class="pi">:</span>
            <span class="na">httpGet</span><span class="pi">:</span>
              <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
              <span class="na">port</span><span class="pi">:</span> <span class="s">web</span>
</code></pre></div></div> <h3 id="explanation-of-the-yaml-file">Explanation of the YAML File</h3> <ul> <li><strong>apiVersion</strong>: Specifies the version of the Kubernetes API to use.</li> <li><strong>kind</strong>: Specifies the type of resource being created (Deployment).</li> <li><strong>metadata</strong>: Contains data that helps uniquely identify the object, including a <code class="language-plaintext highlighter-rouge">name</code> and <code class="language-plaintext highlighter-rouge">namespace</code>.</li> <li><strong>spec</strong>: Defines the desired state of the Deployment. <ul> <li><strong>replicas</strong>: Number of pod replicas to run.</li> <li><strong>selector</strong>: Defines how the Deployment finds which Pods to manage.</li> <li><strong>template</strong>: Defines the Pods that will be created. <ul> <li><strong>metadata</strong>: Labels for the Pods.</li> <li><strong>spec</strong>: Specification of the containers within the Pods. <ul> <li><strong>containers</strong>: List of containers in the Pod. <ul> <li><strong>name</strong>: Name of the container.</li> <li><strong>image</strong>: Docker image to use for the container.</li> <li><strong>imagePullPolicy</strong>: Policy to use for image pulls.</li> <li><strong>ports</strong>: Ports to expose from the container.</li> <li><strong>env</strong>: Environment variables for the container.</li> <li><strong>readinessProbe</strong>: Defines a probe to check if the container is ready.</li> <li><strong>livenessProbe</strong>: Defines a probe to check if the container is alive.</li> </ul> </li> </ul> </li> </ul> </li> </ul> </li> </ul> <hr/> <p>This is a Go demo application used for demonstrating Kubernetes and load balancing.</p> <p>It will create a colored icon for each replica within a ReplicaSet and indicate which one most recently served a response. Replicas which haven’t been heard from will begin to fade out, until after a configurable number of seconds they will disappear.</p> <p>For more information on using the container image, please click <a href="https://github.com/bashofmann/rancher-demo">here</a>.</p> <h3 id="apply-the-deployment">Apply the Deployment</h3> <ol> <li> <p><strong>Apply the YAML file to create the Deployment:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl apply <span class="nt">-f</span> hello-world-deployment.yaml
</code></pre></div> </div> </li> <li> <p><strong>Verify the Deployment and Pods:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get deployments <span class="nt">-n</span> demo
 kubectl get pods <span class="nt">-n</span> demo
</code></pre></div> </div> </li> <li> <p><strong>Check the ReplicaSets:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get replicaset <span class="nt">-n</span> demo
</code></pre></div> </div> </li> <li> <p><strong>For more information on replicas:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get pods <span class="nt">-n</span> demo <span class="nt">-o</span> wide
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">-o wide</code>: Provides additional details about each pod, such as the node it’s running on, the IP address, the container images, and more.</p> </li> <li> <p><strong>See the pods metrics</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top pods <span class="nt">-n</span> demo
</code></pre></div> </div> <p>For nodes</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top nodes
</code></pre></div> </div> </li> </ol> <h2 id="step-6-exposing-the-application">Step 6: Exposing the Application</h2> <p>Create a service to expose the Deployment.</p> <p>A service in Kubernetes provides a stable network endpoint to access a set of pods, ensuring consistent access despite pod restarts or IP changes.</p> <h4 id="types-of-services">Types of Services:</h4> <ul> <li><strong>ClusterIP</strong>: Exposes the service on an internal IP within the cluster, making it accessible only within the cluster.</li> <li><strong>NodePort</strong>: Exposes the service on each node’s IP at a static port, making it accessible from outside the cluster using <code class="language-plaintext highlighter-rouge">&lt;NodeIP&gt;:&lt;NodePort&gt;</code>.</li> <li><strong>LoadBalancer</strong>: Exposes the service externally using a cloud provider’s load balancer.</li> <li><strong>ExternalName</strong>: Maps the service to the contents of the <code class="language-plaintext highlighter-rouge">externalName</code> field (e.g., <code class="language-plaintext highlighter-rouge">foo.bar.example.com</code>), creating a DNS alias.</li> </ul> <ol> <li> <p><strong>Create a ClusterIP Service:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl expose deployment hello-world <span class="nt">--type</span><span class="o">=</span>ClusterIP <span class="nt">--name</span><span class="o">=</span>hello-world-service <span class="nt">--port</span><span class="o">=</span>8080 <span class="nt">--target-port</span><span class="o">=</span>8080 <span class="nt">-n</span> demo
</code></pre></div> </div> <ul> <li><strong>expose deployment hello-world</strong>: Creates a service to expose the deployment named <code class="language-plaintext highlighter-rouge">hello-world</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">--type=ClusterIP</code></strong>: Specifies the type of service to create. <code class="language-plaintext highlighter-rouge">ClusterIP</code> means the service will be accessible only within the cluster.</li> <li><strong><code class="language-plaintext highlighter-rouge">--name=hello-world-service</code></strong>: Assigns the name <code class="language-plaintext highlighter-rouge">hello-world-service</code> to the newly created service.</li> <li><strong><code class="language-plaintext highlighter-rouge">--port=8080</code></strong>: Defines the port on which the service will be exposed.</li> <li><strong><code class="language-plaintext highlighter-rouge">--target-port=8080</code></strong>: Specifies the port on the pod that the service should forward traffic to.</li> <li><strong><code class="language-plaintext highlighter-rouge">-n demo</code></strong>: Limits the scope of the command to the <code class="language-plaintext highlighter-rouge">demo</code> namespace.</li> </ul> </li> <li> <p><strong>Verify the Service:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> kubectl get svc <span class="nt">-n</span> demo
</code></pre></div> </div> </li> </ol> <h2 id="step-7-accessing-the-application">Step 7: Accessing the Application</h2> <p>To access the application from your local machine, use <code class="language-plaintext highlighter-rouge">kubectl port-forward</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward svc/hello-world-service 8080:8080 <span class="nt">-n</span> demo <span class="nt">--address</span> node_address
</code></pre></div></div> <p>Open your browser and go to <code class="language-plaintext highlighter-rouge">http://node_address:8080</code> to see the application in action.</p> <ol> <li><strong>Create a service of type LoadBalancer</strong></li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl expose deployment hello-world <span class="nt">--type</span><span class="o">=</span>LoadBalancer <span class="nt">--name</span><span class="o">=</span>hello-world-service-loadbalancing <span class="nt">--port</span><span class="o">=</span>8080 <span class="nt">--target-port</span><span class="o">=</span>8080 <span class="nt">-n</span> demo
</code></pre></div></div> <p>Open your browser and go to <code class="language-plaintext highlighter-rouge">http://any_node_address:PORT</code> to see the application in action. <code class="language-plaintext highlighter-rouge">PORT</code> here is generated by default, see the image below.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/2r1n2277m60ukt74wohs.png" alt="Image description"/></p> <p>Application on the browser</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/0mah2fyxsrjsat6wdah6.png" alt="Image description"/></p> <h2 id="step-8-inspecting-resources">Step 8: Inspecting Resources</h2> <h3 id="describing-a-pod">Describing a Pod</h3> <p>To get detailed information about a specific pod:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod &lt;pod-name&gt; <span class="nt">-n</span> demo
</code></pre></div></div> <h3 id="describing-a-deployment">Describing a Deployment</h3> <p>To get detailed information about the deployment:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe deployment hello-world <span class="nt">-n</span> demo
</code></pre></div></div> <h3 id="viewing-pod-logs">Viewing Pod Logs</h3> <p>To view the logs of a pod:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs <span class="nt">-f</span> &lt;pod-name&gt; <span class="nt">-n</span> demo
</code></pre></div></div> <h2 id="step-9-cleaning-up">Step 9: Cleaning Up</h2> <p>To delete all the resources created:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete namespace demo
</code></pre></div></div> <h2 id="conclusion">Conclusion</h2> <p>You’ve successfully set up a K3s cluster, deployed a simple application, and used various <code class="language-plaintext highlighter-rouge">kubectl</code> commands to manage and inspect Kubernetes resources. This foundation will help you explore Kubernetes further and leverage its capabilities.</p> <h1 id="bonus">Bonus</h1> <h2 id="scaling-and-deploying-a-horizontal-pod-autoscaler-in-kubernetes">Scaling and Deploying a Horizontal Pod Autoscaler in Kubernetes</h2> <h3 id="step-1-scaling-updown-manually">Step 1: Scaling Up/Down Manually</h3> <p>You can manually scale the number of replicas in a deployment using the <code class="language-plaintext highlighter-rouge">kubectl scale</code> command.</p> <h4 id="scale-up">Scale Up</h4> <p>To scale the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment to 10 replicas:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment hello-world <span class="nt">--replicas</span><span class="o">=</span>10 <span class="nt">-n</span> demo
</code></pre></div></div> <h4 id="scale-down">Scale Down</h4> <p>To scale the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment back down to 3 replicas:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment hello-world <span class="nt">--replicas</span><span class="o">=</span>3 <span class="nt">-n</span> demo
</code></pre></div></div> <h3 id="step-2-deploying-a-horizontal-pod-autoscaler-hpa">Step 2: Deploying a Horizontal Pod Autoscaler (HPA)</h3> <p>The Horizontal Pod Autoscaler automatically adjusts the number of pod replicas in a deployment based on observed CPU utilization (or other select metrics).</p> <h4 id="prerequisites-1">Prerequisites</h4> <p>Ensure that the Metrics Server is deployed in your cluster. The Metrics Server collects resource metrics from the Kubernetes nodes and pods and is required for HPA to function.</p> <p>To install the Metrics Server:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
</code></pre></div></div> <h4 id="deploy-hpa-for-the-hello-world-deployment">Deploy HPA for the <code class="language-plaintext highlighter-rouge">hello-world</code> Deployment</h4> <ol> <li> <p><strong>Create the HPA</strong></p> <p>Create the file <code class="language-plaintext highlighter-rouge">hello-world-hpa.yaml</code></p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">autoscaling/v2</span>
 <span class="na">kind</span><span class="pi">:</span> <span class="s">HorizontalPodAutoscaler</span>
 <span class="na">metadata</span><span class="pi">:</span>
   <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world-autoscaler</span>
   <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
 <span class="na">spec</span><span class="pi">:</span>
   <span class="na">scaleTargetRef</span><span class="pi">:</span>
     <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
     <span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
     <span class="na">name</span><span class="pi">:</span> <span class="s">hello-world</span>
   <span class="na">minReplicas</span><span class="pi">:</span> <span class="m">5</span>
   <span class="na">maxReplicas</span><span class="pi">:</span> <span class="m">20</span>
   <span class="na">metrics</span><span class="pi">:</span>
   <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Resource</span>
     <span class="na">resource</span><span class="pi">:</span>
       <span class="na">name</span><span class="pi">:</span> <span class="s">cpu</span>
       <span class="na">target</span><span class="pi">:</span>
         <span class="na">type</span><span class="pi">:</span> <span class="s">Utilization</span>
         <span class="na">averageUtilization</span><span class="pi">:</span> <span class="m">70</span>
   <span class="na">behavior</span><span class="pi">:</span>
     <span class="na">scaleUp</span><span class="pi">:</span>
       <span class="na">stabilizationWindowSeconds</span><span class="pi">:</span> <span class="m">10</span>
       <span class="na">policies</span><span class="pi">:</span>
       <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Pods</span>
         <span class="na">value</span><span class="pi">:</span> <span class="m">1</span>
         <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>
     <span class="na">scaleDown</span><span class="pi">:</span>
       <span class="na">stabilizationWindowSeconds</span><span class="pi">:</span> <span class="m">10</span>
       <span class="na">policies</span><span class="pi">:</span>
       <span class="pi">-</span> <span class="na">type</span><span class="pi">:</span> <span class="s">Pods</span>
         <span class="na">value</span><span class="pi">:</span> <span class="m">1</span>
         <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>


</code></pre></div> </div> <p>Apply</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> hello-world-hpa.yaml
</code></pre></div> </div> <p>This command sets up an HPA for the <code class="language-plaintext highlighter-rouge">hello-world</code> deployment that:</p> <ul> <li>Targets 70% CPU utilization.</li> <li>Scales the number of pod replicas between 1 and 10 based on the observed CPU utilization.</li> </ul> </li> <li> <p><strong>Verify the HPA</strong></p> <p>To check the status of the HPA:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get hpa <span class="nt">-n</span> demo
</code></pre></div> </div> <p>The output should show something like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME          REFERENCE                TARGETS   MINPODS   MAXPODS   REPLICAS   AGE
hello-world   Deployment/hello-world   10%/50%   1         10        3          5m
</code></pre></div> </div> </li> </ol> <h3 id="step-3-testing-the-hpa">Step 3: Testing the HPA</h3> <p>To test the HPA, you can simulate a load on the <code class="language-plaintext highlighter-rouge">hello-world</code> pods to observe the scaling behavior. Here’s how you can do it:</p> <ol> <li> <p><strong>Run a Load Generator Pod</strong></p> <p>Create a pod that continuously sends requests to the <code class="language-plaintext highlighter-rouge">hello-world</code> service. Create a file named <code class="language-plaintext highlighter-rouge">load-generator.yaml</code> with the following content:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">load-generator</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">demo</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">busybox</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">busybox</span>
    <span class="na">args</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">/bin/sh</span>
    <span class="pi">-</span> <span class="s">-c</span>
    <span class="pi">-</span> <span class="pi">|</span>
      <span class="s">while true; do wget -q -O- http://NODE_IP_ADDRESS:8080; done</span>
</code></pre></div> </div> <p>Apply this YAML file:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> load-generator.yaml
</code></pre></div> </div> </li> <li> <p><strong>Monitor the HPA</strong></p> <p>Keep an eye on the HPA status to see how it adjusts the number of replicas:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>watch <span class="nt">-n3</span> kubectl get hpa,pod <span class="nt">-n</span> demo <span class="nt">-o</span> wide
</code></pre></div> </div> <p>You should see the <code class="language-plaintext highlighter-rouge">TARGETS</code> column indicating higher CPU utilization, and the <code class="language-plaintext highlighter-rouge">REPLICAS</code> column increasing as the HPA scales up the deployment.</p> </li> <li> <p><strong>Stop the Load Generator</strong></p> <p>Once you’ve observed the scaling behavior, delete the load generator pod:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod load-generator <span class="nt">-n</span> demo
</code></pre></div> </div> </li> </ol> <h3 id="summary">Summary</h3> <ul> <li><strong>Manual Scaling</strong>: Use <code class="language-plaintext highlighter-rouge">kubectl scale</code> to manually adjust the number of replicas.</li> <li><strong>Horizontal Pod Autoscaler (HPA)</strong>: Automatically adjusts the number of replicas based on CPU utilization (or other metrics).</li> <li><strong>Metrics Server</strong>: Required for HPA to function, it collects resource metrics.</li> <li><strong>Testing HPA</strong>: Use a load generator to simulate traffic and observe the HPA scaling behavior.</li> </ul> <p>This guide helps you understand and implement both manual and automatic scaling in Kubernetes, ensuring your applications can handle varying loads efficiently.</p> <p><strong>Congratulations !!!</strong></p> <blockquote> <p>Why did the Kubernetes developer go broke? Because he lost all his containers! 🤣</p> </blockquote> <hr/>]]></content><author><name></name></author><category term="tutorial"/><category term="Kubernetes"/><category term="k3s"/><category term="beginners"/><summary type="html"><![CDATA[A comprehensive guide to setting up a lightweight Kubernetes cluster using K3s, deploying a "Hello-World" application, and exploring Kubernetes concepts like namespaces, deployments, services, and scaling with Horizontal Pod Autoscalers.]]></summary></entry><entry><title type="html">Découvrez la puissance de la commande Linux Screen pour une gestion efficace des sessions</title><link href="https://menraromial.github.io/blog/2024/dcouvrez-la-puissance-de-la-commande-linux-screen-pour-une-gestion-efficace-des-sessions/" rel="alternate" type="text/html" title="Découvrez la puissance de la commande Linux Screen pour une gestion efficace des sessions"/><published>2024-02-21T17:17:09+00:00</published><updated>2024-02-21T17:17:09+00:00</updated><id>https://menraromial.github.io/blog/2024/dcouvrez-la-puissance-de-la-commande-linux-screen-pour-une-gestion-efficace-des-sessions</id><content type="html" xml:base="https://menraromial.github.io/blog/2024/dcouvrez-la-puissance-de-la-commande-linux-screen-pour-une-gestion-efficace-des-sessions/"><![CDATA[<p>Avez-vous déjà été dans une situation où vous travailliez sur une tâche importante dans un terminal Linux et soudainement votre connexion est interrompue, perdant ainsi tout votre travail en cours ? Ou peut-être que vous souhaitez exécuter des processus en arrière-plan sans qu’ils soient interrompus lorsque vous vous déconnectez de votre session SSH ? Si oui, alors la commande `screen` est là pour vous sauver la vie !</p> <p><strong>Qu’est-ce que la commande Screen ?</strong></p> <p><strong>screen</strong> est une application de terminal qui permet la gestion de sessions multiples à l’intérieur d’une seule fenêtre de terminal, offrant ainsi une grande flexibilité pour travailler sur plusieurs tâches simultanément. L’un de ses avantages les plus importants est la capacité à détacher une session en cours et à la reprendre plus tard depuis n’importe quel endroit, même après une déconnexion inattendue.</p> <p><strong>Principales fonctionnalités de Screen :</strong></p> <p><strong>1. Création et gestion de sessions :<br/></strong>Avec <strong>screen</strong>, vous pouvez créer plusieurs sessions en parallèle, chacune étant indépendante des autres. Cela vous permet d’organiser vos tâches de manière logique et de passer facilement entre elles.</p> <p><strong>2. Détacher et attacher des sessions :<br/></strong>L’une des fonctionnalités les plus puissantes de <strong>screen</strong> est la possibilité de détacher une session en cours tout en laissant les processus qu’elle exécute se poursuivre en arrière-plan. Plus tard, vous pouvez réattacher cette session depuis n’importe où, même après une reconnexion au système.</p> <p><strong>3. Gestion des fenêtres :<br/>screen</strong> permet également de diviser la fenêtre du terminal en plusieurs régions, chacune pouvant contenir une session différente. Cela vous permet d’afficher plusieurs sessions simultanément et de basculer entre elles facilement.</p> <p><strong>4. Support des scripts et automatisation :<br/></strong>Vous pouvez automatiser des tâches en utilisant <strong>screen</strong> dans des scripts shell, ce qui peut être extrêmement utile pour exécuter des processus en arrière-plan de manière fiable.</p> <p><strong>Utilisation de base de la commande Screen :</strong></p> <ol><li><strong>Démarrer une nouvelle session :</strong></li></ol> <blockquote>screen -S &lt;NOM_session&gt;</blockquote> <p><strong>2. Détacher une session en cours :<br/></strong> Appuyez sur `Ctrl + A`, puis `Ctrl + D`.</p> <p><strong>3. Lister les sessions en cours :<br/></strong> <br/><strong><em> screen -ls</em></strong></p> <p><strong>4. Reprendre une session détachée :<br/></strong> <br/><strong><em> screen -r &lt;NOM_session&gt;</em></strong></p> <p><strong>5. Créer une nouvelle fenêtre :<br/></strong> Appuyez sur `Ctrl + A`, puis `C`.</p> <p><strong>6. Basculer entre les fenêtres :<br/></strong> Appuyez sur `Ctrl + A`, puis le numéro de la fenêtre.</p> <p><strong>Conclusion :</strong></p> <p>La commande <strong>screen </strong>est un outil indispensable pour tout administrateur système ou développeur travaillant sur des serveurs Linux. Sa capacité à gérer efficacement les sessions et les processus en arrière-plan en fait un outil incontournable pour ceux qui travaillent régulièrement en ligne de commande. En utilisant `screen`, vous pouvez éviter les pertes de travail dues à des déconnexions inattendues et améliorer votre productivité globale. N’attendez plus, plongez dans le monde de `screen` et simplifiez la gestion de vos sessions terminal dès aujourd’hui !</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=ae025a0eef82" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry></feed>