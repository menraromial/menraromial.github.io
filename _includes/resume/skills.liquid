<div class="cv-skills">
  {% for content in data[1] %}
  <div>
    <h5>{{ content.name }}</h5>
    <ul>{% for kw in content.keywords %}<li>{{ kw }}</li>{% endfor %}</ul>
  </div>
  {% endfor %}
</div>
